追加修复：售后列表 API 拦截去重 + 只保留最后一次响应
1. 拦截售后列表API() 中，每次收到新的有效响应时 结果容器.clear() 再重新填入，确保只保留最后一次 API 响应的数据（也就是"待商家处理"tab 触发的那次）。不要累加多次响应。
2. 额外安全网：在 结果容器.append() 前用 已捕获订单号集合 按订单号去重。
3. 售后任务.执行() 中，在调用 批量写入队列() 之前，也在 Python 端按订单号去重 当前页记录，防止同一页数据被多次处理。s