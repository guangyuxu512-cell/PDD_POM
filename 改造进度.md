# 改造进度

## 本轮进度：登录态监控、结构化日志、依赖锁定、健康检查与浏览器自动恢复

- `browser/session_monitor.py`
  - 新增登录页 URL、页面文案、关键 Cookie 三段式登录态检测
  - 登录态失效时写入 `operation_logs`，并向 `session:expired` 发布 Redis 事件
  - 若已配置飞书 Webhook，则发送登录态失效告警
- `browser/recovery.py`
  - 新增浏览器恢复器，支持冷却时间、连续失败次数上限和恢复成功/失败日志
- `browser/manager.py`
  - 新增店铺实例元数据缓存、登录页自动标记、崩溃事件广播
  - 新增 `安全获取页面(...)`，在页面/上下文关闭时触发自动恢复
  - `_清理实例(...)` 增加 `browser:crashed` 日志与 Redis 事件
- `pages/base_page.py`
  - 新增 `检查并处理登录态(...)`
  - 在导航、点击、填写、文本点击、占位符填写前自动检查登录态
  - 捕获浏览器关闭类异常并统一抛出 `RuntimeError("浏览器上下文已关闭，需要恢复")`
- `tasks/execute_task.py`
  - 为批次绑定 `trace_id`
  - 增加浏览器关闭类失败的一次恢复性重试
  - 接入任务执行指标统计
- `backend/logging_config.py`
  - 新增统一日志配置，优先使用 `loguru`，依赖未安装时回退到标准库
  - 输出目录统一为 `data/logs/`
  - 支持 `trace_id` 绑定、文件轮转、错误日志拆分
- `backend/services/metrics_service.py`
  - 新增进程内指标服务，统计任务总数、成功/失败数、运行中任务、平均耗时、请求耗时和 uptime
- `backend/services/system_service.py`
  - 健康检查扩展为 `redis / sqlite / browser_pool / celery_workers`
  - 新增运行指标聚合，补充 Redis 内存、浏览器实例和 uptime
- `backend/api/system_api.py`
  - `/api/system/health` 返回结构化健康信息
  - 新增 `/api/system/metrics`
- `backend/main.py`
  - 注册根路径 `/health`
  - 新增请求耗时中间件
  - 启动/关闭过程接入统一日志和指标重置
- `requirements.txt` / `requirements-dev.txt` / `requirements-lock.txt`
  - 拆分生产依赖与测试依赖
  - 引入 `loguru`
  - 生成当前环境的锁定依赖清单
- `docs/deployment.md`
  - 补充开发/生产安装命令
  - 补充 `data/logs/` 日志目录说明
  - 补充 `/health`、`/api/system/health`、`/api/system/metrics` 探针说明
- `backend.spec` / `celery-worker.spec`
  - 补齐 `backend.logging_config`、`backend.services.metrics_service`、`browser.session_monitor`、`browser.recovery` 的 `hiddenimports`
- 批量日志替换
  - 已清理 `backend/`、`browser/`、`pages/`、`tasks/` 中全部 `print()`
  - 当前 `rg -n "print\\(" backend browser pages tasks -g "*.py"` 结果为 0
- 测试与验证
  - 已执行 `python -m compileall backend browser pages tasks`
  - 已执行 `python -m pytest -c tests/pytest.ini tests/ -v`
  - 全量验证结果：`473 passed, 16 warnings`
  - 16 条 warning 为既有第三方依赖弃用提示：`openpyxl` 与 Celery `datetime.utcnow()`

## 本轮进度：PyInstaller spec 显式收集与 onedir 路径修复

- `backend.spec`
  - 改为显式列出 `tasks / backend / browser / pages / pdd_selectors` 关键 `hiddenimports`
  - 补齐 `uvicorn`、`fastapi`、`starlette`、`celery`、`kombu`、`amqp`、`redis` 的 `collect_all`
  - 为兼容当前 PyInstaller 6.17，`collect_all(...)` 结果在 `Analysis(...)` 前合并，避免运行期 `TOC` 格式报错
- `celery-worker.spec`
  - 改为显式列出 Worker 依赖的 `hiddenimports`
  - 补齐 `celery`、`kombu`、`amqp`、`redis` 的 `collect_all`
  - 同样将 `collect_all(...)` 合并前移到 `Analysis(...)` 前，确保 spec 打包命令可直接通过
- `electron/main.js`
  - 打包模式下的 exe 路径改为 `python-backend/backend/backend.exe` 与 `python-backend/celery-worker/celery-worker.exe`
- `tests/unit/test_pyinstaller_spec_files.py`
  - 新增 spec 与 Electron 打包路径的静态回归
  - 覆盖新路径存在与旧路径消失、显式导入存在与 `collect_submodules` 移除
- 测试与验证
  - 已执行定向回归：
    - `python -m pytest -c tests/pytest.ini tests/unit/test_pyinstaller_spec_files.py tests/unit/test_electron_main.py -v`
    - `node --check electron/main.js`
  - 已按任务命令执行打包：
    - `pyinstaller --noconfirm --distpath ./python-backend-dist backend.spec`
    - `pyinstaller --noconfirm --distpath ./python-backend-dist celery-worker.spec`
  - 已执行打包产物启动验收：
    - `python-backend-dist/backend/backend.exe`
  - 已执行全量测试：
    - `python -m pytest -c tests/pytest.ini tests/ -v`
  - 全量验证结果：`459 passed, 16 warnings`
- 注意事项
  - `backend.exe` 8 秒观测内保持运行，并输出 `Application startup complete` 与 `Uvicorn running on http://127.0.0.1:8000`
  - PyInstaller 过程中仍有 `kombu.asynchronous.aws` 缺少 `botocore` 的告警，但不影响本轮 spec 打包成功
  - `.pipeline/task.md`、`backend.spec`、`celery-worker.spec` 为既有本地变更文件，本轮已按任务要求继续修改其内容


## 本轮进度：PyInstaller 冻结模式路径注入修复

- `scripts/pyinstaller_entry.py`
  - 将项目根目录注入改成仅在非 frozen 模式执行，避免 PyInstaller 运行时覆盖 `_internal` 已准备好的导入路径
- `scripts/pyinstaller_celery_entry.py`
  - 同步收口 Celery 入口的 `sys.path.insert(...)` 条件，保持后端入口和 Worker 入口一致
- `tests/unit/test_pyinstaller_entry.py`
  - 新增入口脚本回归测试，分别覆盖后端入口和 Celery 入口在 frozen / 非 frozen 两种模式下的路径行为
  - 对 Celery 入口额外断言 `构建Worker参数()` 默认返回值，避免测试只停留在导入层
- 测试与验证
  - 已执行定向回归：
    - `python -m pytest -c tests/pytest.ini tests/unit/test_pyinstaller_entry.py -v`
  - 已执行全量测试：
    - `python -m pytest -c tests/pytest.ini tests/ -v`
  - 全量验证结果：`456 passed, 16 warnings`
  - 已执行开发模式短时启动验收：
    - `python scripts/pyinstaller_entry.py`
  - 已执行任务给定的 `PyInstaller --onedir` 构建命令
- 注意事项
  - 开发模式启动 5 秒观测内进程保持运行，并输出 `Started server process`、`Waiting for application startup`
  - 按任务命令打包后，`backend.exe` 已进入 `backend.main` 导入阶段，不再出现 `No module named 'backend'`
  - 当前打包产物仍因未收集 `fastapi` 依赖而报 `ModuleNotFoundError: No module named 'fastapi'`，属于现存打包配置问题，本轮未扩大处理
  - `backend.spec` 在打包验收时被 PyInstaller 临时改写，已恢复到验收前内容
  - `.pipeline/task.md`、`backend.spec`、`build_backend.bat`、`celery-worker.spec` 为既有本地变更，本轮未继续修改其目标内容

## 本轮进度：Electron 重启端口清理与进程树回收

- `electron/main.js`
  - Windows 下启动前的代码页切换改为 `stdio: 'inherit'`
  - 增加 `System32` 命令路径常量，减少对 PATH 的依赖
  - `startBackend()` 启动前新增端口占用清理，使用 `netstat` + `findstr` 查找 `LISTENING` 进程并执行 `taskkill /F /T /PID`
  - `stopProcess(...)` 在 Windows 下改为整棵进程树终止，避免关闭 Electron 后残留 `uvicorn`/Worker 子进程
- `tests/unit/test_electron_main.py`
  - 更新 UTF-8 代码页断言
  - 新增后端端口清理和进程树终止的静态回归
- 测试与验证
  - 已执行定向回归：
    - `python -m pytest -c tests/pytest.ini tests/unit/test_electron_main.py -v`
    - `node --check electron/main.js`
  - 已执行全量测试：
    - `python -m pytest -c tests/pytest.ini tests/ -v`
  - 全量验证结果：`452 passed, 16 warnings`
  - 已尝试执行：
    - `cd electron && npx electron .`
- 注意事项
  - 当前执行环境下 GUI 验收未完成，stderr 仍报 `platform_channel.cc(83): 拒绝访问 (0x5)`
  - 当前执行环境下 Electron 子进程对 `chcp` 的执行仍受限制，stderr 仍出现“不是内部或外部命令”
  - 16 条 warning 为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示
  - `.pipeline/task.md` 为既有本地变更，本轮未修改
## 本轮进度：PyInstaller backend.exe 启动路径与 onedir 布局修复
- `scripts/pyinstaller_entry.py`
  - 冻结模式启动前切换到 `backend.exe` 所在目录，避免相对路径漂移
  - 捕获导入异常并在 exe 同级生成 `crash.log`
- `backend/config.py`
  - 冻结模式下按 `exe同级 -> exe上级 -> cwd` 搜索 `.env`
- `backend/models/database.py`
  - 冻结模式下数据库目录固定到 exe 同级 `data/`
- `electron/main.js`
  - 打包模式启动 `backend.exe` / `celery-worker.exe` 时将 `cwd` 设为各自 exe 目录
- `backend.spec`
  - 新增 `('.env', '.')`
  - 在 `EXE(...)` 中设置 `contents_directory='.'`，兼容 PyInstaller 6 的 onedir 新布局，确保 `.env` 落在 exe 同级
- `tests/unit/test_pyinstaller_entry.py`
  - 补充冻结模式切换 cwd、导入失败写 `crash.log` 的回归
  - 修复测试用例残留 `cwd` 副作用，避免影响后续相对路径测试
- `tests/unit/test_packaged_runtime_paths.py`
  - 新增打包运行时 `.env` 与数据库路径解析回归
- `tests/unit/test_electron_main.py`
  - 固定断言打包模式 `cwd: path.dirname(...)`
- `tests/unit/test_pyinstaller_spec_files.py`
  - 新增 `.env` 收集与 `contents_directory='.'` 静态断言
- 测试与验证
  - 已执行 `python -m pytest -c tests/pytest.ini tests/unit/test_pyinstaller_entry.py tests/unit/test_packaged_runtime_paths.py tests/unit/test_electron_main.py tests/unit/test_pyinstaller_spec_files.py -q`
  - 已执行 `node --check electron/main.js`
  - 已执行 `pyinstaller --noconfirm --distpath ./python-backend-dist backend.spec`
  - 已执行 `python-backend-dist/backend/backend.exe`
  - 已执行 `python -m pytest -c tests/pytest.ini tests/ -v`
- 注意事项
  - `python-backend-dist/backend/backend.exe` 已输出 `Application startup complete`
  - `.env` 已确认位于 `backend.exe` 同级，`data/ecom.db` 会在首次启动时自动创建
  - `crash.log` 未生成，说明本轮启动链路没有导入异常
  - PyInstaller 仍有 `kombu.asynchronous.aws` 缺少 `botocore` 的警告，但不影响本轮打包与启动验收
## 本轮进度：PyInstaller 打包后中文日志 UTF-8 修复
- `scripts/encoding_hook.py`
  - 新增 PyInstaller runtime hook，统一设置 `PYTHONUTF8=1` 与 `PYTHONIOENCODING=utf-8`
  - 优先使用 `reconfigure()` 切换编码，仅在原生标准流场景回退到 `TextIOWrapper`
- `scripts/pyinstaller_entry.py`
  - 冻结模式入口增加 UTF-8 双保险输出设置
  - 保持对 pytest / 开发态捕获流无副作用
- `scripts/pyinstaller_celery_entry.py`
  - 冻结模式 Worker 入口增加相同的 UTF-8 双保险逻辑
- `backend.spec`
  - 注册 `runtime_hooks=['scripts/encoding_hook.py']`
  - 新增 `('scripts/encoding_hook.py', 'scripts')`
- `celery-worker.spec`
  - 注册 `runtime_hooks=['scripts/encoding_hook.py']`
  - 新增 `('scripts/encoding_hook.py', 'scripts')`
- `electron/main.js`
  - 保留 `PYTHONUTF8` 与 `PYTHONIOENCODING`
  - `pipeLogs()` 里显式调用 `child.stdout/stderr.setEncoding('utf8')`
- `tests/unit/test_packaged_log_encoding.py`
  - 新增 runtime hook、入口双保险、spec runtime hook 注册和 Electron 日志解码的回归
- 测试与验证
  - 已执行 `python -m pytest -c tests/pytest.ini tests/unit/test_packaged_log_encoding.py tests/unit/test_pyinstaller_entry.py tests/unit/test_pyinstaller_spec_files.py tests/unit/test_electron_main.py -q`
  - 已执行 `node --check electron/main.js`
  - 已执行 `pyinstaller --noconfirm --distpath ./python-backend-dist backend.spec`
  - 已执行 `pyinstaller --noconfirm --distpath ./python-backend-dist celery-worker.spec`
  - 已执行 `python-backend-dist/backend/backend.exe`
  - 已执行 `python -m pytest -c tests/pytest.ini tests/ -v`
- 注意事项
  - 以 UTF-8 读取 `python-backend-dist/backend/startup-utf8.log` 时，中文日志正常，无乱码
  - `backend` 产物中的 `scripts/encoding_hook.py` 位于根目录，`celery-worker` 产物位于 `_internal/scripts/encoding_hook.py`
  - `cd electron && npx electron .` 在当前环境仍因 `platform_channel.cc(83): 拒绝访问 (0x5)` 提前退出，未完成 GUI 侧最终验收
  - PyInstaller 构建阶段仍有 `kombu.asynchronous.aws` 缺少 `botocore` 的警告，但不影响本轮编码修复验收
## 本轮进度：流程执行清理残留 flow_params，避免同店铺重复投递首步任务
- `backend/services/execute_service.py`
  - 新增店铺级待执行 `flow_params` 清理逻辑
  - 读取存量待执行记录后按 `id` 倒序仅保留最新一条
  - 其余残留记录直接更新为 `skipped`，避免后续再次被拉起
  - 空上下文店铺仍保持原有启动逻辑
  - `input_set_id` 输入集创建出来的兼容 `flow_params` 不走本次残留清理
- `tests/unit/test_execute_service.py`
  - 调整 barrier 首步场景回归，改为断言同店铺残留记录只保留最新一条
  - 新增非 barrier 首步遇到残留记录时只投递一次首步任务的回归
- 测试与验证
  - 已执行 `python -m pytest -c tests/pytest.ini tests/unit/test_execute_service.py -q`
  - 已执行 `python -m pytest -c tests/pytest.ini tests/unit/test_batch_execute_shop_name.py -q`
  - 已执行 `python -m pytest -c tests/pytest.ini tests/ -v`
- 注意事项
  - 本轮修复仅作用于“直接读取数据库里已有待执行 `flow_params`”的流程启动分支
  - 全量回归结果为 `485 passed, 16 warnings`
## 本轮进度：流程编排弹窗改为紧凑表格行布局
- `frontend/src/views/FlowManage.vue`
  - 将流程编辑弹窗从大卡片步骤改为紧凑表格行布局，步骤列包含拖拽手柄、序号、任务、失败策略、同步屏障、合并执行和删除操作
  - 弹窗宽度调整为 `min(80vw, 900px)`，并通过 `:deep(.modal-container)` 将最大高度限制为 `80vh`
  - 顶部流程名称 / 流程说明保持并排单行输入，底部改为表格外的 `+ 添加步骤` 按钮
  - 新增原生拖拽插入线、拖拽状态高亮、步骤新增后任务下拉自动聚焦，以及保存前的步骤数量与任务选择校验
  - 保留现有后端 payload 结构与流程预览卡片逻辑，不改动 API 交互
- `tests/unit/test_flow_manage_editor_static.py`
  - 新增流程编辑器静态回归，覆盖弹窗尺寸、表格列结构、拖拽插入线、自动聚焦和保存前校验文案
- 测试与验证
  - 已执行 `python -m pytest -c tests/pytest.ini tests/unit/test_frontend_display_details.py tests/unit/test_flow_manage_editor_static.py -v`
  - 已执行 `npx --prefix frontend vue-tsc -b frontend/tsconfig.json`
  - 已执行 `python -m pytest -c tests/pytest.ini tests/ -v`
  - 全量结果：`487 passed, 16 warnings`
- 注意事项
  - `npm --prefix frontend run build` 在当前环境仍因 `esbuild` 子进程 `spawn EPERM` 失败，属于现有环境限制
  - `.pipeline/task.md` 为既有本地改动，本轮未修改
## 本轮进度：流程管理页统计栏与模板列表压缩
- `frontend/src/views/FlowManage.vue`
  - 删除顶部三张统计卡片，改成单行 `inline-stats` 文案，减少首屏纵向占用
  - 将模板列表从 `flow-grid / flow-card` 卡片网格切换为紧凑 `flow-table`
  - 表格列固定为序号、流程名称、描述、步骤数、步骤摘要、操作，行高压到 `44px`
  - 流程名称改为链接形式，点击即可打开编辑弹窗；步骤数改为 `step-badge`，操作按钮改为 `btn-sm`
  - 新增 `getStepSummary(flow)` 统一拼接步骤摘要，保留现有编辑弹窗与 API 交互不变
- `tests/unit/test_flow_manage_list_static.py`
  - 新增流程管理列表静态回归，覆盖单行统计、表格列结构、流程名称点击编辑，以及旧卡片类名已移除
- 测试与验证
  - 已执行 `python -m pytest -c tests/pytest.ini tests/unit/test_frontend_display_details.py tests/unit/test_flow_manage_editor_static.py tests/unit/test_flow_manage_list_static.py -v`
  - 已执行 `npx --prefix frontend vue-tsc -b frontend/tsconfig.json`
  - 已执行 `python -m pytest -c tests/pytest.ini tests/ -q`
  - 全量结果：`489 passed, 16 warnings`
- 注意事项
  - 本轮未重新执行 `npm --prefix frontend run build`；当前环境此前已知存在 `esbuild` 子进程 `spawn EPERM` 限制
  - `.pipeline/task.md` 为既有本地改动，本轮未修改
## 本轮进度：流程编排弹窗步骤行继续压缩
- `frontend/src/views/FlowManage.vue`
  - 将 `.step-row` 从 `48px` 高度压缩到 `40px`，并同步收紧 `padding` 与 `border-radius`
  - 将步骤表头 `.step-table-header` 压缩到 `36px` 高度，减少弹窗内垂直占用
  - 将步骤区 `select / input[number]` 高度统一压缩到 `32px`，圆角改为 `6px`
  - 将拖拽手柄缩为 `24px * 24px`，删除按钮缩为 `26px * 26px`
  - 将 `.step-table-body` 和相邻步骤行间距继续压缩，目标是 6 步流程在弹窗内更容易一屏显示
- `tests/unit/test_flow_manage_editor_static.py`
  - 新增步骤行尺寸静态断言，覆盖表头高度、表单控件高度、步骤行高度、拖拽手柄尺寸和删除按钮尺寸
- 测试与验证
  - 已执行 `python -m pytest -c tests/pytest.ini tests/unit/test_frontend_display_details.py tests/unit/test_flow_manage_editor_static.py tests/unit/test_flow_manage_list_static.py -v`
  - 已执行 `npx --prefix frontend vue-tsc -b frontend/tsconfig.json`
  - 已执行 `python -m pytest -c tests/pytest.ini tests/ -q`
  - 全量结果：`490 passed, 16 warnings`
- 注意事项
  - 本轮仅调整样式密度，未改动拖拽排序、checkbox、删除和保存逻辑
  - 本轮未重新执行 `npm --prefix frontend run build`；当前环境此前已知存在 `esbuild` 子进程 `spawn EPERM` 限制
  - `.pipeline/task.md` 为既有本地改动，本轮未修改
## 本轮进度：批量执行页与定时任务页统一改为紧凑表格
- `frontend/src/views/BatchExecute.vue`
  - 将页面右侧执行状态区从子组件展示切换为页面内直接渲染的 `status-table`
  - 表格列固定为店铺名称、当前步骤、进度、状态、耗时、操作，行高收口到 `44px`
  - 状态列改为彩色标签：等待中 / 执行中 / 已完成 / 失败 / 已停止
  - 进度列改为 CSS 进度条，顶部新增单行批次汇总文字
  - 每行新增“查看详情”，展开后可查看步骤明细表，不影响原有 SSE 更新链路
- `frontend/src/views/ScheduleManage.vue`
  - 删除 `schedule-grid / schedule-card` 卡片式列表，改为统一 `schedule-table`
  - 列改为开关、任务名称、执行流程、执行周期、上次执行、下次执行、目标店铺数、操作
  - 开关列改为 `Switch`，编辑和删除保留在操作列
  - 顶部统计区改为单行 `inline-stats`，提升首屏可见数量
  - 新建/编辑弹窗宽度改为 `min(80vw, 900px)`，并通过 `:deep(.modal-container)` 限制最大高度为 `80vh`
- `tests/unit/test_batch_execute_schedule_static.py`
  - 新增前端静态回归，覆盖批量执行页表格结构、状态标签、进度条、详情入口，以及定时任务页表格、开关列、弹窗尺寸和旧卡片结构移除
- 测试与验证
  - 已执行 `python -m pytest -c tests/pytest.ini tests/unit/test_batch_execute_schedule_static.py tests/unit/test_frontend_management_page.py tests/unit/test_batch_execute_shop_name.py -v`
  - 已执行 `npx --prefix frontend vue-tsc -b frontend/tsconfig.json`
  - 已执行 `python -m pytest -c tests/pytest.ini tests/ -q`
  - 全量结果：`494 passed, 16 warnings`
- 注意事项
  - 首次全量回归时，`tests/unit/test_anti_detection.py::test_随机延迟在范围内` 出现一次调度抖动导致的超时；单测复跑通过，随后全量复跑通过
  - 本轮未执行 `npm --prefix frontend run build`；当前环境此前已知存在 `esbuild` 子进程 `spawn EPERM` 限制
  - `.pipeline/task.md` 为既有本地改动，本轮未修改
## 本轮进度：SPA 首页禁缓存，避免 Electron 重启后看到旧 UI
- `backend/main.py`
  - 在 `挂载前端静态资源(...)` 的 SPA 回退分支里，给 `index.html` 的 `FileResponse` 增加禁缓存头
  - 仅影响回退到单页入口的响应，不改 `/assets/` 目录下带 hash 的静态资源缓存行为
- `tests/unit/test_startup_entry.py`
  - 为首页回退响应增加 `cache-control / pragma / expires` 断言
  - 同时断言 `/assets/app.js` 没有被写入同样的禁缓存头，避免误伤静态资源缓存
- 测试与验证
  - 已执行 `python -m pytest -c tests/pytest.ini tests/unit/test_startup_entry.py -v`
  - 已执行 `python -m pytest -c tests/pytest.ini tests/ -q`
  - 全量结果：`494 passed, 16 warnings`
- 注意事项
  - 本轮只改后端 SPA 回退响应头，不涉及前端资源构建逻辑
  - 16 条 warning 仍是既有第三方依赖 `celery` 与 `openpyxl` 的弃用提示
  - `.pipeline/task.md` 为既有本地改动，本轮未修改
## 本轮进度：SPA 禁缓存收口、Redis 连接池复用与 frozen task 模块清单自动生成
- `backend/main.py`
  - SPA 回退到 `index.html` 时显式返回 `text/html`
  - 补齐 `Cache-Control / Pragma / Expires` 禁缓存响应头，确保 Electron 重启后不会复用旧 HTML
  - `/assets/` 下带 hash 的静态资源继续保持原有缓存策略
- `backend/services/execute_service.py`
  - 新增模块级同步 / 异步 Redis 连接池，统一复用连接而不是每次 `from_url(...)`
  - `同步获取Redis客户端()`、取消标记相关异步函数和 `执行服务` 内部异步客户端统一切到连接池模式
  - 增加按事件循环重建异步连接池的逻辑，避免 pytest 切换 event loop 时复用旧池导致失败
  - 异步客户端关闭统一改为 `await 客户端.aclose()`，保持连接归还语义一致
- `tasks/registry.py`
  - 删除硬编码 `_FROZEN_TASK_MODULES`
  - frozen 模式改为优先读取 `tasks._frozen_modules.MODULES`
  - 若生成文件缺失则记录 warning 并返回空列表，非 frozen 模式仍保持 `pkgutil` 动态扫描
- `backend.spec`
  - 构建阶段自动扫描 `tasks/*.py` 并生成 `tasks/_frozen_modules.py`
  - 补充 `tasks._frozen_modules` 到 `hiddenimports`
- `.gitignore`
  - 新增 `tasks/_frozen_modules.py` 忽略规则，避免构建产物误提交
- `tests/unit/test_startup_entry.py`
  - 回归补充 SPA 回退响应的 `text/html` 与禁缓存头断言
  - 同时确认 `/assets/app.js` 未被误加禁缓存头
- `tests/unit/test_execute_service.py`
  - 补充同步 / 异步 Redis 连接池复用断言
  - 覆盖空批次 ID 的快速返回与取消标记相关异步客户端关闭路径
- `tests/unit/test_task_registry.py`
  - 补充 frozen 模式读取 `tasks._frozen_modules.MODULES` 的回归
  - 覆盖 `_frozen_modules.py` 缺失时 warning + 空列表分支
- `tests/unit/test_pyinstaller_spec_files.py`
  - 补充 `backend.spec` 自动生成 `_frozen_modules.py` 逻辑与 `.gitignore` 忽略项的静态断言
- 测试与验证
  - 已执行 `python -m pytest -c tests/pytest.ini tests/unit/test_startup_entry.py tests/unit/test_execute_service.py tests/unit/test_task_registry.py tests/unit/test_task_registry_extension.py tests/unit/test_pyinstaller_spec_files.py -v`
  - 已执行 `python -m pytest -c tests/pytest.ini tests/ -q`
  - 全量结果：`500 passed, 18 warnings`
- 注意事项
  - 18 条 warning 中，16 条仍是既有第三方依赖 `celery` 与 `openpyxl` 的 `datetime.utcnow()` 弃用提示
  - 新增 2 条 warning 为 `PytestUnraisableExceptionWarning`，来自 `tests/unit/test_task_service.py` 中 Redis asyncio `StreamWriter.__del__` 在事件循环关闭后的清理时机
  - 当前任务只要求调整 `backend.spec`，本轮未扩展修改 `celery-worker.spec`
  - `.pipeline/task.md` 为既有本地改动，本轮未修改
## 本轮进度：platform 字段、多平台基础注册与前端平台切换
- `backend/models/shop_model.py`
  - `shops` 表结构新增 `platform` 字段，默认值设为 `pdd`
- `backend/models/flow_model.py`
  - `flows` 表结构新增 `platform` 字段，流程模型默认归属 `pdd`
- `backend/models/database.py`
  - `task_logs` 建表 SQL 新增 `platform`
  - 启动初始化时补齐旧库 `shops / flows / task_logs` 的 `platform` 字段
  - 对旧数据执行 `platform='pdd'` 回填，重复执行不报错
- `backend/models/data_structure.py`
  - 店铺创建请求、店铺响应、流程响应、任务日志响应补齐 `platform`
- `backend/services/shop_service.py`
  - 店铺分页查询新增 `platform` 过滤
  - 新建店铺时写入归一化后的 `platform`
- `backend/api/shop_api.py`
  - `GET /api/shops` 支持 `platform` 查询参数
- `backend/services/flow_service.py`
  - 流程列表支持 `platform` 过滤
  - 新建流程默认落库 `platform='pdd'`
- `backend/api/flow_api.py`
  - `GET /api/flows` 支持 `platform` 查询参数
- `backend/api/platform_api.py`
  - 新增统一平台列表接口 `GET /api/platforms`
  - 通过平台注册表返回平台清单，响应仍保持 `成功()` 结构
- `backend/api/router.py`
  - 注册平台接口路由
- `platforms/`
  - 新增多平台基础注册框架和 `PddPlatform`
  - 提供 `register_platform / get_platform / list_platforms`
- `frontend/src/api/platforms.ts`
  - 新增平台列表 API 封装
- `frontend/src/stores/platform.ts`
  - 新增平台 Pinia store
  - 当前平台持久化到 `localStorage.selectedPlatform`
  - 首次加载或接口失败时默认回退到 `pdd`
- `frontend/src/components/PlatformSelector.vue`
  - 新增全局平台选择器，样式对齐当前深色侧边栏
- `frontend/src/App.vue`
  - 侧边栏挂载平台选择器，作为全局切换入口
- `frontend/src/api/types.ts`
  - `Shop` / `ShopPayload` 新增 `platform`
  - 新增 `Platform` 类型供 API 与 store 复用
- `frontend/src/api/shops.ts`
  - 店铺列表请求支持拼接 `platform` 查询参数
- `frontend/src/views/ShopManage.vue`
  - 店铺列表按当前平台自动加载
  - 平台切换时自动刷新
  - 新建店铺自动绑定当前平台，不增加额外表单项
- `frontend/src/components/ShopCard.vue`
  - 改用统一 `Shop` 类型，消除 `platform` 字段接线后的类型分叉
- `tests/unit/test_platform_backend.py`
  - 新增后端回归，覆盖 migration 回填、店铺/流程平台过滤、平台接口和平台注册表异常分支
- `tests/unit/test_platform_frontend_static.py`
  - 新增前端静态回归，覆盖平台 API、Pinia store、全局选择器和店铺页接线
- 测试与验证
  - 已执行 `python -m pytest -c tests/pytest.ini tests/unit/test_platform_backend.py tests/unit/test_platform_frontend_static.py tests/unit/test_shop_and_flow_api.py tests/unit/test_database_model.py tests/unit/test_frontend_management_page.py -v`
  - 已执行 `npx --prefix frontend vue-tsc -b frontend/tsconfig.json`
  - 已执行 `python -m pytest -c tests/pytest.ini tests/ -v`
  - 全量结果：`505 passed, 18 warnings`
- 注意事项
  - `GET /api/platforms` 当前只返回 `pdd`，后续新增平台时只需补充平台注册
  - 本轮未执行 `npm --prefix frontend run build`；当前环境此前已知存在 `esbuild` 子进程 `spawn EPERM` 限制
  - 18 条 warning 中，16 条仍来自既有第三方依赖 `celery` 与 `openpyxl` 的 `datetime.utcnow()` 弃用提示，另外 2 条为既有 `PytestUnraisableExceptionWarning`
  - `.pipeline/task.md` 为既有本地改动，本轮未修改
