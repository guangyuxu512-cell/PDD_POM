
## Prompt 149：结构审计与文档校准

### 审计结论

- 单一职责：未完全做到，核心执行链路存在明显的“大而全”服务文件
- 重复造轮子：存在，主要集中在 Celery 异步桥接、前端遗留面板和导入解析能力
- 屎山代码：存在，但主要集中在少数核心热点文件，不是全仓库普遍失控

### 已同步文档

- `AGENTS.md`
  - 补充当前主路由结构、settings 配置来源、技术债提醒和新增红线
- `docs/architecture.md`
  - 改成当前真实入口、页面容器结构、关键调用链和技术债热点
- `docs/coding-style.md`
  - 明确禁止跨 service 调 `_私有方法`、禁止 API 层 monkey patch、统一复用 `tasks/async_utils.py`
- `docs/frontend.md`
  - 改成当前 `/shops`、`/business`、`/data`、`/monitor`、`/browser`、`/settings` 结构
- `docs/testing.md`
  - 改成当前 `tests/unit` 为主、`tests/单元测试` 兼容保留的现状，移除过期通过数
- `docs/deployment.md`
  - 改成 settings 表 + `backend/config.py` 的配置模型，修正启动命令与运行目录说明

### 优先修复建议

1. 拆分 `backend/services/execute_service.py`
   - 目标拆成执行状态仓储、批次调度、流程预检、停止取消控制等子模块，避免继续堆在同一文件
2. 拆分 `backend/services/task_service.py`
   - 把任务记录、参数准备、浏览器生命周期、流程续跑拆开，降低回归面
3. 提取共享导入解析层
   - 从 `backend/services/task_params_service.py` 中抽出 CSV/XLSX 解析、店铺标识解析、字段规范化与值转换
   - `backend/services/flow_input_service.py` 不再直接调用这些 `_私有方法`
4. 去掉 `backend/api/task_api.py` 中的运行时 monkey patch
   - 用显式策略参数、专门的上下文准备器或内部执行适配器替代 `_准备任务参数 = _准备流程上下文`
5. 清理重复的 Celery 异步桥接实现
   - `tasks/bridge_task.py` 与 `tasks/scheduled_task.py` 统一改用 `tasks/async_utils.py`

### 中期优化建议

- 拆分 `frontend/src/views/BatchExecute.vue`
  - 把配置面板、状态面板、执行时间线和请求编排拆开
- 清理 `frontend/src/views/batch-execute/ExecuteConfigPanel.vue` 与 `BatchStatusPanel.vue`
  - 要么重新接回主页面，要么删除，避免继续形成双实现
- 为超大文件建立改造阈值
  - service、view、store 超过一定体量时，后续需求默认伴随拆分
- 补一轮结构性回归测试
  - 优先覆盖执行链路拆分后最容易回归的批量执行、流程输入、任务续跑和取消逻辑

### 验证

- `python -m pytest -c tests/pytest.ini -q`
- 结果：`525 passed, 18 warnings`
- warnings 仍来自既有第三方依赖 `celery`、`openpyxl` 与既有 `PytestUnraisableExceptionWarning`

## Prompt 150：执行链路拆分与批量执行遗留清理

### 已完成修复

- [x] 拆分 `backend/services/task_service.py`
  - 新增 `task_param_runtime_service.py`、`task_execution_context_service.py`、`task_flow_service.py`
  - `task_service.py` 改为协调层，任务参数运行时、浏览器上下文准备和流程续跑逻辑分别下沉
  - 保留旧方法名作为兼容包装，减少现有调用方和测试 patch 的回归面
- [x] 拆分 `backend/services/execute_service.py`
  - 新增 `execute_state_store.py`，承接执行状态读写与缓存访问
  - 新增 `execute_flow_precheck_service.py`，承接流程预检与元数据校验
  - `execute_service.py` 先收口为调度协调层，保留旧导出与 patch 点
- [x] 提取共享导入解析层
  - 新增 `import_parser_service.py`，统一 CSV/XLSX 解析、店铺标识解析、字段规范化和值转换
  - `task_params_service.py` 与 `flow_input_service.py` 不再跨 service 调私有解析方法
- [x] 去掉 `backend/api/task_api.py` 中的运行时 monkey patch
  - 内部执行链路显式传递 `flow_context`，不再依赖 `_准备任务参数 = _准备流程上下文`
- [x] 清理重复的 Celery 异步桥接实现
  - `tasks/bridge_task.py` 与 `tasks/scheduled_task.py` 统一改用 `tasks/async_utils.py`
- [x] 清理批量执行遗留双实现
  - 删除 `frontend/src/views/batch-execute/ExecuteConfigPanel.vue`
  - 删除 `frontend/src/views/batch-execute/BatchStatusPanel.vue`
  - 删除 `frontend/src/views/batch-execute/BatchStatusPanel.css`

### 仍需继续优化

- [ ] 拆分 `frontend/src/views/BatchExecute.vue`
  - 当前仍聚合配置、状态、时间线和请求编排，体量偏大
- [ ] 继续拆分 `backend/services/execute_service.py`
  - 本轮已下沉状态存储与流程预检，但批次调度、停止取消控制仍可继续独立
- [ ] 为超大文件建立改造阈值
  - 后续 service、view、store 超过阈值时，默认伴随拆分而不是继续叠加需求
- [ ] 补结构性回归测试
  - 优先覆盖执行链路拆分后最容易回归的批量执行、流程输入、任务续跑和取消逻辑

### 验证

- `python -m pytest tests/unit/test_task_service.py tests/unit/test_task_service_browser_reuse.py tests/unit/test_execute_service.py tests/unit/test_task_api_internal_exec.py tests/unit/test_celery_bridge.py tests/unit/test_flow_input_service.py tests/unit/test_task_params_service.py tests/unit/test_batch_execute_schedule_static.py tests/unit/test_batch_execute_shop_name.py -q`
- 结果：`64 passed, 3 warnings`
- `python -m pytest -c tests/pytest.ini -q`
- 结果：`525 passed, 18 warnings`
- warnings 仍来自既有第三方依赖 `celery`、`openpyxl` 与既有 `PytestUnraisableExceptionWarning`
