任务目标： 在系统设置页面，为 Redis、验证码服务、飞书 Webhook 各添加"测试连接"按钮，点击后调用已有后端接口并显示结果。
需要修改的文件：
frontend/src/views/SystemSettings.vue
frontend/src/api/system.ts（如不存在则新建）
Step 1：新建或补全 frontend/src/api/system.ts
新增以下三个函数，统一用 POST 调用后端测试接口：

export async function testRedis(redisUrl?: string): Promise<{ success: boolean; message: string; data?: { latency_ms: number } }>
  → POST /api/system/test-redis
  → body: { redis_url: redisUrl ?? null }

export async function testCaptcha(apiKey?: string): Promise<{ success: boolean; message: string }>
  → POST /api/system/test-captcha
  → body: { api_key: apiKey ?? null }

export async function testFeishu(webhookUrl?: string, secret?: string): Promise<{ success: boolean; message: string }>
  → POST /api/system/test-feishu-webhook
  → body: { webhook_url: webhookUrl ?? null, secret: secret ?? null }

统一错误处理：catch 后返回 { success: false, message: error.message }
​
Step 2：修改 SystemSettings.vue，在对应设置项旁边加测试按钮
逻辑规则（不要硬编码所有 key，只判断以下 3 个）：

1. key === 'redis_url'
   → 该输入框右侧加【测试连接】按钮
   → 点击调用 testRedis(formData['redis_url'])
   → 显示结果：成功时显示绿色 "连接成功，延迟 XXms"，失败显示红色错误信息

2. key === 'yes_captcha_client_key'（或 captcha 相关 key，看实际数据库中存的 key 名）
   → 该输入框右侧加【测试连接】按钮
   → 点击调用 testCaptcha(formData[item.key])
   → 显示结果

3. key === 'feishu_webhook_url'
   → 该输入框右侧加【测试连接】按钮
   → 点击调用 testFeishu(formData['feishu_webhook_url'], formData['feishu_webhook_secret'])
   → 显示结果

按钮状态：
- 请求中：禁用 + 显示"测试中..."
- 完成后：3 秒后自动清除结果提示

建议将测试结果用 ref<Record<string, { ok: boolean; msg: string } | null>>({}) 按 key 存储，
避免多个测试按钮状态互相干扰。
​
Step 3：验收方式
1. 启动后端，在设置页填入 Redis 地址（如 redis://127.0.0.1:6379/0）
2. 点击【测试连接】，期望看到"连接成功，延迟 Xms"
3. 填入错误地址，期望看到红色"连接失败"提示
4. 飞书 Webhook 同理测试
5. 确认测试按钮 loading 状态正常，不重复触发