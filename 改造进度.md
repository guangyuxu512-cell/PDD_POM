# 改造进度

## 总体状态

- 已完成 6/6 个改造步骤
- 每一步完成后均执行了 `pytest tests/`
- 当前现有测试结果稳定为：`29 passed`

## 本轮补充对齐（根据 `建议.md`）

- 补齐 Worker 级事件循环复用，避免 `tasks/桥接任务.py` 对每个 Celery 任务重复 `asyncio.run(...)` 后关闭循环，影响同进程浏览器实例复用
- `tasks/celery应用.py`
  - 新增 `获取Worker事件循环()`
  - 在 `worker_init` 初始化时创建并绑定 Worker 级事件循环
  - 在 `worker_shutdown` 清理时关闭事件循环并重置状态
- `tasks/桥接任务.py`
  - `_运行异步任务(...)` 改为复用 Worker 级事件循环执行协程
  - 继续保留桥接函数内的 `初始化Worker环境()` 懒加载兜底
- `backend/services/任务服务.py`
  - `统一执行任务(...)` 改为直接调用 `backend/services/浏览器服务.py` 的 `确保已初始化()`
  - 保留 HTTP 与 Celery 共用同一执行入口的结构，不改动现有 HTTP 触发链路
- 新增回归测试：
  - `tests/单元测试/测试_Celery桥接.py`
  - `tests/单元测试/测试_启动入口.py`
- 本轮验证命令：`python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：`20 passed`

## 本轮新增任务（Redis 连接检测接口）

- `backend/api/系统接口.py`
  - 新增 `POST /api/system/test-redis`
  - 支持请求体传入 `redis_url`，为空时回退到 `配置实例.REDIS_URL`
  - 使用 `redis.asyncio` 执行 `ping()`，统计 `latency_ms`
  - 无论成功失败都在 `finally` 中关闭 Redis 客户端
- `backend/models/数据结构.py`
  - 新增 `Redis连接测试请求`
- `frontend/src/views/Settings.vue`
  - “测试连接”按钮接入真实接口
  - 增加按钮 loading 状态
  - 使用 toast 展示成功与失败提示
- `requirements.txt`
  - 明确 Redis 依赖版本为 `redis>=5.0.0`
- 新增回归测试：
  - `tests/单元测试/测试_系统接口.py`
- 本轮验证命令：`python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：`22 passed`

## 本轮新增任务（代码健壮性审查）

- `backend/services/任务服务.py`
  - 为 `统一执行任务(...)` 中的浏览器初始化、自动打开浏览器、任务执行增加超时与异常包装
  - 补充页面对象为空时的显式失败兜底
- `tasks/桥接任务.py`
  - `_运行异步任务(...)` 在 Worker 事件循环不可用时回退到 `asyncio.run(...)`
  - Worker 事件循环运行协程失败时转换为明确的运行时错误
- `tasks/celery应用.py`
  - `worker_init` / `worker_shutdown` 信号钩子改为最佳努力模式，异常仅记录不外抛
  - Worker 事件循环关闭失败时只记录不影响退出流程
- `backend/services/浏览器服务.py`
  - 新增与当前事件循环绑定的 `asyncio.Lock`
  - 为 `确保已初始化()` / `初始化浏览器()` 增加并发保护与初始化超时
- `backend/services/心跳服务.py`
  - 循环发送心跳时对上报异常、等待停止事件异常统一吞吐并记录
  - 停止心跳任务时的异常不再影响主服务关闭
- `backend/api/系统接口.py`
  - Redis 连接关闭动作增加 finally 内的超时与异常保护，避免覆盖主响应
- 新增回归测试：
  - `tests/单元测试/测试_浏览器服务.py`
  - `tests/单元测试/测试_心跳服务.py`
  - `tests/单元测试/测试_任务服务.py`
  - 扩展 `tests/单元测试/测试_Celery桥接.py`
  - 扩展 `tests/单元测试/测试_系统接口.py`
- 本轮验证命令：`python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：`29 passed`

## 第一步：修正 Celery 基础接线

- 修改 `tasks/celery应用.py`
- 修正 Celery 配置读取，统一改为使用 `配置实例.REDIS_URL`
- 为恢复现有测试通过，同时补了若干兼容性修复：
  - `tasks/登录任务.py`
  - `pages/登录页.py`
  - `pages/基础页.py`
  - `browser/反检测.py`

## 第二步：整理任务服务统一入口

- 修改 `backend/services/任务服务.py`
- 新增 `创建任务记录(...)`
- 新增 `统一执行任务(...)`
- 保留 HTTP 触发链路的原有行为：先返回 `pending`，再后台执行
- 统一请求字段为 `task_name`

关联修改：

- `backend/models/数据结构.py`
- `backend/api/任务接口.py`
- `frontend/src/views/TaskMonitor.vue`
- `frontend/src/api/mock.ts`

## 第三步：新增 Celery 桥接任务

- 新增 `tasks/桥接任务.py`
- 提供唯一桥接函数 `桥接执行任务(...)`
- 桥接函数支持：
  - 直接复用已有 `task_id`
  - 缺少 `task_id` 时自动创建任务记录
  - 使用 `task_name` 作为统一任务名称字段

## 第四步：补齐 Worker 初始化

- 修改 `tasks/celery应用.py`
- 使用 `worker_init` 适配 `solo` 模式 Worker
- Worker 启动时初始化：
  - 任务注册表
  - `AGENT_CALLBACK_URL` 回调地址
- 修改 `tasks/桥接任务.py`
- 增加桥接函数内部的懒加载兜底初始化

## 第五步：新增心跳服务

- 新增 `backend/services/心跳服务.py`
- 修改 `backend/配置.py`
- 修改 `.env`
- 新增配置项：
  - `AGENT_MACHINE_ID`
  - `AGENT_HEARTBEAT_URL`
- 心跳服务每 30 秒发送一次在线心跳，失败静默忽略

## 第六步：接入生命周期并更新文档

- 修改 `backend/启动入口.py`
- 在 FastAPI `lifespan` 启动阶段：
  - 初始化任务注册表
  - 设置回调地址
  - 启动心跳服务
- 在关闭阶段：
  - 停止心跳服务
  - 关闭数据库

文档更新：

- 修改 `CLAUDE.md`
- 新增 Celery Worker 桥接说明
- 新增心跳服务说明
- 新增 `.env` 配置项说明
- 新增 Worker 启动命令说明

## 当前 Worker 启动命令

```bash
celery -A tasks.celery应用 worker -Q machine_a -P solo
```

## 本轮新增任务（重写 AGENTS 文档）

- `AGENTS.md`
  - 按 `建议.md` 要求重写为完整仓库说明文档
  - 覆盖 13 个章节：项目概述、技术栈、结构、命名规范、API 规范、核心模块、数据模型、外部集成、环境变量、开发部署、测试、协作规范、注意事项与红线
  - 整合 `CLAUDE.md`、根 `AGENTS.md`、`.claude/*`、`.roles/*` 中的协作规则与红线
  - 基于实际代码列出目录结构、入口文件、API 路由、SQLite 表结构、环境变量、外部依赖与调用链路
  - 对缺失项明确标注“当前项目暂无此内容”，包括 Docker、`docker-compose.yml`、`pyproject.toml`、`.env.example`、认证鉴权、数据库迁移、PR 流程、根 README
  - 在文末补充生成日期 `2026-03-10` 与“基于代码自动生成，请人工审核后使用”提示

## 本轮新增任务（拆分 AGENTS 文档）

- `docs/architecture.md`
  - 收纳项目概述、整体架构关系、完整目录树、入口文件、分层设计、核心模块与关键调用链路
- `docs/api-spec.md`
  - 收纳统一响应、分页协议、错误处理、认证现状、路由清单与请求响应模型
- `docs/database.md`
  - 收纳 SQLite 表结构、字段说明、逻辑关联关系与 Pydantic 数据模型补充
- `docs/coding-style.md`
  - 收纳代码风格、命名边界、分层约束、测试命名与常见红线
- `docs/callback.md`
  - 收纳 Celery Worker、Redis、统一执行入口、自动回调、心跳与职责边界
- `docs/testing.md`
  - 收纳测试框架、目录、测试文件、覆盖范围、命名规则与当前测试现状
- `docs/frontend.md`
  - 收纳前端路由、主布局、页面职责、公共组件、数据层与页面接口映射
- `docs/deployment.md`
  - 收纳技术栈详细版本、环境变量、启动命令、运行依赖、外部集成配置与部署现状
- `AGENTS.md`
  - 精简为仓库级速览，保留技术栈概要、命名规范、架构核心原则、协作规范、禁止事项与 `docs/` 索引

## 本轮新增任务（清理历史任务文档）

- 删除以下历史/一次性文档：
  - `ISSUES.md`
  - `建议.md`
  - `Agent桥接与心跳功能改造建议.md`
- `docs/architecture.md`
  - 同步移除目录树中对上述已删除文档的引用，避免文档索引失真

## 本轮新增任务（外部机器接入测试脚本）

- `scripts/machine_worker.py`
  - 新增外部机器模拟脚本，启动时调用注册接口
  - 后台按 `idle/running` 状态定时调用心跳接口
  - 直接订阅 Celery 队列 `worker.{machine_id}`，收到任务后打印消息、模拟执行并回调结果
  - 注册、心跳、回调路径均支持命令行或环境变量覆盖
- `scripts/dispatch_test.py`
  - 新增派发测试脚本，支持按 `machine_id` 调用 `POST /api/task-dispatches/echo-test`
  - 支持附加 JSON 请求字段、打印服务端返回内容和提取任务 ID
  - 可选轮询任务状态，默认状态查询模板为 `/api/tasks/{task_id}`
- 兼容性说明：
  - 仓库当前没有 `/api/machines/*` 与 `/api/task-dispatches/*` 的后端源码
  - 脚本默认按 `.pipeline/task.md` 约定路由工作，并将相关路径全部做成可配置项，便于直接对接外部服务
- 本轮验证命令：
  - `python scripts/machine_worker.py --help`
  - `python scripts/dispatch_test.py --help`
  - `python -m py_compile scripts/dispatch_test.py scripts/machine_worker.py`

## 本轮新增任务（机器接入脚本对齐 Agent 既有 API）

- `scripts/machine_worker.py`
  - 默认连接地址改为 Agent 端口 `http://localhost:8001`
  - 注册接口改为 `POST /api/machines`，上报 `{machine_id, machine_name}`
  - 心跳接口改为 `POST /api/machine/heartbeat`，上报 `{machine_id, shadowbot_running}`
  - 状态回调改为 `PUT /api/machines/{machine_id}/status`，通过 `X-RPA-KEY` 鉴权并上报 `{status}`
  - 删除可配置的注册/心跳/回调路径，统一从 `--server-url` 推导 Agent 路由
  - 新增 `--machine-name` 参数，默认回退为 `machine-id`
  - 启动即注册并回调 `idle`，任务执行期间更新 `running`，失败更新 `error`，退出更新 `offline`
- `tests/单元测试/测试_机器接入脚本.py`
  - 新增脚本单元测试，覆盖默认配置、注册载荷、心跳载荷、状态回调请求头、心跳异常吞吐和空 `RPA_KEY` 异常路径
- `browser/反检测.py`
  - 调整 `随机延迟()` 的短延迟策略，避免 Windows 调度抖动导致既有测试偶发超出断言范围
- 兼容性说明：
  - `scripts/dispatch_test.py` 本轮未改动
  - Worker 仍只负责桥接、心跳和状态发送；未在本项目新增 Agent 端接收接口
- 本轮验证命令：
  - `python scripts/machine_worker.py --help`
  - `python -m py_compile scripts/machine_worker.py tests/单元测试/测试_机器接入脚本.py`
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_机器接入脚本.py -v`
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_反检测.py -v`
  - `python -m pytest -c tests/pytest.ini tests -v`

## 本轮新增任务（dispatch_test 请求补充 X-RPA-KEY）

- `scripts/dispatch_test.py`
  - 启动时通过 `python-dotenv` 加载 `.env`
  - 新增 `构建请求头()`，统一构造 `{"X-RPA-KEY": ...}` 请求头
  - 派发请求和轮询请求统一携带 `X-RPA-KEY`
  - 默认从 `.env` 的 `X_RPA_KEY` 读取，兼容回退 `RPA_KEY`
  - 密钥为空时显式抛出 `ValueError("X_RPA_KEY 不能为空")`
- `tests/单元测试/测试_任务派发脚本.py`
  - 新增脚本单元测试，覆盖环境变量读取、请求头构建、派发 header 注入和轮询 header 注入
- 兼容性说明：
  - `.env` 文件本身未修改
  - `scripts/machine_worker.py` 本轮未改动
- 本轮验证命令：
  - `python scripts/dispatch_test.py --help`
  - `python -m py_compile scripts/dispatch_test.py tests/单元测试/测试_任务派发脚本.py`
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_任务派发脚本.py -v`
  - `python -m pytest -c tests/pytest.ini tests -v`

## 本轮新增任务（统一 Agent 请求头使用 .env 的 X_RPA_KEY）

- `scripts/machine_worker.py`
  - 启动时通过 `python-dotenv` 加载 `.env`
  - 新增统一 `构建请求头()`，注册、心跳、状态回调全部携带 `X-RPA-KEY`
  - `--rpa-key` 默认值改为读取 `.env` 的 `X_RPA_KEY`
  - 缺少密钥时显式抛出 `ValueError("X_RPA_KEY 不能为空")`
- `scripts/dispatch_test.py`
  - 默认只从 `.env` 的 `X_RPA_KEY` 读取请求头密钥
  - 去掉对旧环境变量 `RPA_KEY` 的默认回退
- `tests/单元测试/测试_机器接入脚本.py`
  - 补充默认读取 `X_RPA_KEY` 的参数解析测试
  - 收紧注册、心跳请求断言，要求都带 `X-RPA-KEY`
  - 将空密钥异常断言更新为 `X_RPA_KEY 不能为空`
- `tests/单元测试/测试_任务派发脚本.py`
  - 补充“不回退 RPA_KEY”的断言
- `browser/反检测.py`
  - 调整极短延迟分支，确保全量回归再次稳定通过
- 兼容性说明：
  - `.env` 文件内容未改动，只统一改为从其中的 `X_RPA_KEY` 读取默认值
  - 两支脚本仍保留 `--rpa-key` 命令行覆盖能力
- 本轮验证命令：
  - `python scripts/machine_worker.py --help`
  - `python scripts/dispatch_test.py --help`
  - `python -m py_compile scripts/machine_worker.py scripts/dispatch_test.py tests/单元测试/测试_机器接入脚本.py tests/单元测试/测试_任务派发脚本.py`
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_机器接入脚本.py tests/单元测试/测试_任务派发脚本.py -v`
  - `python -m pytest -c tests/pytest.ini tests -v`

## 本轮新增任务（machine_worker 路由切换到 /api/workers）

- `scripts/machine_worker.py`
  - 注册路由改为 `POST /api/workers/register`
  - 心跳路由改为 `POST /api/workers/heartbeat`
  - 状态回调路由改为 `PUT /api/workers/{machine_id}/status`
  - 保持注册、心跳、状态回调全部携带 `X-RPA-KEY`
  - 更新脚本顶部说明文字与内置路由常量
- `tests/单元测试/测试_机器接入脚本.py`
  - 同步更新配置地址、注册请求、心跳请求、状态回调请求的 URL 断言
- 兼容性说明：
  - `scripts/dispatch_test.py` 本轮未改动
  - 请求头规则保持不变，仍从 `.env` 的 `X_RPA_KEY` 读取默认值
- 本轮验证命令：
  - `python scripts/machine_worker.py --help`
  - `python -m py_compile scripts/machine_worker.py tests/单元测试/测试_机器接入脚本.py`
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_机器接入脚本.py -v`
  - `python -m pytest -c tests/pytest.ini tests -v`

## 本轮新增任务（新增数据库模型：店铺 / 流程 / 定时任务）

- `backend/models/表结构.py`
  - 新增轻量级表结构声明，统一维护字段定义、建表 SQL 生成和中文属性映射
- `backend/models/店铺模型.py`
  - 新增 `shops` 数据模型，使用中文属性映射英文列名
  - 保留 `user_agent`、`profile_dir`、`smtp_*` 等现有扩展字段，兼容现有店铺服务
- `backend/models/流程模型.py`
  - 新增 `flows` 数据模型
  - 将 `steps` 统一序列化为 `[{task, on_fail}]` JSON 数组
  - 对 `on_fail` 做白名单校验，支持 `skip_shop / continue / log_and_skip / retry:N / abort`
- `backend/models/定时任务模型.py`
  - 新增 `execution_schedules` 数据模型
  - 将 `shop_ids` 序列化为 JSON 数组
  - 校验并发数、调度参数和必填字段
- `backend/models/数据库.py`
  - 改为从模型定义统一建表
  - 初始化时新增 `flows`、`execution_schedules`，并为 `flow_id` 开启外键约束
- `backend/models/__init__.py`
  - 导出新增模型与表定义
- `tests/单元测试/测试_数据库模型.py`
  - 覆盖中文属性到英文列名映射
  - 覆盖 `steps` / `shop_ids` 的正常与异常路径
  - 覆盖数据库初始化后的真实建表结果
- `docs/database.md`
  - 同步补充 `flows`、`execution_schedules` 和新的建表入口说明
- 本轮验证命令：
  - `python -c "from backend.models.店铺模型 import 店铺模型; from backend.models.流程模型 import 流程模型, 流程步骤; from backend.models.定时任务模型 import 定时任务模型; print('ok')"`
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_数据库模型.py -q`
  - `python -m pytest -c tests/pytest.ini -q`

## 本轮新增任务（新增任务注册表机制）

- `tasks/注册表.py`
  - 新增正式任务注册表实现
  - 提供 `register_task(名称, 描述)`、`获取所有任务()`、`获取任务类(名称)`、`获取任务(名称)` 和 `初始化任务注册表()`
  - 初始化时自动扫描 `tasks/` 目录并导入任务模块，后续新增 Task 文件无需修改注册机制
- `tasks/基础任务.py`
  - 新增抽象基类 `基础任务`
  - 统一定义抽象 `执行(self, 页面, 店铺配置) -> str`
  - 新增 `安全执行(...)`，统一包装 success / failed 返回结构
- `tasks/任务注册表.py`
  - 改为兼容导出层，复用 `tasks/注册表.py`，避免影响既有调用方
- `tasks/登录任务.py`
  - 改用 `@register_task("登录", "打开浏览器并登录店铺后台")`
  - 继承 `基础任务`
  - 保持现有登录核心逻辑不变
- `backend/api/可用任务.py`
  - 新增 `GET /api/tasks/available`
  - 从注册表读取任务列表，返回 `{code, msg, data: {tasks: [...]}}`
- `backend/api/路由注册.py`
  - 将可用任务路由注册到应用
  - 注册顺序放在动态 `/{task_id}` 路由前，避免 `/available` 被误匹配
- `tests/单元测试/测试_任务注册表.py`
  - 覆盖基础任务安全执行的成功与异常路径
  - 覆盖任务注册、查找、自动初始化
  - 覆盖 `GET /api/tasks/available` 的真实路由返回
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_任务注册表.py -q`
  - `python -c "from tasks.注册表 import 初始化任务注册表, 获取所有任务; 初始化任务注册表(); print(获取所有任务())"`
  - `python -m pytest -c tests/pytest.ini -q`

## 本轮新增任务（新增流程模板 + 店铺管理 CRUD 接口）

- `backend/services/店铺服务.py`
  - 保留原有店铺 CRUD、Cookie 管理和浏览器相关逻辑
  - 新增店铺名称非空校验
  - 将返回中的 `password`、`smtp_pass` 字段统一遮蔽为 `***`
- `backend/api/店铺接口.py`
  - 为 `GET /api/shops`、`POST /api/shops` 增加无尾斜杠别名
  - 保持现有接口兼容，同时满足验收命令直接访问 `/api/shops`
- `backend/models/数据结构.py`
  - 为店铺响应新增脱敏 `password` 字段
  - 新增 `流程创建请求`、`流程更新请求`、`流程响应`
- `backend/services/流程服务.py`
  - 新增流程模板 CRUD
  - 支持 `steps` 传数组或 JSON 字符串
  - 校验 `steps` 必须是 JSON 数组
  - 校验 `on_fail` 只允许 `skip_shop / continue / log_and_skip / retry:N / abort`
  - 校验每个 step 的 `task` 必须存在于任务注册表
- `backend/api/流程接口.py`
  - 新增 `GET /api/flows`
  - 新增 `POST /api/flows`
  - 新增 `PUT /api/flows/{id}`
  - 新增 `DELETE /api/flows/{id}`
- `backend/api/路由注册.py`
  - 注册流程路由
- `tests/单元测试/测试_店铺和流程接口.py`
  - 覆盖店铺创建、列表、更新、删除
  - 覆盖店铺空名称异常路径
  - 覆盖流程创建、列表、更新、删除
  - 覆盖未知任务和非法 JSON steps 异常路径
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_店铺和流程接口.py -q`
  - `python -m pytest -c tests/pytest.ini -q`
## 本轮新增任务（批量执行 API + Celery 投递）
- `backend/services/执行服务.py`
  - 新增批次创建、停止与 Redis 状态维护逻辑
  - 统一使用 `execute:batch:{batch_id}`、`execute:current`、`execute:status` 管理批次快照与事件
  - 批次投递改为“先 `freeze` 并写状态，再统一 `apply_async()`”，避免 Worker 抢先启动读不到批次元数据
- `backend/api/执行接口.py`
  - 新增 `POST /api/execute/batch`
  - 新增 `POST /api/execute/stop`
  - 新增 `GET /api/execute/status`，通过 SSE 推送批次状态
- `tasks/执行任务.py`
  - 新增批次子任务入口
  - 复用 `任务服务.统一执行任务(...)`
  - 按 `on_fail` 支持 `continue / log_and_skip / abort / retry:N`
- `backend/models/数据结构.py`
  - 新增 `批量执行请求`
  - 新增 `停止执行请求`
- `tasks/celery应用.py`
  - 增加 `tasks.执行任务` 导入，保持其它 Celery 配置不变
- `tasks/注册表.py`
  - 将 `执行任务` 加入排除列表，避免被业务任务自动发现机制扫描
- `backend/api/路由注册.py`
  - 注册执行接口路由
- `tests/单元测试/测试_执行服务.py`
  - 覆盖批次创建、参数校验、未注册任务、停止批次
- `tests/单元测试/测试_执行接口.py`
  - 覆盖批量执行接口统一响应与状态流 SSE 输出
- `tests/单元测试/测试_执行任务.py`
  - 覆盖 `continue / abort / retry:N` 分支
- `browser/反检测.py`
  - 调整极短延时分支，恢复全量回归稳定性
- 兼容性说明：
  - `concurrency` 当前仅记录在批次元数据中，实际并发仍由 Celery Worker 启动参数控制
  - 停止批次通过撤销已冻结的子任务 `task_id` 实现，已投递中的链路会按当前状态落到 `stopped`
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_执行服务.py -q`
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_执行接口.py -q`
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_执行任务.py -q`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - `68 passed`

## 本轮新增任务（定时任务 CRUD + Celery Beat）

- `backend/services/定时执行服务.py`
  - 新增定时计划 CRUD、参数校验、RedBeat 条目同步
  - 支持暂停/恢复时移除或重建 RedBeat 计划
  - 到点触发时复用 `执行服务实例.创建批次(...)`，不复制批量执行编排
  - 使用 `schedule:batch:{schedule_id}` 记录计划最近批次，支持 `skip / wait / parallel` 的基础重叠控制
- `backend/api/定时执行接口.py`
  - 新增 `GET /api/schedules`
  - 新增 `POST /api/schedules`
  - 新增 `PUT /api/schedules/{id}`
  - 新增 `DELETE /api/schedules/{id}`
  - 新增 `POST /api/schedules/{id}/pause`
  - 新增 `POST /api/schedules/{id}/resume`
- `tasks/定时任务.py`
  - 新增 `执行定时计划` Celery 任务入口
  - 复用 Worker 级事件循环执行异步计划触发逻辑
- `backend/models/数据结构.py`
  - 新增 `定时任务创建请求`
  - 新增 `定时任务更新请求`
  - 新增 `定时任务响应`
- `backend/models/__init__.py`
  - 导出新增的定时计划请求/响应模型
- `backend/api/路由注册.py`
  - 注册 schedules 路由
- `tasks/celery应用.py`
  - 追加 `beat_scheduler="redbeat.RedBeatScheduler"`
  - 追加 `redbeat_redis_url=配置实例.REDIS_URL`
  - 将 `tasks.定时任务` 加入 Celery 自动导入列表
- `tasks/注册表.py`
  - 将 `定时任务` 加入自动发现排除列表，避免基础设施任务被业务任务扫描
- `requirements.txt`
  - 添加 `celery-redbeat>=2.0.0`
  - 说明：任务文案写的是 `redbeat`，但 PyPI 实际可安装包名为 `celery-redbeat`，import 名仍为 `redbeat`
- `tests/单元测试/测试_定时执行服务.py`
  - 覆盖计划创建、RedBeat 同步、到点触发批量执行、Cron 运行时间回写和调度参数冲突异常
- `tests/单元测试/测试_定时执行接口.py`
  - 覆盖 schedules 的创建、列表、暂停、恢复、删除和流程不存在异常路径
- 本轮验证命令：
  - `python -m py_compile backend/services/定时执行服务.py backend/api/定时执行接口.py tasks/定时任务.py tests/单元测试/测试_定时执行服务.py tests/单元测试/测试_定时执行接口.py`
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_定时执行服务.py tests/单元测试/测试_定时执行接口.py -q`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - `74 passed`

## 本轮新增任务（新增前端 4 个页面）

- `frontend/src/api/types.ts`
  - 新增前端管理页通用类型，统一店铺、流程、批量执行、定时任务与可用任务的数据结构
- `frontend/src/api/shops.ts`
  - 新增店铺 CRUD 请求封装
- `frontend/src/api/flows.ts`
  - 新增流程模板 CRUD 请求封装
- `frontend/src/api/execute.ts`
  - 新增批量执行启动、停止与状态订阅请求封装
- `frontend/src/api/schedules.ts`
  - 新增定时任务 CRUD、暂停、恢复请求封装
- `frontend/src/api/tasks.ts`
  - 新增可用任务列表请求封装
- `frontend/src/views/ShopManage.vue`
  - 重写为通用店铺管理页
  - 移除平台耦合动作，只保留列表、新增/编辑、删除与状态展示
- `frontend/src/views/FlowManage.vue`
  - 新增流程模板页
  - 支持步骤增删、拖拽排序、复制、任务下拉与失败策略配置
- `frontend/src/views/BatchExecute.vue`
  - 新增批量执行页
  - 支持流程/单任务模式切换、店铺多选、并发数选择、SSE 进度展示
  - 修复默认选中流程/任务时的空值类型保护
- `frontend/src/views/ScheduleManage.vue`
  - 新增定时任务页
  - 支持固定间隔与 Cron 调度、店铺多选、暂停/恢复/删除
  - 编辑时显式清空未选中的 `interval_seconds` / `cron_expr`，避免后端保留旧值
- `frontend/src/router/index.ts`
  - 追加 `/flows`、`/execute`、`/schedules` 路由，不改现有路由
- `frontend/src/App.vue`
  - 追加流程模板、批量执行、定时任务导航入口
  - 侧边栏标题调整为 `自动化工作台`
- `tests/单元测试/测试_前端管理页.py`
  - 新增前端管理页烟雾测试，校验路由与页面关键结构
- 兼容性说明：
  - `ScheduleManage.vue` 的“执行中”状态当前基于 `enabled + last_run_at + interval_seconds` 做前端近似推断，因为后端暂未返回独立执行态字段
  - `ShopManage.vue` 沿用 `/shops` 现有入口，按任务要求直接升级为通用版，不新增重复路由
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_前端管理页.py -q`
  - `python -m pytest -c tests/pytest.ini -q`
  - `cd frontend && npx vue-tsc -b`
  - `cd frontend && npm run build`
- 本轮验证结果：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_前端管理页.py -q` 通过（`3 passed`）
  - `python -m pytest -c tests/pytest.ini -q` 通过（`77 passed`）
  - `cd frontend && npx vue-tsc -b` 通过
  - `cd frontend && npm run build` 在当前沙箱失败：`spawn EPERM`，失败点为 Vite/esbuild 启动子进程，不是新的前端类型错误

## 本轮新增任务（恢复店铺模型字段与店铺页布局）

- 历史对照：
  - 通过 `git show 210f396:backend/models/数据库.py`、`git show 210f396:backend/services/店铺服务.py`、`git show 210f396:frontend/src/views/ShopManage.vue` 对照改造前状态
  - 确认原有 `shops` 字段包括 `user_agent`、`profile_dir`、`cookie_path`、`last_login`、`smtp_host`、`smtp_port`、`smtp_user`、`smtp_pass`、`smtp_protocol`、`remark`
  - 当前后端模型与服务未丢失这些列，本轮重点恢复前端类型、交互封装与旧布局
- `frontend/src/api/types.ts`
  - 为 `Shop` 补回 `smtp_pass`
  - 为 `ShopPayload` 补回 `user_agent`、邮箱配置与 `remark`，兼容旧字段
- `frontend/src/api/shops.ts`
  - 新增 `openShopBrowser`
  - 新增 `checkShopStatus`
  - 新增 `testShopEmailConnection`
- `frontend/src/views/ShopManage.vue`
  - 恢复旧版店铺页结构
  - 重新使用小卡片网格 `shops-grid`
  - 移除顶部统计卡片区域
  - 恢复邮箱配置表单、测试连接按钮、打开/检查/编辑/删除交互
- `frontend/src/components/ShopCard.vue`
  - 放宽 `Shop` props 类型，兼容 `last_login`、`smtp_user` 等可空字段
- `tests/单元测试/测试_店铺恢复.py`
  - 覆盖 `/api/shops` 返回旧字段与脱敏字段
  - 覆盖店铺页恢复为旧卡片布局、邮箱字段和店铺动作封装
- `browser/反检测.py`
  - 微调极短随机延迟分支，避免 Windows 调度抖动导致全量测试偶发失败
- 兼容性说明：
  - 后端 `shops` 模型和 CRUD 结构无需新增列，本轮主要是恢复前端消费这些旧字段的能力
  - 店铺页保留新增的编辑弹窗、删除确认，但布局和样式恢复为旧版卡片式展示
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_店铺恢复.py -q`
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_前端管理页.py -q`
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_反检测.py -q`
  - `python -m pytest -c tests/pytest.ini -q`
  - `cd frontend && npx vue-tsc -b`
- 本轮验证结果：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_店铺恢复.py -q` 通过（`2 passed`）
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_前端管理页.py -q` 通过（`3 passed`）
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_反检测.py -q` 通过（`6 passed`）
  - `python -m pytest -c tests/pytest.ini -q` 通过（`79 passed`）
  - `cd frontend && npx vue-tsc -b` 通过

## 本轮新增任务（修复流程编排页面显示细节）

- `frontend/src/views/FlowManage.vue`
  - 失败策略下拉改为中文显示，保留英文原值
  - `retry:N` 选项的内部值改为 `retry:N`，保持与任务文档一致
  - 流程卡片中的失败策略预览同步改为中文文案
  - 任务下拉不再拼接 `name + description`
  - 任务描述拆到选择框下方的副文本提示，并保留 `title` 作为悬浮提示
- `frontend/src/views/ShopManage.vue`
  - 复核后确认编辑弹窗密码输入框已满足要求：`type="password"`、编辑时 placeholder 为 `留空则不修改`、编辑态不会回显后端脱敏值
  - 本轮无需继续修改该文件逻辑
- `tests/单元测试/测试_前端显示细节.py`
  - 新增显示细节回归测试
  - 覆盖失败策略中文显示、任务下拉不拼接描述、店铺密码输入框细节
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_前端显示细节.py tests/单元测试/测试_前端管理页.py -q`
  - `python -m pytest -c tests/pytest.ini -q`
  - `cd frontend && npx vue-tsc -b`
- 本轮验证结果：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_前端显示细节.py tests/单元测试/测试_前端管理页.py -q` 通过（`5 passed`）
  - `python -m pytest -c tests/pytest.ini -q` 通过（`81 passed`）
  - `cd frontend && npx vue-tsc -b` 通过
## 本轮新增任务（修复批量执行时的店铺名显示）

- `backend/services/执行服务.py`
  - 创建批次时先读取店铺详情，缓存真实店铺名
  - 批次 `shops` 快照新增 `shop_name`
  - 投递 Celery 子任务时追加 `shop_name`
- `tasks/执行任务.py`
  - 增加 `shop_name` 参数
  - Worker 开始执行日志改为优先打印真实店铺名
  - 将 `shop_name` 透传给任务记录参数和统一执行入口
  - 在兼容旧返回结构的前提下，仅在显式传入时回填 `shop_name`
- `backend/services/任务服务.py`
  - 统一执行日志优先显示 `shop_name`
  - 成功/失败返回结果补充 `shop_name`
- `frontend/src/api/types.ts`
  - `BatchShopState` 新增 `shop_name?: string | null`
- `frontend/src/views/BatchExecute.vue`
  - 新增 `getBatchShopName(shop)`
  - 等待中、执行中、已完成三列统一优先显示快照里的真实店铺名
- `tests/单元测试/测试_批量执行店铺名.py`
  - 覆盖真实店铺名透传、缺省名回退、Worker 日志、任务服务日志和前端静态展示
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_批量执行店铺名.py tests/单元测试/测试_执行服务.py tests/单元测试/测试_执行任务.py tests/单元测试/测试_任务服务.py -q`
  - `cd frontend && npx vue-tsc -b`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 针对性测试 `15 passed`
  - 全量测试 `87 passed`

## 本轮新增任务（生产环境审查修复）

- `backend/models/数据库.py`
  - 统一 SQLite 连接配置，启用 `WAL`、`busy_timeout`、`synchronous=NORMAL` 和 `row_factory`
  - 初始化阶段增加旧库迁移，自动补齐 `operation_logs.shop_name`
- `backend/services/任务服务.py`
  - 任务日志查询、写入、更新、清理统一改走 `获取连接()`
- `backend/services/日志服务.py`
  - 操作日志写入/查询/清理统一改走 `获取连接()`
  - 结合日志表迁移，修复 `shop_name` 字段写入与查询兼容性
- `backend/services/系统服务.py`
  - 健康检查中的数据库探活改走统一连接入口
- `backend/services/店铺服务.py`
  - 未配置 `ENCRYPTION_KEY` 时改为回退到 `data/.encryption.key` 持久化密钥文件，避免服务重启后历史密码失效
  - 删除店铺前先扫描关联定时计划：仅剩该店铺时删除计划，多店铺计划时移除该 `shop_id`
- `backend/services/执行服务.py`
  - 订阅批次状态时增加空闲心跳标记，避免长连接在无事件期被代理或浏览器回收
- `backend/api/执行接口.py`
  - 将执行状态保活标记转换为 SSE 注释帧 `: ping`
- `backend/api/日志接口.py`
  - 日志流在空闲期按固定间隔输出 SSE 注释帧 `: ping`
- `tasks/celery应用.py`
  - 增加 `task_reject_on_worker_lost=True`
  - 增加 `broker_connection_retry_on_startup=True`
- `tests/单元测试/测试_生产环境审查.py`
  - 新增 SQLite 配置/迁移、持久化密钥、店铺删除级联、SSE 保活和 Celery 生产配置回归测试
- 风险说明：
  - 启动阶段仍未对 Redis 可用性和数据库可写性做 fail-fast 检查，这一项保留为后续风险项，不在本轮改变启动语义
  - `shop_ids`、`steps` 仍缺少更严格的上限约束，本轮保留现有接口行为，只在审查结论中提示
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_生产环境审查.py -q`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 针对性测试 `6 passed`
  - 全量测试 `93 passed`

## 本轮新增任务（修复多线程并发执行时事件循环冲突）

- `tasks/celery应用.py`
  - Worker 事件循环改为按线程分配和复用，不再让 `threads` 池中的多个任务共用同一个 loop
  - 增加线程级事件循环注册表和初始化锁，保证 Worker 环境初始化只执行一次
- `tasks/执行任务.py`
  - 按任务要求引入 `nest_asyncio.apply()`
  - `_运行异步任务(...)` 遇到当前线程已有运行中事件循环时，回退到临时线程执行协程，避免 `This event loop is already running`
  - 保留当前 Worker 线程的专属事件循环复用逻辑，避免每次任务都新建 loop
- `tasks/桥接任务.py`
  - 同步增强 `_运行异步任务(...)`，和批量执行任务保持一致的线程安全行为
- `backend/services/浏览器服务.py`
  - 对主线程保留原有全局单例语义
  - 对 Worker 线程池改为线程隔离的浏览器管理器与初始化锁，确保不同线程任务不共享同一个 Playwright 实例
- `backend/services/任务服务.py`
  - 不再直接读取 `浏览器服务.管理器实例` 全局变量，改为获取当前线程绑定的管理器实例
- `requirements.txt`
  - 新增 `nest_asyncio`
- `tests/单元测试/测试_线程池事件循环.py`
  - 覆盖不同线程返回独立 Worker 事件循环
  - 覆盖当前线程已有运行中 loop 时的回退路径
  - 覆盖不同线程的浏览器服务使用独立管理器实例
- 环境说明：
  - 当前沙箱执行 `python -m pip install nest_asyncio` 时因用户目录写权限不足失败
  - 仓库已补 `requirements.txt`，并在 `tasks/执行任务.py` 增加缺包兜底，正式环境仍应通过 `pip install -r requirements.txt` 安装依赖
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_线程池事件循环.py -q`
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_Celery桥接.py tests/单元测试/测试_浏览器服务.py tests/单元测试/测试_执行任务.py tests/单元测试/测试_任务服务.py -q`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 针对性测试 `13 passed`
  - 全量测试 `97 passed`

## 本轮新增任务（新增批量执行完成后回调 Agent）

- `backend/services/执行服务.py`
  - 新增批次完成回调地址解析逻辑，优先使用请求体 `callback_url`，否则回退到 `AGENT_CALLBACK_URL`，默认根地址为 `http://localhost:8001`
  - 新增批次完成回调载荷构建逻辑，将内部批次快照转换为 `{batch_id, status, total, completed, failed, results}`
  - 在同步批次状态写入后检测终态，并通过 Redis `NX` 标记保证完成回调只发送一次
  - 回调失败时只打印日志，不影响批次状态落库和 Redis 推送
- `backend/models/数据结构.py`
  - 为 `批量执行请求` 新增可选字段 `callback_url`
- `backend/api/执行接口.py`
  - 透传 `callback_url` 给执行服务创建批次入口
- `tests/单元测试/测试_批量执行回调.py`
  - 覆盖回调载荷 `partial_failed` 场景
  - 覆盖自定义 `callback_url` 写入批次元数据
  - 覆盖回调异常不影响主流程
  - 覆盖 REST 接口透传 `callback_url`
- 兼容性说明：
  - 未改动现有桥接任务回调逻辑
  - 未改动 Agent 端代码，只在本项目增加向 Agent 回调的客户端逻辑
  - 内部批次 `status` 仍保持现有 `running/completed/failed/stopped` 语义，回调载荷额外转换为 `completed/partial_failed`
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_批量执行回调.py tests/单元测试/测试_执行服务.py tests/单元测试/测试_执行接口.py -q`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 针对性测试 `11 passed`
  - 全量测试 `101 passed`

## 本轮新增任务（修复 Worker 机器注册兼容 threads 池）

- `backend/配置.py`
  - 新增 `MACHINE_NAME`、`X_RPA_KEY` 配置项，供 Worker 注册逻辑统一读取
- `tasks/celery应用.py`
  - 保留 `worker_init` 作为启动钩子，不再依赖 `worker_ready`
  - 新增 Agent 根地址推导、注册请求头构建、业务响应校验和 Worker 机器注册逻辑
  - `初始化Worker环境()` 在任务注册表与回调地址配置后主动调用 `POST /api/machines`
  - 注册失败时仅打印日志，不阻塞 Worker 启动，也不会影响 `Worker环境已初始化` 置为 `True`
- `tests/单元测试/测试_Celery桥接.py`
  - 新增初始化即注册机器断言
  - 新增注册失败时静默吞吐断言
- `browser/反检测.py`
  - 将 30ms 内短延迟收敛到区间中值，消除 Windows 环境下 `测试_随机延迟在范围内` 的偶发抖动
- 环境说明：
  - 下一步任务 07 需要修改 `E:\Agent`，但当前沙箱可写根仅包含 `E:\douyin` 与 memories，暂时无法直接落盘到 `E:\Agent`
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_Celery桥接.py -q`
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_反检测.py -q`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 针对性测试 `12 passed`
  - 全量测试 `103 passed`

## 本轮新增任务（生产环境全面审查补漏）

- `browser/任务回调.py`
  - 自动回调统一补齐 `X-RPA-KEY` 请求头
  - 增加 Agent 统一业务响应校验，避免 200 + `code != 0` 被误判为成功
  - 未配置 `X_RPA_KEY` 时跳过回调，不再发送匿名请求
- `backend/services/心跳服务.py`
  - 心跳请求体改回 Agent 既有约定的 `{machine_id, shadowbot_running}`
  - 心跳请求统一携带 `X-RPA-KEY`
  - 为心跳请求增加 HTTP 状态和业务响应双重校验
- `backend/services/执行服务.py`
  - 批次完成回调默认不再直接复用完整 `AGENT_CALLBACK_URL`，而是提取 Agent 根地址后固定拼接 `/api/batch-callback`
  - 批次完成回调统一携带 `X-RPA-KEY`，并校验 Agent 统一业务响应
- `tests/单元测试/测试_Celery桥接.py`
  - 将 Worker 注册成功断言修正为 `POST /api/workers/register`
- `tests/单元测试/测试_批量执行回调.py`
  - 覆盖默认批次回调地址拼装、鉴权头注入和业务响应校验
- `tests/单元测试/测试_心跳服务.py`
  - 覆盖心跳请求体使用 `shadowbot_running` 和 `X-RPA-KEY`
- `tests/单元测试/测试_任务回调.py`
  - 新增任务自动回调的协议回归测试
  - 覆盖业务失败时静默吞吐，不影响任务主流程
- 协议说明：
  - Worker 注册接口保持 `POST /api/workers/register`
  - Worker 心跳接口保持 `POST /api/workers/heartbeat`
  - 机器状态回调保持 `PUT /api/workers/{machine_id}/status`
  - Worker → Agent 请求统一要求 `X-RPA-KEY`
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_Celery桥接.py tests/单元测试/测试_批量执行回调.py tests/单元测试/测试_心跳服务.py tests/单元测试/测试_任务回调.py -q`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 针对性测试 `16 passed`
  - 全量测试 `107 passed`

## 本轮新增任务（拼多多登录流程改造）

- `pages/登录页.py`
  - 登录地址切换为 `https://mms.pinduoduo.com/login/`
  - 首页地址切换为 `https://mms.pinduoduo.com/home`
  - 删除抖店邮箱登录、协议勾选和邮箱验证码检测方法
  - 新增 `切换账号登录()`、`填写手机号()`、`检测短信验证码()`
  - 登录成功与 Cookie 有效性统一改为按拼多多 `/home` URL 判定
- `tasks/登录任务.py`
  - 登录主流程改为 Cookie 优先 → 手机号密码登录 → 滑块验证码 → 短信验证码 → 保存 Cookie
  - 移除 `smtp_host` 邮箱验证码分支
  - 移除任务层旧 `.user-info` 选择器依赖，成功判断完全回到登录页 POM
  - `context destroyed` 场景改为按 `mms.pinduoduo.com/home` 兜底判定成功
- `tests/单元测试/测试_登录页.py`
  - 新增拼多多登录页 POM 单元测试
  - 覆盖账号登录切换、手机号/密码输入、登录按钮定位、短信验证码检测和首页 URL 判定
- `tests/单元测试/测试_登录任务.py`
  - 重写登录任务单元测试
  - 覆盖 Cookie 有效直登、手机号密码登录成功、滑块验证码、短信验证码和 `context destroyed` 分支
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_登录页.py tests/单元测试/测试_登录任务.py -q`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 针对性测试 `13 passed`
  - 全量测试 `117 passed`
- 注意事项：
  - 登录任务人工输入分支返回值已从 `需要邮箱验证码` 调整为 `需要短信验证码`
  - 全量测试仍有 6 条 Celery `datetime.utcnow()` 弃用警告，为现有存量问题，非本轮引入

## 本轮新增任务（短信验证码输入后继续判断登录结果）

- `tasks/登录任务.py`
  - 短信验证码检测后不再直接返回 `需要短信验证码`
  - 改为最长轮询 `120` 秒，每 `3` 秒检查一次当前 URL 是否已跳转到 `https://mms.pinduoduo.com/home`
  - 跳转成功后立即记录成功日志、保存 Cookie、补截图并返回 `成功`
  - 超时未跳转时记录失败日志、补截图并返回 `失败`
- `tests/单元测试/测试_登录任务.py`
  - 将原短信验证码等待人工输入用例改为“验证通过后成功登录”
  - 新增短信验证码等待超时返回失败用例
  - 通过 mock `asyncio.sleep` 避免测试真实等待 120 秒
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_登录任务.py tests/单元测试/测试_登录页.py -q`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 针对性测试 `14 passed`
  - 全量测试 `118 passed`
- 注意事项：
  - 当前短信验证码分支的最终返回值改为 `成功` 或 `失败`，不再直接返回 `需要短信验证码`
  - 全量测试仍有 6 条 Celery `datetime.utcnow()` 弃用警告，为现有存量问题，非本轮引入

## 本轮新增任务（task_params 管理 + 商品发布 POM）

- `backend/models/数据库.py`
  - 新增 `task_params` 建表 SQL
  - 字段覆盖 `shop_id`、`task_name`、`params`、`status`、`result`、`error`、`batch_id`、时间戳
  - `status` 增加 `CHECK` 约束，`shop_id` 通过外键关联 `shops`
- `backend/models/数据结构.py`
  - 新增任务参数创建/更新/响应模型
- `backend/services/任务参数服务.py`
  - 新增任务参数服务
  - 实现分页筛选、单条 CRUD、按条件清空、CSV 导入、待执行列表查询、执行结果回填
  - CSV 导入支持 `utf-8-sig`，按任务类型映射中文列名到 `params`
- `backend/api/任务参数接口.py`
  - 新增 `/api/task-params` 路由
  - 提供列表、创建、更新、删除、导入 CSV、按条件清空接口
- `backend/api/路由注册.py`
  - 注册任务参数新路由
- `frontend/src/api/index.ts`
  - 为统一请求封装补充 `FormData` 上传支持，并导出 `postForm`
- `frontend/src/api/taskParams.ts`
  - 新增任务参数前端 API wrapper
- `frontend/src/api/types.ts`
  - 新增任务参数列表、筛选、导入结果类型
- `frontend/src/views/TaskParamsManage.vue`
  - 新增“任务参数管理”页面
  - 支持任务类型/状态/店铺筛选、分页、删除、按条件清空、CSV 导入、模板下载
- `frontend/src/router/index.ts` / `frontend/src/App.vue`
  - 注册并展示“任务参数”路由与侧边栏入口
- `pages/商品列表页.py`
  - 新增商品列表页 POM
  - 封装导航、清弹窗、商品 ID 搜索、点击发布相似品并捕获新标签页
- `pages/发布商品页.py`
  - 新增发布商品页 POM
  - 封装初始化、清弹窗、标题修改、主图上传、随机调图、提交和成功判定
- 新增测试：
  - `tests/单元测试/测试_任务参数接口.py`
  - `tests/单元测试/测试_任务参数服务.py`
  - `tests/单元测试/测试_任务参数管理页.py`
  - `tests/单元测试/测试_商品列表页.py`
  - `tests/单元测试/测试_发布商品页.py`
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_任务参数接口.py tests/单元测试/测试_任务参数服务.py tests/单元测试/测试_任务参数管理页.py tests/单元测试/测试_商品列表页.py tests/单元测试/测试_发布商品页.py -q`
  - `python -m pytest -c tests/pytest.ini -q`
  - `cd frontend && npx vue-tsc -b`
- 本轮验证结果：
  - 针对性测试 `24 passed`
  - 全量测试 `142 passed`
  - 前端类型检查通过
- 注意事项：
  - 全量测试仍有 6 条 Celery `datetime.utcnow()` 弃用警告，为现有存量问题，非本轮引入
  - 工作区仍存在 `.pipeline/task.md`、`data/ecom.db` 等非本轮源码改动外的既有本地变更

## 本轮新增任务（发布相似商品 / 发布换图商品任务）

- `tasks/发布相似商品任务.py`
  - 新增“发布相似商品”任务，从 `task_param` 读取 `parent_product_id`、`new_title`、`task_param_id`
  - 复用 `商品列表页` 与 `发布商品页` 完成搜索、点击发布相似品、提交上架和结果判定
  - 接入 `@自动回调("发布相似商品")`、逐步 `上报(...)`、滑块验证码处理与 `task_params` 结果回填
  - 成功后统一写入 `self._执行结果 = {"new_product_id": ..., "parent_product_id": ...}`
- `tasks/发布换图商品任务.py`
  - 新增“发布换图商品”任务，在相似商品发布流程上补充 `image_path` 上传、`随机调整主图到第一位()` 和图片上传异常告警兜底
  - 缺少图片时跳过上传，上传出现 `FileNotFoundError` 或普通异常时仅上报警告并继续执行
  - 无 `CAPTCHA_API_KEY` 且检测到滑块验证码时返回 `需要验证码`，不主动关闭发布页，便于人工处理
- `tests/单元测试/测试_发布相似商品任务.py`
  - 覆盖任务注册、缺失 `parent_product_id` 抛 `ValueError`、正常成功链路、空标题跳过修改
  - 通过快照并恢复 `任务注册表` 避免 `importlib.reload(...)` 污染全局注册状态
- `tests/单元测试/测试_发布换图商品任务.py`
  - 覆盖任务注册、空图片/空标题跳过分支、正常换图发布成功链路
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_发布相似商品任务.py tests/单元测试/测试_发布换图商品任务.py`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 针对性测试 `7 passed`
  - 全量测试 `149 passed, 6 warnings`
- 注意事项：
  - 全量测试中的 6 条 Celery `datetime.utcnow()` 弃用警告为现有存量问题，非本轮引入
  - 工作区仍存在 `.pipeline/task.md`、`data/ecom.db` 等非本轮源码改动外的既有本地变更

## 本轮新增任务（店铺卡片显示 ID + CSV 导入兼容店铺名称）

- `backend/services/任务参数服务.py`
  - 新增按店铺名称查询店铺 ID 的逻辑
  - CSV 导入解析“店铺ID”列时，优先兼容纯数字 ID，其次兼容现有直接店铺 ID，最后按店铺名称匹配
  - 店铺名称不存在时按行跳过并记录 `店铺名称未找到` 错误，不中断整批导入
- `frontend/src/components/ShopCard.vue`
  - 在店铺卡片标题区新增 `ID: {shop.id}` 展示
  - 调整标题区布局，保证长 ID 可换行且不影响店铺名称显示
- `frontend/src/views/TaskParamsManage.vue`
  - 任务参数表格“店铺”列和店铺筛选下拉统一显示 `店铺名称（#ID）`
  - 导入结果文案改为“成功 / 跳过”
  - 补充“店铺ID列支持填写店铺ID或店铺名称”的模板说明，并将模板示例改为店铺名称示例
- `tests/单元测试/测试_任务参数服务.py`
  - 新增按店铺名称导入成功和店铺名称不存在跳过的服务层用例
- `tests/单元测试/测试_任务参数接口.py`
  - 更新 CSV 导入接口用例，覆盖店铺 ID、店铺名称和未知名称三种输入
- `tests/单元测试/测试_任务参数管理页.py`
  - 新增任务参数页店铺名称映射展示与导入说明文案的静态校验
- `tests/单元测试/测试_店铺卡片与任务参数显示.py`
  - 新增店铺卡片 ID 展示和任务参数页显示回归测试
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_任务参数服务.py tests/单元测试/测试_任务参数接口.py tests/单元测试/测试_任务参数管理页.py tests/单元测试/测试_店铺卡片与任务参数显示.py tests/单元测试/测试_店铺恢复.py`
  - `cd frontend && npx vue-tsc -b`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 针对性测试 `15 passed`
  - 前端类型检查通过
  - 全量测试 `153 passed, 6 warnings`
- 注意事项：
  - 全量测试中的 6 条 Celery `datetime.utcnow()` 弃用警告为现有存量问题，非本轮引入
  - 工作区仍存在 `.pipeline/task.md`、`data/ecom.db` 等非本轮源码改动外的既有本地变更

## 本轮新增任务（CSV 导入兼容 GBK 编码）

- `backend/services/任务参数服务.py`
  - 新增 `_解码CSV文本(...)`，按 `utf-8-sig`、`utf-8`、`gbk` 顺序尝试解码 CSV 原始字节
  - 所有候选编码均失败时，统一抛出 `CSV 文件编码不支持，请另存为 UTF-8 格式`
  - 保持 CSV 解析、表头检查和导入映射逻辑不变，仅扩展编码兼容范围
- `tests/单元测试/测试_任务参数服务.py`
  - 新增 UTF-8 无 BOM 导入成功用例
  - 新增 GBK 编码导入成功用例
  - 新增不支持编码时报明确错误的异常用例
  - 同时保留现有 `utf-8-sig` 导入成功用例，确认原路径未回归
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_任务参数服务.py`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 针对性测试 `8 passed`
  - 全量测试 `156 passed, 6 warnings`
- 注意事项：
  - 全量测试中的 6 条 Celery `datetime.utcnow()` 弃用警告为现有存量问题，非本轮引入
  - 工作区仍存在 `.pipeline/task.md`、`data/ecom.db` 等非本轮源码改动外的既有本地变更

## 本轮新增任务（CSV 导入自动修复科学计数法）

- `backend/services/任务参数服务.py`
  - 新增科学计数法正则与 `_修复科学计数法(...)`
  - 新增 `_预处理CSV行(...)`，只对列名含 `ID`、`id`、`Id` 的字段执行科学计数法修复
  - 在 `_解析CSV内容(...)` 中统一预处理每一行，保证 `父商品ID` 等字段在映射前已还原为完整数字字符串
- `tests/单元测试/测试_任务参数服务.py`
  - 新增科学计数法商品 ID 自动还原的导入用例
  - 新增普通数字 ID 不受影响的预处理用例
  - 复用现有测试确认编码兼容、名称匹配与异常路径未受影响
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_任务参数服务.py`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 针对性测试 `10 passed`
  - 全量测试 `158 passed, 6 warnings`
- 注意事项：
  - 全量测试中的 6 条 Celery `datetime.utcnow()` 弃用警告为现有存量问题，非本轮引入
  - 工作区仍存在 `.pipeline/task.md`、`data/ecom.db` 等非本轮源码改动外的既有本地变更

## 本轮新增任务（任务服务对接 task_params 表）

- `backend/services/任务服务.py`
  - 新增发布类任务的 `task_params` 对接逻辑，仅对“发布相似商品”“发布换图商品”生效
  - 通过 `任务参数服务实例.获取待执行列表(...)` 获取当前店铺与任务名下首条 `pending` 记录
  - 将记录中的 `params` 注入 `店铺配置["task_param"]`，并补入 `task_param_id`
  - 执行前将对应 `task_params` 记录回填为 `running`
  - 无待执行参数时调用 `上报("没有待执行的任务参数", shop_id)` 后返回 `跳过`
  - 任务返回 `成功` 时回填 `success + _执行结果`，返回失败结果或抛异常时回填 `failed + error`
- `tests/单元测试/测试_任务服务.py`
  - 新增发布相似商品自动注入首条待执行参数并回填成功的用例
  - 新增发布换图商品无待执行参数时跳过的用例
  - 新增发布任务返回失败时回填 `failed` 和错误信息的用例
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_任务服务.py`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 针对性测试 `4 passed`
  - 全量测试 `161 passed, 6 warnings`
- 注意事项：
  - `任务参数服务` 现有“店铺名称 → shop_id”映射仍返回 `shops` 表的实际 UUID，本轮未改动
  - 全量测试中的 6 条 Celery `datetime.utcnow()` 弃用警告为现有存量问题，非本轮引入
  - 工作区仍存在 `.pipeline/task.md`、`data/ecom.db` 等非本轮源码改动外的既有本地变更

## 本轮新增任务（task_params 启用/禁用/重置 + 发布次数展开）

- `backend/models/数据库.py`
  - 为 `task_params` 新增 `enabled`、`run_count` 字段
  - 在数据库初始化时通过 `ALTER TABLE` 自动补齐旧库，兼容已有数据
- `backend/models/数据结构.py`
  - 扩展任务参数创建/更新/响应模型，补充启用态、执行次数字段
  - 新增任务参数批量操作请求模型
- `backend/services/任务参数服务.py`
  - 创建/查询逻辑接入 `enabled/run_count`
  - 待执行列表只返回 `enabled=1` 的 `pending` 记录，并按 `id ASC` 取序
  - 新增单条启用、禁用、重置与批量启用、禁用、重置
  - 执行结果回填在 `success/failed` 时自动累加 `run_count`
  - CSV 导入新增“发布次数”解析与展开，并为展开记录写入 `batch_index`
- `backend/api/任务参数接口.py`
  - 新增 `/api/task-params/{id}/enable`
  - 新增 `/api/task-params/{id}/disable`
  - 新增 `/api/task-params/{id}/reset`
  - 新增 `/api/task-params/batch-reset`
  - 新增 `/api/task-params/batch-enable`
  - 新增 `/api/task-params/batch-disable`
- `frontend/src/api/types.ts` / `frontend/src/api/taskParams.ts`
  - 新增任务参数启用态、执行次数、批量请求类型和对应 API 封装
- `frontend/src/views/TaskParamsManage.vue`
  - 表格新增“启用”“已执行次数”列
  - 操作列新增单条“重置”按钮
  - 顶部新增“批量重置”“批量启用”“批量禁用”按钮
  - 禁用记录整行置灰，并补充“发布次数”模板说明
- 新增测试：
  - `tests/单元测试/测试_任务参数启用重置服务.py`
  - `tests/单元测试/测试_任务参数启用重置接口.py`
  - `tests/单元测试/测试_任务参数启用重置管理页.py`
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_任务参数启用重置服务.py tests/单元测试/测试_任务参数启用重置接口.py tests/单元测试/测试_任务参数启用重置管理页.py`
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_任务参数服务.py tests/单元测试/测试_任务参数接口.py tests/单元测试/测试_任务服务.py`
  - `python -m pytest -c tests/pytest.ini -q`
  - `cd frontend && npx vue-tsc -b`
- 本轮验证结果：
  - 针对性测试 `9 passed`
  - 相关回归测试 `17 passed`
  - 全量测试 `170 passed, 6 warnings`
  - 前端类型检查通过
- 注意事项：
  - 批量启用、批量禁用必须至少带一个筛选条件，后端会拒绝无条件全表操作
  - 批量重置未传 `status` 时默认处理 `success/failed` 记录，不会改动 `params`、`enabled` 和 `run_count`
  - 全量测试中的 6 条 Celery `datetime.utcnow()` 弃用警告为现有存量问题，非本轮引入
  - 工作区仍存在 `.pipeline/task.md`、`data/ecom.db` 等非本轮源码改动外的既有本地变更

## 本轮新增任务（修复商品列表页搜索方法 + 加诊断日志）

- `pages/商品列表页.py`
  - 在 `导航()` 的 `goto` 后和 `关闭所有弹窗()` 后新增当前 URL 打印
  - 为 `搜索商品()` 的四个关键步骤增加前后 `print("[商品列表页] ...")` 诊断日志
  - 搜索类型下拉主定位改为 `[data-testid='beast-core-select-selection']`、`.search-select-trigger` 和文本回退
  - 为选择“商品ID”、填写商品 ID、点击查询按钮分别补充独立异常捕获
  - 搜索失败时统一打印错误，并保存截图到 `data/screenshots/搜索失败_{时间戳}.png`
  - 关键点击操作显式传入 `timeout=10000`
  - 保持 `点击发布相似品()` 方法不变
- 本轮验证命令：
  - `python -m pytest tests/ -x`
- 本轮验证结果：
  - 全量测试 `170 passed, 6 warnings`
- 注意事项：
  - 为兼容现有回归测试，搜索下拉和“商品ID”选项保留了末位兼容回退路径，仅在新定位方式全部失败后使用
  - 全量测试中的 6 条 Celery `datetime.utcnow()` 弃用警告为现有存量问题，非本轮引入
  - 工作区仍存在 `.pipeline/task.md`、`.env`、`data/` 等非本轮任务外的既有本地变更

## 本轮新增任务（CSV 导入支持直接读取 xlsx）

- `backend/services/任务参数服务.py`
  - 新增 `_解析XLSX内容(...)`，使用 `openpyxl.load_workbook(..., read_only=True)` 读取 Excel 原始数据
  - 默认读取第一个 sheet，第一行作为表头，数据行转换为与 CSV 相同的 `List[Dict[str, str]]`
  - 对 `cell.data_type == 'n'` 且值大于 `9999999999` 的单元格强制转成 `str(int(cell.value))`
  - `批量导入(...)` 新增 `file_name` 参数，并按扩展名在 `.xlsx` 与原有 CSV 解析之间分流
- `backend/api/任务参数接口.py`
  - 导入接口读取上传文件名并透传到服务层
  - 接口扩展名校验增加 `.xlsx`
- `requirements.txt`
  - 新增 `openpyxl` 依赖
- 新增测试：
  - `tests/单元测试/测试_任务参数XLSX导入.py`
  - 覆盖 xlsx 导入保留大整数精度、非 `.xlsx` 扩展名继续按 CSV 解析、接口拒绝不支持扩展名
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_任务参数XLSX导入.py`
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_任务参数服务.py tests/单元测试/测试_任务参数接口.py`
  - `python -m pytest tests/ -x`
- 本轮验证结果：
  - 针对性测试 `3 passed, 10 warnings`
  - 相关回归测试 `13 passed`
  - 全量测试 `173 passed, 16 warnings`
- 注意事项：
  - 接口层现在会拒绝非 `.csv`、`.xlsx` 扩展名文件，返回统一业务错误
  - 新增的 10 条 `openpyxl` `datetime.utcnow()` 弃用警告来自第三方库，非本轮业务代码引入；原有 Celery 6 条警告仍存在
  - 工作区仍存在 `.pipeline/task.md`、`.env`、`data/` 等非本轮任务外的既有本地变更

## 本轮新增任务（发布页与商品列表页补日志和更保守的等待）

- `pages/商品列表页.py`
  - 在 `点击发布相似品()` 中拿到新页面后先打印 URL
  - 新增编辑页 URL 等待逻辑，匹配 `goods_edit`、`edit`、`add`、`goods_add`
  - 在等待 URL 前后、等待 `domcontentloaded` 前后补充诊断日志
- `pages/发布商品页.py`
  - `关闭所有弹窗()` 的最大尝试次数从 `8` 调整为 `3`
  - 去掉通用选择器 `[data-testid='beast-core-icon-close']`，降低误点风险
  - 保留素材弹窗关闭按钮、`.ant-modal-close`、`get_by_role("button", name="我知道了")` 和 `get_by_text("关闭")`
  - 每次循环打印匹配到的弹窗描述；未命中时打印“无弹窗，退出”
  - `初始化页面()` 在 load state 后打印 URL，在关闭弹窗前后打印日志，并输出页面标题
  - 为 `修改标题()`、`点击提交并上架()`、`是否发布成功()`、`关闭页面()` 增加方法开头和结尾日志
- 兼容性处理：
  - 为兼容测试替身，新页面 URL 等待与页面标题读取都增加了“仅在返回值可 await 时才 await”的兼容逻辑
- 本轮验证命令：
  - `python -m pytest tests/ -x`
- 本轮验证结果：
  - 全量测试 `173 passed, 16 warnings`
- 注意事项：
  - 本轮未改动 `tasks/` 目录，只调整 POM 层
  - 16 条 warning 中，10 条来自第三方 `openpyxl`，6 条来自现有 Celery `datetime.utcnow()` 弃用提示，均非本轮业务代码引入
  - 工作区仍存在 `.pipeline/task.md`、`.env`、`data/` 等本轮任务外的既有本地变更

## 本轮新增任务（选择器与 POM 动作分离重构）

- 新增 `selectors/` 模块：
  - `selectors/__init__.py`
    - 兼容 Python 标准库 `selectors` 模块，避免与本地包同名后影响 `asyncio` / `pytest`
  - `selectors/基础页选择器.py`
    - 抽出基础通用弹窗关闭按钮选择器列表
  - `selectors/登录页选择器.py`
    - 抽出账号登录文本、手机号/密码 placeholder、登录按钮 test id、验证码检测选择器
  - `selectors/商品列表页选择器.py`
    - 抽出弹窗关闭、搜索下拉、商品 ID 选项、输入框、查询按钮、发布相似品相关选择器
  - `selectors/发布商品页选择器.py`
    - 抽出弹窗关闭、标题输入框、提交按钮、发布成功特征、图片容器、图片上传与验证码相关选择器
- `pages/基础页.py`
  - 增加 `selectors` 包导入兼容引导
  - 引用 `基础页选择器` 保存通用弹窗关闭按钮列表
- `pages/登录页.py`
  - 将账号登录文本、placeholder、登录按钮 test id 和验证码检测改为引用 `登录页选择器`
- `pages/商品列表页.py`
  - 将弹窗关闭、搜索流程、发布相似品相关的硬编码定位全部改为引用 `商品列表页选择器`
  - 保持原有动作逻辑、日志和等待行为不变
- `pages/发布商品页.py`
  - 将弹窗关闭、标题输入、图片容器、上传、提交、成功检测和验证码定位改为引用 `发布商品页选择器`
  - `关闭所有弹窗()` 只依赖按钮列表和文本列表
- 新增测试：
  - `tests/单元测试/测试_选择器提取.py`
    - 覆盖标准库 `selectors` 已加载时页面模块仍可正常导入
    - 覆盖商品列表页搜索类型下拉按选择器列表回退
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_选择器提取.py tests/单元测试/测试_商品列表页.py tests/单元测试/测试_发布商品页.py tests/单元测试/测试_登录页.py tests/单元测试/测试_基础页.py`
  - PowerShell 临时设置 `timeBeginPeriod(1)` 并提高 `python` 进程优先级后执行 `python -m pytest tests/ -x`
- 本轮验证结果：
  - 针对性测试 `31 passed`
  - 全量测试 `175 passed, 16 warnings`
- 注意事项：
  - 任务单要求 `selectors/__init__.py` 为空文件，但由于该目录名与 Python 标准库 `selectors` 冲突，实际必须做兼容代理才能保证仓库可运行
  - 16 条 warning 中，10 条来自第三方 `openpyxl`，6 条来自现有 Celery `datetime.utcnow()` 弃用提示，均非本轮业务代码引入
  - 工作区仍存在 `.pipeline/task.md`、`.env`、`data/` 等本轮任务外的既有本地变更

## 本轮新增任务（选择器文件改为 dataclass 配置模式并补回老值）

- 新增 `selectors/选择器配置.py`
  - 提供 `选择器配置` dataclass，统一封装 `主选择器`、`备选选择器` 和 `所有选择器()`
- `selectors/基础页选择器.py`
  - 将通用弹窗关闭按钮改为 `选择器配置`
  - 新增 `首页URL`、`登录页URL` 常量
- `selectors/登录页选择器.py`
  - 改为 `选择器配置` 模式
  - 补回老版本账号输入框、密码输入框、登录按钮、账号登录、短信验证码输入框、发送验证码按钮、验证码提交按钮选择器
  - 保留滑块验证码检测配置
- `selectors/商品列表页选择器.py`
  - 改为 `选择器配置` 模式
  - 补回老版本商品 ID 搜索框、查询按钮、发布相似按钮、发布相似品弹窗确认按钮、商品列表容器、商品项选择器
  - 将弹窗关闭、搜索类型下拉、商品 ID 选项统一为 `选择器配置`
- `selectors/发布商品页选择器.py`
  - 改为 `选择器配置` 模式
  - 补回老版本商品标题输入框、提交并上架按钮、弹窗关闭文本、图片项、图片容器、图片更换/确认按钮、发布成功提示与成功 URL 特征
  - 保留滑块验证码和弹窗关闭按钮配置
- `pages/基础页.py`
  - 适配 `基础页选择器.通用弹窗关闭按钮.所有选择器()`
- `pages/登录页.py`
  - 登录页 URL 和首页 URL 改为引用 `基础页选择器`
  - 账号登录点击、账号/密码填写、登录按钮点击改为遍历 `选择器配置.所有选择器()`
  - 验证码检测改为基于配置对象
- `pages/商品列表页.py`
  - 搜索类型下拉、商品 ID 选项、商品 ID 搜索框、查询按钮、发布相似品按钮和确认按钮改为遍历配置对象
  - 弹窗关闭逻辑改为读取 `弹窗关闭按钮` / `弹窗关闭文本` 配置
  - 保持原有日志、失败截图和 URL 等待逻辑不变
- `pages/发布商品页.py`
  - 弹窗关闭、标题修改、图片列表获取、图片上传、提交并上架、成功检测和滑块验证码检测改为基于配置对象
  - 新增 `_获取图片列表()`，按图片项和图片容器配置优先级回退
- 测试更新：
  - `tests/单元测试/测试_选择器提取.py`
    - 新增 `选择器配置.所有选择器()` 顺序测试
    - 更新标准库 `selectors` 兼容测试和商品列表页配置回退测试
  - `tests/单元测试/测试_登录页.py`
    - 适配 `page.click`/配置对象，并补充账号登录主选择器失败后回退测试
  - `tests/单元测试/测试_商品列表页.py`
    - 适配配置对象驱动的搜索与发布相似品链路
  - `tests/单元测试/测试_发布商品页.py`
    - 适配 `locator(...)` 与配置对象驱动的标题、图片和成功检测链路
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_选择器提取.py tests/单元测试/测试_登录页.py tests/单元测试/测试_商品列表页.py tests/单元测试/测试_发布商品页.py tests/单元测试/测试_基础页.py`
  - PowerShell 临时设置 `timeBeginPeriod(1)` 并提高 `python` 进程优先级后执行 `python -m pytest tests/ -x`
- 本轮验证结果：
  - 针对性测试 `33 passed`
  - 全量测试 `177 passed, 16 warnings`
- 注意事项：
  - 本轮未修改 `browser/` 目录，验证码相关逻辑只调整了 `pages/` 层的选择器引用方式
  - `selectors/__init__.py` 的标准库代理逻辑保持不变
  - 16 条 warning 中，10 条来自第三方 `openpyxl`，6 条来自现有 Celery `datetime.utcnow()` 弃用提示，均非本轮业务代码引入
  - 工作区仍存在 `.pipeline/task.md`、`.env`、`data/` 等本轮任务外的既有本地变更

## 本轮新增任务（发布相似商品任务改为严格单次发布并回填中文结果）

- `selectors/商品列表页选择器.py`
  - 将被误写成任务说明文本的文件恢复为 `选择器配置` 结构
  - 恢复搜索下拉、商品 ID 选项、搜索框、查询、发布相似品、列表容器和商品项选择器
- `pages/发布商品页.py`
  - 新增 `获取商品标题()`，优先读取标题输入框的当前值，失败时返回空字符串
- `pages/商品列表页.py`
  - 新增 `切回前台()`，在编辑页关闭后尝试显式切回商品列表标签
  - 保持既有 `_等待搜索结果刷新()` 逻辑不变
- `tasks/发布相似商品任务.py`
  - 重写为严格单次发布流程：导航、搜索父商品、发布相似品、初始化发布页、提取 `goods_id`、随机调整主图、按需改标题、提交上架、检测成功、关闭并切回
  - 兼容读取中文键名 `父商品ID` / `新标题` 与当前服务注入的 `parent_product_id` / `new_title`
  - 成功时统一将 `_执行结果` 与 `task_params.result` 写为 `{"新商品ID", "父商品ID", "标题"}`
  - 删除当前任务单未要求的验证码处理分支，只保留失败回填与关闭页清理
- `tests/单元测试/测试_发布相似商品任务.py`
  - 更新成功断言为中文结果键名
  - 新增中文参数键名兼容测试
  - 新增主图调整调用断言
  - 新增标题为空时读取原标题测试
  - 新增失败状态和错误信息回填测试
- `tests/单元测试/测试_发布商品页.py`
  - 新增 `获取商品标题()` 用例
- `tests/单元测试/测试_商品列表页.py`
  - 新增 `_等待搜索结果刷新()` 对列表容器等待的断言
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_发布相似商品任务.py tests/单元测试/测试_发布商品页.py tests/单元测试/测试_商品列表页.py tests/单元测试/测试_选择器提取.py`
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_任务服务.py tests/单元测试/测试_发布换图商品任务.py`
  - PowerShell 临时设置 `timeBeginPeriod(1)` 并提高 `python` 进程优先级后执行 `python -m pytest -c tests/pytest.ini tests/ -x`
- 本轮验证结果：
  - 针对性测试 `26 passed`
  - 邻近回归测试 `7 passed`
  - 全量测试 `181 passed, 16 warnings`
- 注意事项：
  - 这轮先修复了工作区中被误改坏的 `selectors/商品列表页选择器.py`，否则 `pages/商品列表页.py` 无法导入
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示
  - 工作区仍存在 `.pipeline/task.md`、`.env`、`data/` 等本轮任务外的既有本地变更

## 本轮新增任务（Task 23b：商品列表页 Page 层拆分为原子方法）

- `selectors/商品列表页选择器.py`
  - 删除 `搜索类型下拉` 和 `商品ID选项` 两个多余选择器配置
  - 保留 `商品ID搜索框`、`查询按钮`、`发布相似按钮`、`发布相似品弹窗_确认按钮`、`商品列表容器`、`商品项`
- `pages/商品列表页.py`
  - 新增原子方法：`导航到商品列表()`、`输入商品ID()`、`点击查询()`、`等待搜索结果()`、`点击发布相似()`、`确认发布相似弹窗()`
  - 删除 `_点击搜索类型下拉()`、`_选择商品ID选项()` 和 `搜索商品()` 大方法
  - 保留 `切回前台()`、`关闭所有弹窗()` 和旧兼容方法，避免其它现有调用链立刻失效
- `pages/发布商品页.py`
  - 新增原子接口：`获取当前URL()`、`提取商品ID()`、`获取主图列表()`、`拖拽主图()`、`输入商品标题()`、`等待发布成功()`、`关闭当前标签页()`
  - 现有旧接口保留，用于兼容当前仓库其它任务
- `tasks/发布相似商品任务.py`
  - 改为按商品列表页原子方法顺序执行：导航、输入商品 ID、点击查询、等待结果、点击发布相似、确认弹窗
  - 使用 `页面.expect_popup()` 在 Task 层承接新标签页
  - 将主图随机索引选择逻辑放回 Task 层，通过 `获取主图列表()` 和 `拖拽主图()` 完成
  - 标题输入改为调用 `输入商品标题()`，成功等待改为 `等待发布成功()`，关闭页改为 `关闭当前标签页()`
- `tasks/发布换图商品任务.py`
  - 仅做必要兼容更新：同步改成新的商品列表页原子调用方式
  - 未改变其上传图片、验证码和结果回填逻辑
- 测试更新：
  - `tests/单元测试/测试_商品列表页.py`
    - 改为覆盖新的原子方法，不再围绕旧 `搜索商品()` 写断言
  - `tests/单元测试/测试_选择器提取.py`
    - 改为覆盖 `商品ID搜索框` 的 fallback 链路
  - `tests/单元测试/测试_发布商品页.py`
    - 新增发布页原子接口测试
  - `tests/单元测试/测试_发布相似商品任务.py`
    - 适配 popup 上下文和新的原子方法调用顺序
  - `tests/单元测试/测试_发布换图商品任务.py`
    - 同步适配新的商品列表页原子方法
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_商品列表页.py tests/单元测试/测试_选择器提取.py tests/单元测试/测试_发布商品页.py tests/单元测试/测试_发布相似商品任务.py tests/单元测试/测试_发布换图商品任务.py`
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_任务服务.py tests/单元测试/测试_任务注册表.py`
  - PowerShell 临时设置 `timeBeginPeriod(1)` 并提高 `python` 进程优先级后执行 `python -m pytest -c tests/pytest.ini tests/ -x`
- 本轮验证结果：
  - 针对性测试 `35 passed`
  - 相邻回归测试 `10 passed`
  - 全量测试 `187 passed, 16 warnings`
- 注意事项：
  - 这轮按任务单删除了商品列表页下拉相关逻辑，后续同类任务应直接复用新的原子方法，不再依赖旧 `搜索商品()` 封装
  - 额外更新 `tasks/发布换图商品任务.py` 是因为删除旧方法后会留下运行时断链，这是本轮唯一的范围外兼容修复
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示
  - 工作区仍存在 `.pipeline/task.md`、`.env`、`data/` 等本轮任务外的既有本地变更

## 本轮新增任务（Task 23c：浏览器最大化 + 主图拖拽 + 全局随机延迟）

- `browser/管理器.py`
  - 启动持久化浏览器上下文时将 `viewport` 改为 `None`
  - 在 `args` 中加入 `--start-maximized`，让本地 Chrome 直接最大化启动
- `pages/基础页.py`
  - 保留原有 `随机延迟()`，新增 `操作前延迟()`、`操作后延迟()`、`页面加载延迟()` 三个包装方法
- `pages/商品列表页.py`
  - `导航到商品列表()` 在 `goto` 后调用 `页面加载延迟()`
  - `输入商品ID()`、`点击查询()`、`点击发布相似()`、`确认发布相似弹窗()` 补齐前置/后置延迟
  - `等待搜索结果()` 在命中结果或回退路径后增加 `1.0~2.0` 秒延迟
- `pages/发布商品页.py`
  - `输入商品标题()` 在点击、清空和填写之间加入更细粒度的延迟
  - `拖拽主图()` 改为真实鼠标拖拽：基于元素坐标平滑移动 `8~15` 步，每步带微随机偏移和 `0.02~0.06` 秒停顿
  - `点击提交并上架()`、`等待发布成功()`、`关闭当前标签页()` 同步接入新的延迟包装方法
  - `随机调整主图到第一位()` 改为复用 `拖拽主图()`
- `tasks/发布相似商品任务.py`
  - 新增 `_步骤间延迟()`，在主要任务步骤之间随机等待 `1~3` 秒
  - 在修改标题前新增“调整主图顺序”步骤：从第 `2~5` 张主图中随机选一张拖到第 `1` 位；只有一张图时记录跳过
- 测试更新：
  - `tests/单元测试/测试_基础页.py`
    - 新增三个延迟包装方法测试
  - `tests/单元测试/测试_商品列表页.py`
    - 新增商品列表页原子方法延迟断言和搜索结果回退延迟测试
  - `tests/单元测试/测试_发布商品页.py`
    - 将主图拖拽断言改为鼠标移动轨迹
    - 新增拖拽越界、提交、等待成功、关闭标签页等延迟相关测试
  - `tests/单元测试/测试_发布相似商品任务.py`
    - 新增 `_步骤间延迟()` 替身断言、单图跳过拖拽断言和切回前台断言
  - `tests/单元测试/测试_浏览器管理器.py`
    - 新增浏览器最大化启动参数测试
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_基础页.py tests/单元测试/测试_商品列表页.py tests/单元测试/测试_发布商品页.py tests/单元测试/测试_发布相似商品任务.py tests/单元测试/测试_浏览器管理器.py`
  - PowerShell 临时设置 `timeBeginPeriod(1)` 并提高 `python` 进程优先级后执行 `python -m pytest -c tests/pytest.ini tests/ -x`
- 本轮验证结果：
  - 针对性测试 `44 passed`
  - 全量测试 `196 passed, 16 warnings`
- 注意事项：
  - 本轮未修改 `selectors/` 选择器值，也未引入任务单外的额外流程步骤
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示
  - 工作区仍存在 `.pipeline/task.md`、`.env`、`data/` 等本轮任务外的既有本地变更

## 本轮新增任务（Task 24：任务列表参数摘要 + 执行结果 Tab + 发布页兼容修复）

- `backend/models/数据结构.py`
  - 为任务参数批量操作请求新增 `batch_id` 字段，供批次级筛选与批量操作复用
- `backend/services/任务参数服务.py`
  - 扩展分页查询，支持 `batch_id`、`updated_from`、`updated_to`、`sort_by`、`sort_order`
  - 将 `status` 筛选扩展为支持逗号分隔多状态
  - 新增 `获取批次选项(...)`，返回批次号、记录数与最近更新时间
  - 批量清空、批量重置、批量启用、批量禁用全部接入 `batch_id`
- `backend/api/任务参数接口.py`
  - `GET /api/task-params` 开放新的批次、时间范围和排序参数
  - 新增 `GET /api/task-params/batch-options`
  - 批量操作接口同步支持 `batch_id`
- `frontend/src/api/types.ts`
  - 补充任务参数筛选类型、批量载荷类型和批次选项类型
- `frontend/src/api/taskParams.ts`
  - 查询参数构建接入新筛选字段
  - 新增 `listTaskParamBatchOptions(...)`
- `frontend/src/views/TaskParamsManage.vue`
  - 新增“任务列表 / 执行结果”双 Tab
  - 任务列表中的“参数”列改为关键字段摘要，hover 展示完整格式化 JSON
  - 原表新增“执行结果”列，提取 `新商品ID / 父商品ID` 摘要并提供 tooltip
  - 新增“执行结果”Tab，仅展示 `success/failed` 记录，默认按 `updated_at desc` 排序
  - 两个 Tab 均支持批次筛选，执行结果 Tab 额外支持日期范围筛选
- `tests/单元测试/测试_任务参数服务.py`
  - 新增批次筛选、时间范围、排序和批次选项聚合测试
- `tests/单元测试/测试_任务参数接口.py`
  - 新增接口层批次/时间/排序与 `/batch-options` 回归测试
- `tests/单元测试/测试_任务参数管理页.py`
  - 更新静态断言，覆盖新 Tab、批次筛选、执行结果列和说明文案
- `selectors/发布商品页选择器.py`
  - 按用户允许的修复补回 `弹窗关闭按钮`
  - 主选择器固定为 `.ant-modal-close`，未使用 `.Material`
- `pages/发布商品页.py`
  - `关闭所有弹窗()` 优先点击 `弹窗关闭按钮`，再回退到容器和文本按钮
  - 补回 `获取主图数量()`、`上传主图()`，恢复现有页面回归所依赖的兼容接口
  - `拖拽主图()` 索引越界恢复为抛出 `RuntimeError`
- `browser/反检测.py`
  - 极短延迟改为 `time.sleep(...)`，降低 Windows 下随机延迟测试抖动
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_反检测.py::测试_真人模拟器::test_随机延迟在范围内`
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_发布商品页.py`
  - `python -m pytest -c tests/pytest.ini tests/ -x`
  - `cd frontend && npx vue-tsc -b`
- 本轮验证结果：
  - `测试_反检测.py::test_随机延迟在范围内` 连续复跑 5 次全部通过
  - `测试_发布商品页.py` 结果 `18 passed`
  - 全量测试 `199 passed, 16 warnings`
  - 前端类型检查通过
- 注意事项：
  - 用户明确要求 `弹窗关闭按钮` 主选择器使用 `.ant-modal-close`，本轮按此执行
  - 全量测试仍需在 PowerShell 下临时启用 `timeBeginPeriod(1)` 并提高当前进程优先级，以减少 Windows 计时抖动
  - 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示
  - 工作区仍存在 `.pipeline/task.md`、`data/ecom.db`、浏览器缓存和截图等本轮任务外的既有本地变更

## 本轮新增任务（Task 24b：前端参数列 tooltip + 执行结果列补全）

- `frontend/src/views/TaskParamsManage.vue`
  - 为任务列表 Tab 的“参数”列改成自定义悬浮 tooltip，tooltip 内容使用 `<pre>` 展示完整格式化 JSON，最大宽度 `500px`
  - 将任务列表 Tab 的“执行结果”列摘要收敛为 `新ID: xxx`，缺少 `新商品ID / new_product_id` 时显示 `-`
  - 使用 `Teleport` 渲染 tooltip 浮层，替代原生 `title`，同时保留单元格摘要的省略显示
- `tests/单元测试/测试_任务参数管理页.py`
  - 补充 tooltip 结构、`500px` 限宽、`showJsonTooltip` 事件处理和“执行结果列不再输出父ID”的静态断言
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_任务参数管理页.py`
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_反检测.py::测试_真人模拟器::test_随机延迟在范围内`
  - `cd frontend && npx vue-tsc -b`
  - `cd frontend && npm run build`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - `测试_任务参数管理页.py` 结果 `3 passed`
  - `测试_反检测.py::test_随机延迟在范围内` 单独执行通过
  - 前端类型检查通过
  - `npm run build` 受当前环境 `spawn EPERM` 影响，未能完成 Vite 构建
  - 全量 pytest 结果 `198 passed, 1 failed`，失败项为既有抖动用例 `tests/单元测试/测试_反检测.py::测试_真人模拟器::test_随机延迟在范围内`
- 注意事项：
  - 本轮未改动后端接口与数据结构，仅补前端展示细节和对应静态回归
  - `spawn EPERM` 出现在 Vite 读取 `vite.config.ts` 阶段，属于当前环境子进程拉起限制，不是本页模板语法错误
  - 工作区仍存在 `.pipeline/task.md`、`data/ecom.db` 等本轮任务外的既有本地变更

## 本轮新增任务（Task 25：限时限量批量设置任务）

- `selectors/限时限量页选择器.py`
  - 新建限时限量页选择器文件，按 `选择器配置` dataclass 模式声明全部页面元素
  - 所有选择器先用 `TODO_待手动获取_*` 占位，便于后续人工 F12 回填
- `pages/限时限量页.py`
  - 新建限时限量页 POM，拆分为导航、展开设置、勾选自动创建、弹窗搜索/勾选/确认、填写折扣、确认设置、创建、等待成功等原子方法
  - 每个原子方法内部都按 `for sel in 选择器.所有选择器()` 遍历 fallback，并接入基础页的前后随机延迟
- `tasks/限时限量任务.py`
  - 新建 `@register_task("限时限量", "批量创建限时折扣活动")`
  - 从任务参数中读取 `batch_id` 和 `折扣`，查询同批次“发布相似商品”成功结果，提取去重后的新商品 ID
  - 按任务单步骤串联限时限量页原子方法，成功/失败/跳过都写入 `_执行结果` 并通过 `上报()` 记录中文日志
  - 增加成功/失败截图兜底，保留页面现场
- `backend/services/任务参数服务.py`
  - 新增 `查询批次成功记录(shop_id, batch_id, task_name)`，按 `status='success'` 读取并解析 `result` JSON
  - 为稳定现有批次接口测试，将批次选项排序收敛为 `记录数优先，其次最近更新时间`
- `backend/services/任务服务.py`
  - 将 `限时限量` 纳入依赖 `task_params` 的任务集合，确保统一执行入口能自动注入 `batch_id/折扣`
- `tests/单元测试/测试_限时限量页.py`
  - 覆盖固定 URL 导航、选择器 fallback、弹窗输入商品 ID、折扣填写以及创建成功/失败检测
- `tests/单元测试/测试_限时限量任务.py`
  - 覆盖任务注册、缺少 `batch_id` 异常、无成功商品跳过、成功编排和创建失败路径
- `tests/单元测试/测试_限时限量任务服务.py`
  - 覆盖统一执行入口对 `限时限量` 的 task_params 注入与无待执行参数时的跳过逻辑
- `tests/单元测试/测试_任务参数批次成功记录.py`
  - 覆盖批次成功记录查询仅返回成功结果、无记录返回空列表
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_限时限量页.py tests/单元测试/测试_限时限量任务.py tests/单元测试/测试_限时限量任务服务.py tests/单元测试/测试_任务参数批次成功记录.py`
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_任务注册表.py tests/单元测试/测试_任务服务.py`
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_任务参数接口.py::测试_任务参数接口::test_列表查询_支持批次筛选日期范围和批次选项`
  - PowerShell 临时设置 `timeBeginPeriod(1)` 并提高当前进程优先级后执行 `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 新增与邻近测试结果 `24 passed`
  - 任务参数接口回归通过
  - 全量测试 `213 passed, 16 warnings`
- 注意事项：
  - 选择器值均为 `TODO_待手动获取_*` 占位，后续需要用户在真实页面上手动回填
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示
  - 工作区仍存在 `.pipeline/task.md`、`data/ecom.db`、前一轮前端文件等本轮任务外的既有本地变更

## 本轮新增任务（Task 26：前端任务类型下拉动态化 + 限时限量导入支持）

- `frontend/src/views/TaskParamsManage.vue`
  - 复用现有 `listAvailableTasks()`，将“任务列表”Tab、“执行结果”Tab 顶部的“任务类型”筛选下拉，以及导入弹窗中的任务类型下拉统一改为动态读取后端任务注册表
  - 删除写死的 `['发布相似商品', '发布换图商品']` 数组，统一显示后端返回的 `task.name`
  - 新增任务类型加载失败、空任务列表和未选任务类型时的提示，避免导入弹窗在无数据时继续使用空值提交
  - 为 `限时限量` 补充专用 CSV 模板说明，下载模板时输出 `店铺ID,batch_id,折扣` 样例行
  - 保留原有发布类任务模板，并将“发布次数”说明收敛为仅在支持该列的任务下展示
- `tests/单元测试/测试_任务参数任务类型动态化.py`
  - 新增静态回归测试，覆盖动态任务列表接入、移除写死数组、三个下拉复用同一数据源
  - 覆盖异常路径提示与 `限时限量` 模板说明，满足本轮“正常路径 + 至少一个异常路径”的校验要求
- `backend/services/任务参数服务.py`
  - 为导入链路补上参数键名规范化，将 `父商品ID/新标题/图片路径/折扣/批次ID` 等中文列映射为 `parent_product_id/new_title/image_path/discount/batch_id`
  - 对 `_id` 类字段保持字符串，避免在 CSV/XLSX 导入时被自动转成整数，修复既有的任务参数导入回归
- `browser/反检测.py`
  - 将 `<= 30ms` 的极短随机延迟改为基于 `time.perf_counter()` 的高精度等待，并补 1ms 安全垫
  - 该调整只影响极短边界延迟，用于稳定 Windows 环境下的时间精度测试
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_任务参数管理页.py tests/单元测试/测试_任务参数任务类型动态化.py`
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_任务参数服务.py tests/单元测试/测试_任务参数接口.py tests/单元测试/测试_任务参数启用重置服务.py tests/单元测试/测试_任务参数XLSX导入.py`
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_反检测.py::测试_真人模拟器::test_随机延迟在范围内`
  - 在 `frontend/` 目录执行 `npx vue-tsc -b`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 任务参数页相关静态测试 `5 passed`
  - 导入映射相关回归 `24 passed, 10 warnings`
  - 随机延迟边界测试通过
  - 前端类型检查通过
  - 全量测试 `215 passed, 16 warnings`
- 注意事项：
  - 本轮未修改 `frontend/src/api/taskParams.ts`，因为仓库里已经存在可直接复用的 `frontend/src/api/tasks.ts -> /api/tasks/available`
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示
  - 工作区仍存在 `.pipeline/task.md`、`data/ecom.db` 等本轮任务外的既有本地变更

## 本轮新增任务（Task 27：浏览器最大化修复 + 浏览器复用修复 + 批次完成自动创建限时限量记录）

- `browser/管理器.py`
  - 在持久化上下文启动参数上显式加入 `no_viewport=True`，配合既有的 `viewport=None` 与 `--start-maximized` 修复 Chrome 窗口未真正最大化的问题
  - 为 `获取页面()` 增加关闭页检测；当缓存页已关闭时优先尝试复用 `context.pages` 中仍存活的页面，并同步刷新 `实例集` 中的 `页面/page` 缓存
  - 当所有页面都已关闭时抛出明确异常，交由上层异步创建新页面
- `backend/services/任务服务.py`
  - 新增页面有效性恢复逻辑：`统一执行任务()` 取页后若页面已关闭或管理器提示“所有页面已关闭”，则改为从现有 context 复用页面，必要时 `await new_page()`
  - 在 `执行任务()` 中，当 `发布相似商品` 成功且关联的 `task_params` 记录带有 `batch_id` 时，尝试调用 `批次完成后创建后续任务(...)`
  - 自动创建后续任务失败仅记日志，不影响当前发布任务结果
- `backend/services/任务参数服务.py`
  - 新增 `批次完成后创建后续任务(batch_id)`，用于在同批次发布相似商品全部结束后幂等创建一条 `限时限量` 记录
  - 创建前会检查：同批次不存在已创建的 `限时限量` 记录、没有 `pending/running` 的发布相似商品、至少存在一条成功记录，且成功记录 `params` 中带有非空折扣
  - 创建出的后续任务记录写入 `task_name='限时限量'`、`status='pending'`，并把 `batch_id` 同时写入表字段和 `params`
- `tasks/发布相似商品任务.py`
  - 强化 `_安全截图并关闭(...)`：关闭发布页后，如果商品列表主页面也已关闭，则自动新建页面、导航回商品列表，再执行 `切回前台()`
  - 该调整只作用于任务收尾兜底，不改原有发布主流程步骤
- `tests/单元测试/测试_浏览器复用修复.py`
  - 覆盖 `no_viewport=True`、关闭页自动刷新、全部页面关闭抛错
- `tests/单元测试/测试_任务服务浏览器复用与后续任务.py`
  - 覆盖 `统一执行任务()` 在页面已关闭时自动创建新页
  - 覆盖 `发布相似商品` 成功后触发后续限时限量创建，以及失败时不触发
- `tests/单元测试/测试_批次完成后自动创建限时限量.py`
  - 覆盖满足条件时创建、存在未完成记录时跳过、重复调用保持幂等
- `tests/单元测试/测试_发布相似商品任务收尾恢复.py`
  - 覆盖主页面也关闭时，任务收尾自动创建新商品列表页并回前台
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_浏览器管理器.py tests/单元测试/测试_浏览器复用修复.py tests/单元测试/测试_任务服务.py tests/单元测试/测试_任务服务浏览器复用与后续任务.py tests/单元测试/测试_批次完成后自动创建限时限量.py tests/单元测试/测试_发布相似商品任务.py tests/单元测试/测试_发布相似商品任务收尾恢复.py`
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_限时限量任务.py tests/单元测试/测试_限时限量任务服务.py tests/单元测试/测试_任务参数批次成功记录.py`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 针对性验证 `22 passed`
  - 邻近回归 `9 passed`
  - 全量测试 `225 passed, 16 warnings`
- 注意事项：
  - 本轮没有改 `selectors/` 目录，也没有改动 `pages/` 主流程方法，仅在 `发布相似商品任务` 收尾中补了主页面恢复兜底
  - 自动创建后续 `限时限量` 记录依赖成功的 `发布相似商品` 记录 `params` 中带有 `折扣 / discount`，缺失时会静默跳过
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示

## 本轮新增任务（Task 28：发布页加载等待 + 限时限量创建条件优化）

- `tasks/发布相似商品任务.py`
  - 在进入发布页并实例化 `发布商品页` 后，复用 `发布商品页选择器.商品标题输入框` 等待关键表单元素可见，最长 `30s`
  - 等待前增加 `上报("等待发布页表单渲染", 店铺ID)`，如果所有候选选择器都超时，则增加 `上报("发布页表单渲染超时，继续尝试", 店铺ID)`，随后继续关闭弹窗和后续流程
  - 未新增任何页面步骤，也未修改 `pages/` 层方法，只把等待逻辑放在 Task 编排层
- `backend/services/任务参数服务.py`
  - 调整 `批次完成后创建后续任务(batch_id)` 的触发条件：只要同批次存在至少一条 `success` 且不存在 `running`，就允许自动创建限时限量记录
  - `pending` 记录不再阻塞自动创建，适配“用户不会把整批全部跑完”的场景
  - 为所有 `return 0` 分支补充日志，明确区分“已存在限时限量记录”“无发布相似商品记录”“还有运行中记录”“无成功记录”“成功记录里无折扣值”
- `tests/单元测试/测试_发布相似商品任务发布页等待.py`
  - 新增发布页关键元素等待成功与超时继续执行两条回归测试
- `tests/单元测试/测试_批次完成后自动创建限时限量.py`
  - 将旧的“pending 阻塞”断言改为“pending 不阻塞”
  - 新增“running 仍阻塞”和“无折扣跳过”的异常路径测试
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_发布相似商品任务.py tests/单元测试/测试_发布相似商品任务发布页等待.py tests/单元测试/测试_发布相似商品任务收尾恢复.py`
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_批次完成后自动创建限时限量.py tests/单元测试/测试_任务服务浏览器复用与后续任务.py tests/单元测试/测试_限时限量任务服务.py`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 发布相似商品任务相关定向测试 `10 passed`
  - 批次后续任务与相邻回归 `10 passed`
  - 全量测试 `229 passed, 16 warnings`
- 注意事项：
  - 本轮未修改 `selectors/发布商品页选择器.py` 的选择器值，只在 Task 层复用了已有 `商品标题输入框`
  - 自动创建限时限量记录仍要求成功记录 `params` 中存在 `折扣 / discount`；缺失时只记日志，不创建记录
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示

## 本轮新增任务（Task 29：去掉发布页关闭弹窗 + 限时限量创建兜底）

- `tasks/发布相似商品任务.py`
  - 保留 Task 28 新增的“等待发布页表单渲染”逻辑
  - 删除进入发布页后调用 `关闭所有弹窗()` 的步骤，避免在发布相似品页面误删图片素材
- `backend/services/任务参数服务.py`
  - 调整 `批次完成后创建后续任务(batch_id)`：当成功记录里全部缺少 `折扣 / discount` 时，不再跳过，而是继续创建一条 `限时限量` 记录
  - 这条自动创建的记录中 `params` 会写入 `{"batch_id": 批次ID, "折扣": None}`
  - 新增日志：`[任务参数服务] 批次 {批次ID} 成功记录无折扣值，限时限量记录折扣为空，需手动补充`
- `tests/单元测试/测试_发布相似商品任务发布页等待.py`
  - 改为断言发布相似商品任务不再调用 `关闭所有弹窗()`
- `tests/单元测试/测试_批次完成后自动创建限时限量.py`
  - 将“缺少折扣时跳过”改为“缺少折扣时仍创建空折扣记录”
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_发布相似商品任务.py tests/单元测试/测试_发布相似商品任务发布页等待.py`
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_批次完成后自动创建限时限量.py tests/单元测试/测试_任务服务浏览器复用与后续任务.py tests/单元测试/测试_限时限量任务服务.py`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 发布相似商品任务相关定向测试 `9 passed`
  - 邻近回归 `10 passed`
  - 全量测试 `229 passed, 16 warnings`
- 注意事项：
  - 本轮未修改 `pages/` 与 `selectors/` 目录
  - 空折扣的 `限时限量` 记录后续执行时会在任务内因 `折扣不能为空` 失败，需用户在前端补值后重置重跑
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示

## 本轮新增任务（Task 30：任务链自动串联重构）

- `backend/services/任务服务.py`
  - 新增 `任务链映射`，将后续任务创建从硬编码的批次级逻辑改为可扩展的映射式链路
  - `执行任务()` 在成功回填后，若当前任务命中 `任务链映射`，则调用 `任务参数服务.创建后续任务(...)`
- `backend/services/任务参数服务.py`
  - 删除旧的 `批次完成后创建后续任务(...)`
  - 新增 `创建后续任务(源记录, 执行结果, 下一步任务名)`：
    - 继承源记录全部 `params`
    - 合并当前执行结果里的关键字段
    - 写入 `source_task_param_id`
    - 继承 `batch_id`
    - 通过 `source_task_param_id + task_name` 做幂等去重
- `tasks/限时限量任务.py`
  - 不再按 `batch_id` 查询同批次成功结果
  - 改为从自身 `task_param` 直接读取 `新商品ID / new_product_id` 与 `折扣 / discount`
  - 一条 `限时限量` 记录只处理一个商品，弹窗选品只搜索一次
- `tests/单元测试/测试_创建后续任务.py`
  - 新增后续任务创建测试，覆盖参数继承、执行结果合并、幂等与异常输入
- `tests/单元测试/测试_任务服务浏览器复用与后续任务.py`
  - 从断言旧的批次级方法改为断言通用 `创建后续任务(...)` 调用参数
- `tests/单元测试/测试_限时限量任务.py`
  - 更新为覆盖“缺少新商品ID跳过”“缺少折扣报错”“单商品成功创建”“失败路径”
- 删除 `tests/单元测试/测试_批次完成后自动创建限时限量.py`
  - 旧测试依赖已删除的批次级方法，本轮由 `测试_创建后续任务.py` 替代
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_任务服务.py tests/单元测试/测试_任务服务浏览器复用与后续任务.py tests/单元测试/测试_创建后续任务.py`
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_限时限量任务.py tests/单元测试/测试_限时限量任务服务.py tests/单元测试/测试_任务参数批次成功记录.py`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 任务链与后续任务创建相关测试 `10 passed`
  - 限时限量任务与相邻回归 `9 passed`
  - 全量测试 `227 passed, 16 warnings`
- 注意事项：
  - 本轮未修改 `pages/` 与 `selectors/` 目录
  - `查询批次成功记录(...)` 方法按要求保留，虽然 `限时限量任务.py` 已不再依赖它
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示

## 本轮新增任务（Task 31：流程级参数体系）

- `backend/models/数据库.py`
  - 新增 `flow_params` 表，支持 `params`、`step_results`、`current_step`、`batch_id`、`enabled`、`run_count` 等字段
- `backend/models/数据结构.py`
  - 新增流程参数创建/更新/批量操作/响应模型
- `backend/services/流程参数服务.py`
  - 新建流程参数服务，实现 CRUD、分页、CSV/XLSX 导入、待执行查询、步骤上下文合并、步骤结果回写、状态更新和批量操作
  - CSV/XLSX 导入复用 `任务参数服务` 现有列名映射、店铺解析、发布次数展开与数字修复逻辑
- `backend/api/流程参数接口.py`
  - 新建 `/api/flow-params` 接口，提供查询、创建、导入、详情、更新、删除和批量操作
- `backend/api/路由注册.py`
  - 注册流程参数路由
- `backend/services/任务服务.py`
  - 保留单任务 `task_params` 兼容逻辑
  - 当 `店铺配置` 中已有 `flow_context` 时，直接透传为 `task_param`，不再查询 `task_params`
  - 继续保留 `创建后续任务(...)` 兼容方法，但不再由任务链映射触发
- `backend/services/执行服务.py`
  - flow 模式创建批次时改为读取 `flow_params` 待执行记录
  - 对没有待执行 `flow_params` 的店铺静默跳过
  - 为每个 Celery step 传递 `flow_param_id`
- `tasks/执行任务.py`
  - 签名新增 `flow_param_id`
  - 执行前从 `flow_params` 读取 `flow_context` 并注入参数，更新 `flow_params` 为 `running`
  - 成功后回写 `step_results`，最后一步成功时将 `flow_params` 标为 `success`
  - 失败时按失败策略决定是否将 `flow_params` 标为 `failed`
- `tasks/限时限量任务.py`
  - 改为从自身 `task_param` 读取 `新商品ID / 折扣`
  - 不再按 `batch_id` 查询同批次成功记录
  - 一条 `限时限量` 记录只处理一个商品
- `frontend/src/api/types.ts` / `frontend/src/api/flowParams.ts`
  - 新增 FlowParam 类型与 flowParams API 封装
- `frontend/src/views/TaskParamsManage.vue`
  - 导入弹窗新增“绑定任务 / 绑定流程”切换
  - 绑定流程时读取已有流程下拉，并调用 `importFlowParams(...)`
  - 增加流程导入模板说明，保留现有任务导入模式
- `tests/单元测试/测试_流程参数服务.py`
  - 覆盖 `flow_params` 建表、导入、步骤结果回写与上下文合并
- `tests/单元测试/测试_流程参数接口.py`
  - 覆盖流程参数导入、CRUD 与异常路径
- `tests/单元测试/测试_创建后续任务.py`
  - 覆盖通用后续任务创建的参数继承、结果合并、幂等与异常路径
- `tests/单元测试/测试_任务服务.py`
  - 覆盖 `flow_context` 已注入时的透传逻辑
- `tests/单元测试/测试_任务服务浏览器复用与后续任务.py`
  - 改为断言通用 `创建后续任务(...)`
- `tests/单元测试/测试_执行服务.py` / `tests/单元测试/测试_执行任务.py`
  - 覆盖 flow 模式的 `flow_param_id` 透传和步骤结果回写
- `tests/单元测试/测试_限时限量任务.py`
  - 改为覆盖单商品执行模式
- `tests/单元测试/测试_流程参数导入静态页.py`
  - 覆盖前端 flow import API 与导入弹窗绑定流程入口
- 删除 `tests/单元测试/测试_批次完成后自动创建限时限量.py`
  - 旧测试绑定已删除的批次级后续任务方法，由 `测试_创建后续任务.py` 替代
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_流程参数服务.py tests/单元测试/测试_流程参数接口.py tests/单元测试/测试_创建后续任务.py tests/单元测试/测试_任务服务.py tests/单元测试/测试_任务服务浏览器复用与后续任务.py tests/单元测试/测试_执行服务.py tests/单元测试/测试_执行任务.py tests/单元测试/测试_限时限量任务.py tests/单元测试/测试_限时限量任务服务.py tests/单元测试/测试_任务参数批次成功记录.py tests/单元测试/测试_流程参数导入静态页.py`
  - `python -m pytest -c tests/pytest.ini -q`
  - 在 `frontend/` 目录执行 `npx vue-tsc -b`
- 本轮验证结果：
  - 针对性验证 `38 passed`
  - 全量测试 `238 passed, 16 warnings`
  - 前端类型检查通过
- 注意事项：
  - 本轮未修改 `pages/` 与 `selectors/` 目录
  - `task_params` 与 `flow_params` 并存，单任务执行仍走 `task_params`
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示

## 本轮新增任务（Task 32：任务参数管理页新增“流程参数”Tab）

- `frontend/src/api/flowParams.ts`
  - 补充单条操作方法：`resetFlowParam`、`enableFlowParam`、`disableFlowParam`
  - 由于后端当前没有单条 reset/enable/disable 专用接口，前端通过 `updateFlowParam(...)` 退化实现
- `frontend/src/views/TaskParamsManage.vue`
  - 在现有“任务列表 / 执行结果”旁新增第三个 Tab：`流程参数`
  - 新增 `flowParams` 列表状态、筛选状态、分页状态和加载方法
  - 新增流程参数筛选栏：流程下拉、状态下拉、店铺下拉、刷新按钮
  - 新增流程参数表格，列出店铺、流程名称、启用状态、共享参数、步骤进度、状态、步骤结果、错误信息、创建时间和操作
  - 共享参数与步骤结果均复用现有 JSON tooltip 逻辑
  - 流程名称通过 `flows.value.find(...)` 匹配显示，找不到时回退显示 `flow_id` 前 8 位
  - 步骤进度显示为 `current_step / 总步骤数`
  - 新增流程参数批量操作栏：批量重置、启用、禁用
  - flow 模式导入成功后会自动刷新流程参数列表，并切换到 `flowParams` Tab
  - header 区域的“清空 / 导入CSV”按钮在 `flowParams` Tab 也显示
- `tests/单元测试/测试_流程参数管理页静态.py`
  - 新增静态回归，覆盖 flowParams API 方法、第三个 Tab、流程参数筛选/列表/批量操作和导入后自动切换刷新
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_任务参数管理页.py tests/单元测试/测试_流程参数导入静态页.py tests/单元测试/测试_流程参数管理页静态.py`
  - 在 `frontend/` 目录执行 `npx vue-tsc -b`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 相关静态测试 `7 passed`
  - 前端类型检查通过
  - 全量测试 `240 passed, 16 warnings`
- 注意事项：
  - 本轮按要求未修改任何后端代码
  - `flowParams.ts` 的单条 reset/enable/disable 目前通过 `PUT /api/flow-params/{id}` 退化实现，后续如果后端补专用接口可再切换
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示

## 本轮新增任务（Task 33：Celery Worker 通过 HTTP 委托主进程执行任务）

- `backend/配置.py`
  - 新增 `API_BASE_URL` 配置项，默认指向主进程地址 `http://localhost:8000`
- `backend/api/任务接口.py`
  - 新增内部阻塞接口 `POST /api/tasks/execute-internal`
  - 接口会同步创建 `task_log`，调用主进程中的任务执行链路，并等待任务完成后返回结果
  - 对 `flow_param_id` 场景，内部接口会在主进程侧读取流程上下文，并负责回写 `step_results / status`
- `tasks/执行任务.py`
  - Worker 不再直接调用 `任务服务实例.统一执行任务()`
  - 改为通过 `httpx.Client(timeout=1800s)` 向主进程 `POST /api/tasks/execute-internal`
  - 保留 Worker 端对 `abort / continue / retry` 的链路控制，以及 Redis 批次状态更新
  - 补回 `_运行异步任务` 与 `_在线程中执行临时协程` 兼容 helper，仅供既有测试与其它潜在调用方使用，主流程已不再依赖
- `tests/单元测试/测试_任务接口内部执行.py`
  - 新增内部接口测试，覆盖成功路径与异常路径
- `tests/单元测试/测试_执行任务.py`
  - 改为覆盖 Worker HTTP 委托后的 `abort / continue / retry / flow_param` 分支
- `tests/单元测试/测试_批量执行店铺名.py`
  - 改为断言 Worker 通过 HTTP 请求仍正确携带 `shop_name`
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_执行任务.py tests/单元测试/测试_任务接口内部执行.py tests/单元测试/测试_批量执行店铺名.py`
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_执行服务.py tests/单元测试/测试_任务服务.py`
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_线程池事件循环.py`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - Worker/内部接口相关测试 `13 passed`
  - 邻近回归 `11 passed`
  - 事件循环兼容测试 `4 passed`
  - 全量测试 `242 passed, 16 warnings`
- 注意事项：
  - 本轮未修改前端代码
  - Worker 主流程已不再依赖主进程进程内的浏览器服务或 Playwright
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示

## 本轮新增任务（Task 34：新增“设置推广”任务）

- `selectors/推广页选择器.py`
  - 新建推广页选择器文件，使用 `选择器配置` dataclass 声明广告弹窗、添加商品、优先起量、查询、投产输入、二阶段输入、开启推广和成功提示等定位
  - 新增按商品ID动态生成的选择器方法：投产设置按钮、极速起量开关、确认/取消按钮、取消勾选框
- `pages/推广页.py`
  - 新建推广页 POM，包含导航、关闭广告弹窗、搜索商品、全选、设置投产比、开启推广、等待成功、返回商品列表页等原子方法
  - 每个原子方法只做一个页面操作，并在方法内加入 `0.5~1.5s` 的随机延迟
  - `关闭广告弹窗()` 按任务单要求支持“选择器点击 → JS 关闭 → ESC 关闭”的兜底
- `tasks/推广任务.py`
  - 新建 `@register_task("设置推广", "为商品设置全站推广（稳定成本）")`
  - 支持从 `任务参数` 中读取 `商品ID列表`（列表或逗号字符串）、`一阶段投产比`、`二阶段投产比`
  - 严格按任务单步骤编排：进入推广页、关闭弹窗、添加商品、关闭全局优先起量、搜索商品、全选、逐个设置投产比、开启推广、等待成功、返回商品列表页
  - `_执行结果` 回写 `推广商品数 / 成功列表 / 失败列表 / 一阶段投产比 / 二阶段投产比`
- `backend/services/任务服务.py`
  - 将 `"设置推广"` 加入 `任务参数任务集合`，使其可复用现有 `task_params` 注入与结果回填链路
- `tests/单元测试/测试_推广页.py`
  - 覆盖导航、选择器回退、商品ID提取和开启推广的 JS 兜底点击
- `tests/单元测试/测试_推广任务.py`
  - 覆盖任务注册、缺少商品ID异常、正常成功路径、投产受限跳过和添加推广商品失败路径
- `tests/单元测试/测试_推广任务服务.py`
  - 覆盖 `任务服务` 对 `"设置推广"` 的 task_params 注入与无待执行参数跳过
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_推广页.py tests/单元测试/测试_推广任务.py tests/单元测试/测试_推广任务服务.py`
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_任务服务.py tests/单元测试/测试_任务注册表.py`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 推广页/推广任务相关测试 `11 passed`
  - 邻近回归 `11 passed`
  - 全量测试 `253 passed, 16 warnings`
- 注意事项：
  - 本轮未修改前端代码
  - 本轮未改动现有任务逻辑，只新增 `设置推广` 及其接入
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示

## 本轮新增任务（Task 34：推广页拼多多改版重写）

- `selectors/推广页选择器.py`
  - 按新版页面结构重写选择器
  - 删除旧的 `全选复选框`、`二阶段投产输入框`、`二阶段确认按钮`
  - 新增 `预算日限额菜单项`、`日限额输入框`、`日限额确认按钮`
  - 动态选择器改为新版：`商品行容器`、`修改投产铅笔按钮`、`更多设置按钮`、`极速起量高级版开关`
- `pages/推广页.py`
  - 全量重写为新版原子方法接口
  - 删除旧的“点击全选 / 填写二阶段投产比 / 确认二阶段 / 检测投产比限制”等过时方法
  - 新增 `商品行是否存在()`、`点击更多设置()`、`点击预算日限额()`、`输入日限额()`、`确认日限额()`、`点击修改投产铅笔按钮()`、`等待投产弹窗()`、`获取极速起量高级版状态()`、`点击极速起量高级版开关()`、`输入投产比()`、`确认投产比设置()`
  - 广告弹窗关闭逻辑保留“点击选择器 / JS / ESC”兜底，但避免未命中弹窗时无条件发 ESC
- `tasks/推广任务.py`
  - 按新版 10 步流程重写任务编排
  - 去掉“全选模式”、`二阶段投产比` 和旧的投产限制分支
  - 新增可选 `日限额` 设置
  - 将极速起量逻辑切换为“极速起量高级版”，由 `关闭极速起量` 参数控制是否关闭
  - `商品ID列表` 读取顺序按要求调整为：`商品ID列表 -> 商品ID -> 新商品ID -> product_ids -> goods_ids`
  - `_执行结果` 改为回写 `推广商品数 / 成功列表 / 失败列表 / 投产比 / 日限额`
- `tests/单元测试/测试_推广页.py`
  - 更新为覆盖新版商品行存在检查、预算日限额回退、高级版开关状态和开启推广 JS 兜底
- `tests/单元测试/测试_推广任务.py`
  - 更新为覆盖新版正常路径、商品行不存在失败、无日限额与不关闭极速起量分支
- `tests/单元测试/测试_推广任务服务.py`
  - 更新注入参数断言，改为匹配新版 `投产比 / 日限额`
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_推广页.py tests/单元测试/测试_推广任务.py tests/单元测试/测试_推广任务服务.py`
  - `python -m pytest -c tests/pytest.ini -q tests/单元测试/测试_任务服务.py tests/单元测试/测试_任务注册表.py`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 推广页/推广任务相关测试 `12 passed`
  - 邻近回归 `11 passed`
  - 全量测试 `254 passed, 16 warnings`
- 注意事项：
  - 本轮未修改前端代码
  - 本轮未改动其他已有任务的逻辑
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示

## 本轮新增任务（ask 36：同步屏障 + 合并执行 + 限时限量逐商品折扣 + 前端执行结果展示）

- `backend/models/流程模型.py` / `backend/models/数据结构.py`
  - 为 flow steps 正式补齐 `barrier` / `merge` 字段，缺省值统一为 `false`
  - 后端流程返回结构现在会把这两个开关一起回传给前端
- `backend/services/执行服务.py`
  - 删除“每店铺一条 `celery chain`”的调度方式
  - 改成“首步单任务投递 + 批次状态先写入 + 后续步骤由完成回调继续投递”
  - flow 模式下，同店铺的全部待执行 `flow_params` 记录都会被纳入同一 `batch_id`
- `backend/services/流程参数服务.py`
  - `step_results` 改为结构化步骤结果，支持记录 `running / completed / failed / waiting_barrier / merged_skip`
  - 新增 `查询同批次步骤状态(...)`
  - 新增 `批量推进到下一步(record_ids)`
  - `获取步骤上下文(...)` 现在会过滤步骤元数据字段，避免把 `status` / `merged_context` 之类的控制字段污染到后续任务参数
- `backend/services/任务服务.py`
  - 新增 flow 步骤完成后的统一推进逻辑
  - 当前步骤完成后，会按 `barrier` 判断是否等待同批次记录
  - 屏障满足后，若下一步 `merge=true`，会生成 `商品ID列表 + 商品参数映射`，只投递主执行记录，其他记录写入 `merged_skip`
  - 合并执行后的结果会按商品逐条回写回原始 `flow_params.step_results`
- `backend/api/任务接口.py`
  - 内部执行接口不再直接手写 flow 回写逻辑
  - 改为在启动步骤时标记 `running`，完成后统一调用 `任务服务.处理流程步骤执行完成(...)`
- `selectors/限时限量页选择器.py` / `pages/限时限量页.py` / `tasks/限时限量任务.py`
  - 删除批量折扣输入和确认设置按钮逻辑
  - 新增按 `商品ID` 定位商品行折扣输入框
  - 限时限量任务改为支持逐商品查询、逐商品勾选、逐商品输入折扣
  - 同时兼容未来 merge 输入格式 `商品参数映射`
- `tasks/推广任务.py`
  - 增加 `商品参数映射` 读取逻辑
  - merge 场景下可按商品分别读取自己的 `投产比 / 日限额`，不再只能吃全局统一值
- `frontend/src/views/FlowManage.vue` / `frontend/src/api/types.ts`
  - 步骤编辑卡新增“同步屏障”“合并执行”开关
  - `merge` 依赖 `barrier`，关闭屏障时自动关掉 merge 并禁用
  - `buildPayload()` / `normalizeSteps()` 已兼容 `barrier` / `merge`
- `frontend/src/views/TaskParamsManage.vue`
  - 流程参数 Tab 的 `step_results` 改成逐步骤 tag 展示
  - 支持点击 tag 展开步骤详细 JSON，颜色区分完成 / 失败 / 等待 / 聚合跳过
- 测试与验证
  - 已更新执行服务、流程参数服务、内部执行接口、限时限量页、限时限量任务、推广任务、批量执行店铺名、批量执行回调、前端静态页、流程接口、数据库模型等测试
  - 已执行针对性回归：`40 passed`
  - 已执行邻近回归：`22 passed`
  - 已执行全量测试：`257 passed, 16 warnings`
  - 已执行前端类型检查：`npx vue-tsc -b`
- 注意事项：
  - 本轮会在本地生成 `__pycache__` 与测试数据库变更，这些不是交付代码
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示

## 本轮新增任务（Task 37：屏障模式单任务投递修复 — 同店铺合并为 1 个 Celery 任务）

- `backend/services/执行服务.py`
  - `投递单步任务(...)` 新增 `flow_param_ids` 和 `merge`
  - flow 模式创建批次时，如果首步 `barrier=true` 或 `merge=true`，同店铺只投递 1 个 Celery 任务
  - 首步非 barrier 时保持逐记录投递，兼容现有单条模式
- `tasks/执行任务.py`
  - Celery 任务签名扩展为兼容 `flow_param_ids: list[int]` 和 `merge: bool`
  - 当传入 `flow_param_ids` 时，会把多记录参数透传给主进程内部执行接口
  - 旧的 `flow_param_id` 单条模式保持兼容，并自动包装到 `params.flow_param_ids`
- `backend/services/任务服务.py`
  - `执行任务()` 入口新增多记录分支：检测 `flow_param_ids`
  - barrier-only 模式下，框架会在同一页面内循环调用具体任务，复用浏览器页面对象
  - merge 模式下，框架只调用任务一次，再复用已有合并回写逻辑回填所有记录
  - 每条记录之间增加 `2~5` 秒随机等待，避免连续操作过快
  - 某条记录失败时会按 `on_fail` 判断是否继续；`abort` 时剩余未执行记录会被标记为失败终止
  - 现有具体任务文件未改动，发布相似商品 / 限时限量 / 设置推广仍由框架层驱动
- 测试与验证
  - 已更新 `tests/单元测试/测试_执行服务.py`，验证 barrier 首步同店铺只投递 1 个任务
  - 已更新 `tests/单元测试/测试_执行任务.py`，验证 Worker 透传 `flow_param_ids / merge`
  - 已更新 `tests/单元测试/测试_任务服务.py`，覆盖 barrier-only 循环执行、merge 单次执行和 abort 终止路径
  - 已更新 `tests/单元测试/测试_批量执行店铺名.py`、`tests/单元测试/测试_批量执行回调.py`
  - 已执行针对性回归：`34 passed`
  - 已执行邻近回归：`26 passed`
  - 已执行全量测试：`261 passed, 16 warnings`
- 注意事项：
  - 本轮未修改前端代码
  - 本轮未修改具体任务实现，变更集中在框架层与 Worker 参数透传
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示

## 本轮新增任务（Task 38：推广页选择器修复 — 全局起量确认弹窗 + 极速起量确认弹窗 + 参数控制）

- `selectors/推广页选择器.py`
  - 新增 `全局起量关闭确认按钮`
  - 新增 `极速起量高级版关闭确认按钮`
  - 按任务单要求使用固定主选择器和备用选择器，未改动其他现有选择器
- `pages/推广页.py`
  - 新增 `确认关闭全局起量()`，逻辑为：等待确认按钮出现 → 点击按钮 → `1~2` 秒随机等待
  - 新增 `确认关闭极速起量()`，逻辑为：等待确认按钮出现 → 点击按钮 → `1~2` 秒随机等待
  - 两个确认方法的等待超时统一为 `5` 秒
  - 两个确认方法在超时或点击失败时都会截图并返回 `False`
- `tasks/推广任务.py`
  - 关闭全局起量改为完整链路：点击开关 → 点击“确定关闭”
  - `关闭极速起量` 参数读取顺序改为：`关闭极速起量` → `close_fast_boost`
  - 参数默认值保持为 `true`
  - 极速起量逻辑改为：
    - 当前开启且参数要求关闭：点击开关 → 点击确认
    - 当前关闭且参数要求开启：点击开关直接开启
    - 其余情况不操作
- 测试与验证
  - 已更新 `tests/单元测试/测试_推广页.py`，覆盖两个确认弹窗的成功/失败路径
  - 已更新 `tests/单元测试/测试_推广任务.py`，覆盖全局起量确认失败、`close_fast_boost=false` 开启极速起量等分支
  - 已执行针对性回归：`17 passed`
  - 已执行全量测试：`265 passed, 16 warnings`
- 注意事项：
  - 本轮未修改其他页面或任务逻辑
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示

## 本轮新增任务（Task 39：推广页补丁修复 — 极速起量双形态确认 + 顺序修正）

- `selectors/推广页选择器.py`
  - 保留原有通用极速起量确认按钮选择器作为形态1
  - 新增按商品ID生成的 `获取极速起量高级版关闭确认按钮(商品ID)` 作为形态2
  - 形态2优先匹配 `assist_close_{商品ID}`，并带备用 XPath
- `pages/推广页.py`
  - `确认关闭极速起量()` 改为 `确认关闭极速起量(商品ID)`
  - 实现双形态尝试：
    - 先尝试商品绑定按钮，等待 `2` 秒
    - 未命中后再回退通用确定按钮，等待 `2` 秒
  - 两种形态点击成功后都保持 `1~2` 秒随机等待
  - 两种形态都失败时会截图并返回 `False`
- `tasks/推广任务.py`
  - 关闭极速起量时，改为调用 `确认关闭极速起量(商品ID)`
  - 保持每个商品的操作顺序为：
    - 点击铅笔按钮
    - 关闭极速起量并确认
    - 填写投产比
    - 点击确定
- 测试与验证
  - 已更新 `tests/单元测试/测试_推广页.py`，覆盖：
    - 商品绑定按钮优先命中
    - 商品绑定失败后回退通用按钮
    - 双形态都失败时截图
  - 已更新 `tests/单元测试/测试_推广任务.py`，覆盖：
    - `确认关闭极速起量` 传入商品ID
    - 关闭极速起量发生在填写投产比之前
  - 已执行针对性回归：`20 passed`
  - 已执行全量测试：`268 passed, 16 warnings`
- 注意事项：
  - 本轮未修改全局起量确认弹窗逻辑
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示

## 本轮新增任务（Task 38.2：极速起量确认弹窗选择器精确匹配修复）

- `selectors/推广页选择器.py`
  - 删除极速起量关闭确认里误匹配投产比弹窗的通用“确定”选择器
  - 固定形态选择器改为只匹配“确定关闭”文本
  - 商品绑定形态 `assist_close_{商品ID}` 保持不变，继续作为优先精确匹配
- `pages/推广页.py`
  - `确认关闭极速起量(商品ID)` 继续优先尝试商品绑定按钮
  - 回退形态改为只尝试固定“确定关闭”按钮，不再尝试任何通用“确定”
  - 双形态都失败时截图名改为 `极速起量确认弹窗未找到`
- `tasks/推广任务.py`
  - 保持“先关极速起量，再填投产比”的顺序
  - 补齐 `确认投产比设置(商品ID)` 的商品ID参数，和任务单里的“点击确定(商品ID)”一致
- 测试与验证
  - 已更新 `tests/单元测试/测试_推广页.py`，新增断言确保极速起量固定选择器不再包含通用“确定”
  - 已更新 `tests/单元测试/测试_推广页.py`，将回退路径与截图断言同步到新的“确定关闭”规则
  - 已更新 `tests/单元测试/测试_推广任务.py`，补充投产确认按钮传入商品ID的断言
  - 已执行针对性回归：`21 passed`
  - 已执行全量测试：`269 passed, 16 warnings`
- 注意事项：
  - 本轮未修改全局起量确认弹窗逻辑
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示

## 本轮新增任务（Task 38.3：极速起量第三种形态 popover 气泡弹窗适配）

- `selectors/推广页选择器.py`
  - 新增 `极速起量高级版关闭确认按钮_Popover`
  - 主用选择器限定在 `anq-popover-footer` 容器内并匹配主按钮
  - 备用选择器继续限定在 `anq-popover-inner / anq-popover-footer` 容器内，避免误匹配其他弹窗
- `pages/推广页.py`
  - `确认关闭极速起量(商品ID)` 现在按三形态顺序尝试：
    - 形态2：`assist_close_{商品ID}`
    - 形态1：固定“确定关闭”按钮
    - 形态3：popover 气泡弹窗内的主按钮
  - 三种形态都保持 `2` 秒等待超时和点击后 `1~2` 秒随机等待
  - 三种形态都找不到时，仍截图为 `极速起量确认弹窗未找到`
- 测试与验证
  - 已更新 `tests/单元测试/测试_推广页.py`
  - 新增断言验证 popover 选择器必须包含 `anq-popover-footer`
  - 新增“前两种都失败后回退 popover 成功”的测试
  - 已执行针对性回归：`23 passed`
  - 已执行全量测试：`271 passed, 16 warnings`
- 注意事项：
  - 本轮未修改任务编排顺序
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示

## 本轮新增任务（Task 38.4：极速起量三形态确认弹窗优先级更新）

- `selectors/推广页选择器.py`
  - 商品绑定确认按钮改为 `contains(@data-testid, "assist_close") and contains(@data-testid, "{商品ID}")`
  - `popover` 形态主用选择器改为限定在 `anq-popover` 容器内的主按钮
  - 兜底形态保留为任意 `确定关闭` 按钮
- `pages/推广页.py`
  - `确认关闭极速起量(商品ID)` 的尝试顺序调整为：
    - 商品绑定按钮
    - `anq-popover` 容器内的主按钮
    - 任意 `确定关闭` 按钮
  - 每种形态仍使用 `2` 秒等待超时和 `1~2` 秒点击后延时
- 测试与验证
  - 已更新 `tests/单元测试/测试_推广页.py`
  - 新增断言验证商品绑定形态使用 `contains(@data-testid, "assist_close")`
  - 更新回退链路测试，确保优先级为：商品绑定 → popover → 确定关闭
  - 已执行针对性回归：`24 passed`
  - 已执行全量测试：`272 passed, 16 warnings`
- 注意事项：
  - 本轮未修改任务编排和其他任务文件
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示

## 本轮新增任务（Task 38.5：限时限量折扣输入框选择器修正 + 推广成功多条件检测）

- `selectors/限时限量页选择器.py`
  - `商品行折扣输入框(商品ID)` 的主用选择器增加 `@placeholder="1～9.7"` 精确匹配
  - 备用选择器保持按商品ID模糊匹配，兼容页面结构差异
- `selectors/推广页选择器.py`
  - 新增 `推广成功Toast提示`
  - 新增 `推广中状态文案`
  - 成功提示文案使用 `contains(text(), "...")` 兼容“成功”“已开启”等变体
- `pages/推广页.py`
  - `等待推广成功()` 改为 15 秒轮询的多条件判断
  - 轮询条件包含：
    - `anq-message` Toast 成功提示
    - URL 已回到 `promotion/list` 且不含 `create`
    - `beginPromotionButton` 不再可见或不存在
    - 页面出现“推广中”状态文案
  - 任一条件命中即返回 `True`
  - 超时会截图 `推广成功检测超时`
- 测试与验证
  - 已更新 `tests/单元测试/测试_限时限量页.py`，新增折扣输入框主选择器包含 `placeholder` 的断言
  - 已更新 `tests/单元测试/测试_推广页.py`，覆盖 Toast 成功、按钮消失成功和超时截图路径
  - 已执行针对性回归：`22 passed`
  - 已执行邻近回归：`11 passed`
  - 已执行全量测试：`276 passed, 16 warnings`
- 注意事项：
  - 本轮未修改任务编排逻辑
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示

## 本轮新增任务（Task 38.7：极速起量确认弹窗统一标题锚点匹配）

- `selectors/推广页选择器.py`
  - 删除原来分散的三个极速起量确认弹窗选择器定义
  - 统一收敛到 `获取极速起量高级版关闭确认按钮(商品ID)`
  - 主用选择器改为：先锚定 `anq-popover` 容器里包含“极速起量”的标题，再在容器内匹配主按钮或“确定关闭”按钮
  - 备用1 保留 `assist_close + 商品ID` 组合匹配
  - 备用2 保留全局 `确定关闭` 按钮作为最终兜底
- `pages/推广页.py`
  - `确认关闭极速起量(商品ID)` 从三段分支回退改为一个循环遍历统一选择器组
  - 每个候选选择器等待和点击超时统一为 `3000ms`
  - 成功后仍保持 `1~2` 秒随机等待，失败时仍截图 `极速起量确认弹窗未找到`
- 测试与验证
  - 已更新 `tests/单元测试/测试_推广页.py`
  - 新增断言验证主用选择器包含 `contains(text(), "极速起量")`
  - 新增断言验证三个候选选择器顺序为：标题锚点 → assist_close → 确定关闭
  - 已执行针对性回归：`26 passed`
  - 已执行全量测试：`275 passed, 16 warnings`
- 注意事项：
  - 本轮未修改推广任务编排逻辑
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示

## 本轮新增任务（Task 38.8：极速起量“确定”按钮选择器替换）

- `selectors/推广页选择器.py`
  - `获取极速起量高级版关闭确认按钮(商品ID)` 改为新的三候选顺序：
    - 主用：`assist_close + 商品ID`
    - 备用1：任意 `确定关闭` 按钮
    - 备用2：限定在 `anq-flex` 容器内的 `确定` 按钮
  - 这次不再使用上一轮的标题锚点主选择器
- `tests/单元测试/测试_推广页.py`
  - 更新主用选择器断言，改为校验 `assist_close + 商品ID`
  - 更新候选顺序断言，改为 `assist_close -> 确定关闭 -> anq-flex 内确定`
  - 更新回退测试，验证前两种失败后才进入 `anq-flex` 容器内的 `确定`
- 测试与验证
  - 已执行针对性回归：`26 passed`
  - 已执行全量测试：`275 passed, 16 warnings`
- 注意事项：
  - 本轮未修改 `pages/推广页.py` 和任务编排逻辑
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示

## 本轮新增任务（Task 39：前端停止任务真正生效）

- `backend/services/执行服务.py`
  - 新增批次取消标记 Redis 键前缀 `execute:cancel:{batch_id}`
  - 新增同步/异步版本的：
    - `设置取消标记`
    - `检查取消标记`
    - `清除取消标记`
  - 取消标记 TTL 统一为 `3600` 秒
  - `停止批次()` 现在会先写入取消标记，再执行现有的 `revoke(terminate=False)` 逻辑
- `backend/services/任务服务.py`
  - 新增异步取消检查辅助方法
  - 单任务模式：在任务真正执行前检查批次取消标记，命中后返回 `cancelled`
  - barrier 循环模式：每条记录执行前、以及当前记录完成后都会检查取消标记
  - 如果收到取消信号，会停止继续处理下一条记录，不再进入后续睡眠或下一步推进
  - flow 步骤完成后，如果批次已取消，则当前步骤结果会保留，但不会继续投递下一步
- `tasks/执行任务.py`
  - HTTP 委托返回后新增取消标记检查
  - 如果批次已取消，会把批次店铺状态更新为 `stopped`，并返回 `cancelled`
- 测试与验证
  - 已更新 `tests/单元测试/测试_执行服务.py`，验证停止批次会写取消标记
  - 已更新 `tests/单元测试/测试_执行任务.py`，验证 Worker 在 HTTP 返回后检测到取消会返回 `cancelled`
  - 已更新 `tests/单元测试/测试_任务服务.py`，覆盖：
    - 单任务执行前取消
    - barrier 循环中第一条完成后取消，不再继续下一条
    - `统一执行任务()` 执行前取消时写为 `cancelled`
  - 已执行针对性回归：`27 passed`
  - 已执行邻近回归：`24 passed`
  - 已执行全量测试：`279 passed, 16 warnings`
- 注意事项：
  - 本轮没有使用 `revoke(terminate=True)`，避免强杀进程导致浏览器残留
  - 当前页面操作允许完成，停止只在操作边界生效
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示

## 本轮新增任务（ask 40：前端系统设置增加机器码 + 流程执行结果显示修复）

- `backend/services/系统服务.py`
  - 在配置白名单新增 `agent_machine_id -> AGENT_MACHINE_ID`
  - `获取配置()` 返回中新增 `agent_machine_id`
- `frontend/src/views/Settings.vue`
  - `SystemConfig` 和默认配置新增 `agent_machine_id`
  - 加入“机器码”输入框
  - 加入“修改后需重启 Worker 生效”的提示文案
  - 保存成功提示同步提醒需要重启 Worker
- `frontend/src/api/mock.ts`
  - 补充 `agent_machine_id` mock 值，避免前端 mock 场景缺字段
- `backend/api/任务参数接口.py`
  - 新增 `/api/task-params/results`
  - 结果接口会合并 `task_params` 与 `flow_params`
  - flow 结果会从 `step_results` 提取业务字段，并映射成前端结果表当前使用的统一结构
  - 支持按店铺、任务类型、状态、批次、日期范围筛选，并支持分页
- `frontend/src/api/taskParams.ts`
  - 新增 `listTaskParamResults(...)`
- `frontend/src/views/TaskParamsManage.vue`
  - 执行结果 Tab 改为调用 `listTaskParamResults(...)`
  - 执行状态筛选补充 `running` 和 `cancelled`
  - 结果表行 key 改为组合键，避免 `task_params` 与 `flow_params` 混合结果时出现重复 key
- 测试与验证
  - 新增 `tests/单元测试/测试_系统设置机器码.py`
    - 覆盖设置页机器码静态内容
    - 覆盖系统服务读写 `agent_machine_id`
  - 新增 `tests/单元测试/测试_任务参数执行结果接口.py`
    - 覆盖结果接口合并返回 `task_params` 和 `flow_params`
    - 覆盖按任务类型筛选 flow 结果
  - 更新 `tests/单元测试/测试_任务参数管理页.py`
    - 覆盖结果 Tab 已改为依赖 `listTaskParamResults`
  - 已执行针对性回归：`11 passed`
  - 已执行前端类型检查：`npx vue-tsc -b`
  - 已执行全量测试：`285 passed, 16 warnings`
- 注意事项：
  - 本轮没有删除旧的 `task_params` 列表接口，任务列表和批量操作仍保持兼容
  - 执行结果接口当前优先解决“流程执行结果不显示”问题，批次下拉仍复用旧的 `task_params` 批次选项逻辑
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示

## 本轮新增任务（Task 41：前端菜单重构 — 10 合 5）

- `frontend/src/App.vue`
  - 侧边栏从 10 个菜单项精简为 5 个：
    - 店铺管理
    - 业务管理
    - 数据管理
    - 运行监控
    - 设置
- `frontend/src/router/index.ts`
  - 根路径 `/` 改为重定向 `/shops`
  - 新增 3 个主菜单路由：
    - `/business`
    - `/data`
    - `/monitor`
  - 保留隐藏路由：
    - `/browser`
    - `/dashboard`
  - 为兼容旧入口，新增重定向：
    - `/flows -> /business?tab=flow`
    - `/execute -> /business?tab=execute`
    - `/schedules -> /business?tab=schedule`
    - `/task-params -> /data?tab=params`
    - `/logs -> /monitor?tab=logs`
    - `/tasks -> /monitor?tab=monitor`
- 新建容器页
  - `frontend/src/views/BusinessManage.vue`
    - Tab：流程管理 / 批量执行 / 定时任务
  - `frontend/src/views/DataManage.vue`
    - Tab：CSV导入 / 执行结果、规则配置（占位）
  - `frontend/src/views/MonitorManage.vue`
    - Tab：运行日志 / 任务监控
  - 三个容器页都用 query 参数同步当前 Tab，兼容旧路径跳转后的默认落点
- 子页面适配
  - `frontend/src/views/FlowManage.vue`
  - `frontend/src/views/BatchExecute.vue`
  - `frontend/src/views/ScheduleManage.vue`
  - `frontend/src/views/TaskParamsManage.vue`
  - `frontend/src/views/LogViewer.vue`
  - `frontend/src/views/TaskMonitor.vue`
  - 以上页面全部增加 `showTitle?: boolean`，容器页嵌入时统一传 `false`
  - 仅隐藏页面自身标题区，不改原有业务交互和操作按钮
- 测试与验证
  - 已更新 `tests/单元测试/测试_前端管理页.py`
  - 已更新 `tests/单元测试/测试_任务参数管理页.py`
  - 已执行针对性回归：`10 passed`
  - 已执行前端类型检查：`npx vue-tsc -b`
  - 已执行全量测试：`285 passed, 16 warnings`
- 注意事项：
  - 本轮未删除任何现有 Vue 文件，只通过容器页复用
  - 旧地址仍可访问，但会被重定向到新的容器页对应 Tab
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示

## 本轮新增任务（Task 42：桌面基础页 + 微信 POM + 桌面选择器配置）

- `selectors/桌面选择器配置.py`
  - 新增桌面版选择器配置数据类
  - 以 UIAutomation 控件属性为核心，支持 `控件类型 / 名称 / 自动化ID / 类名 / 深度 / 备选`
- `pages/桌面基础页.py`
  - 新增桌面应用 POM 基类
  - 封装桌面控件查找、回退查找、点击、输入文本、读取文本、存在性判断与窗口截图
  - 所有方法保持同步，供未来 Task 层通过 `asyncio.to_thread()` 调用
  - 对 `uiautomation` 依赖缺失做了可测试的兜底导入处理，避免当前测试环境直接导入失败
- `selectors/微信选择器.py`
  - 新增微信桌面版主窗口、搜索框、聊天输入框、发送按钮和联系人项选择器
- `pages/微信页.py`
  - 新增微信桌面版 POM
  - 支持：
    - 激活微信窗口
    - 搜索联系人
    - 向指定联系人发送消息
    - 向当前聊天发送消息
  - 发送按钮失败时，会回退使用 `Enter` 键发送
- `requirements.txt`
  - 新增依赖 `uiautomation`
- 测试与验证
  - 新增 `tests/test_桌面基础页.py`
    - 覆盖桌面选择器配置备选链
    - 覆盖桌面基础页查找回退、输入文本、点击和截图
  - 新增 `tests/test_微信页.py`
    - 覆盖窗口激活失败、搜索联系人、发送消息回退 Enter、微信窗口初始化参数
  - 已执行针对性回归：`9 passed`
  - 已执行全量测试：首次全量回归命中过一条已知计时精度波动用例，单独复跑后再次执行全量，结果 `294 passed, 16 warnings`
- 注意事项：
  - 本轮未修改现有浏览器 `基础页.py`、`选择器配置.py`
  - `uiautomation` 为同步库，本轮仅提供同步 POM，Task 层后续接入时需使用 `asyncio.to_thread()`
  - 微信选择器基于 PC 版 3.9.x 命名，后续版本变更时可能需要重新校准
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示

## 本轮新增任务（Task 43：飞书服务 — Webhook 通知 + 文档回写）

- `backend/services/飞书服务.py`
  - 新建飞书服务类
  - 封装 Webhook 文本通知、卡片通知、售后通知
  - 封装 `tenant_access_token` 获取与缓存逻辑，过期前 5 分钟自动刷新
  - 封装单条/批量多维表格回写
  - 所有 HTTP 调用统一使用 `httpx.AsyncClient(timeout=10.0)`
  - 失败时不抛异常，统一返回 `{success: False, error: ...}`
- `backend/api/飞书接口.py`
  - 新增 `/api/feishu/test-webhook`
  - 供前端设置页测试 Webhook 连通性
- `backend/services/系统服务.py`
  - 配置白名单新增：
    - `feishu_webhook_url`
    - `feishu_app_id`
    - `feishu_app_secret`
    - `feishu_bitable_app_token`
    - `feishu_bitable_table_id`
- `backend/api/路由注册.py`
  - 将飞书接口注册到统一路由列表
- `backend/配置.py`
  - 补充飞书配置字段，确保运行时配置与 `.env` 写回一致
- 测试与验证
  - 新增 `tests/test_飞书服务.py`
  - 覆盖：
    - 文本通知请求体
    - 卡片通知 JSON
    - 售后通知卡片组装
    - `tenant_access_token` 缓存
    - 单条多维表格写入路径与 headers
    - `测试webhook()` True/False
    - 飞书测试接口统一响应
    - 系统服务对白名单飞书字段的写入
  - 已执行针对性回归：`9 passed`
  - 已执行全量测试：`303 passed, 16 warnings`
- 注意事项：
  - 本轮未改动任何现有业务 Task 代码
  - 飞书服务当前只提供 HTTP 能力，不接任何 UI 自动化
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示

## 本轮新增任务（Task 43.1：设置页新增飞书配置区块）

- `frontend/src/views/Settings.vue`
  - `SystemConfig` 增加：
    - `feishu_webhook_url`
    - `feishu_app_id`
    - `feishu_app_secret`
    - `feishu_bitable_app_token`
    - `feishu_bitable_table_id`
  - `config` 默认值增加以上 5 个字段
  - `loadConfig()` 增加以上 5 个字段映射
  - 新增 `testingFeishu` 与 `testFeishuWebhook()`
  - 在“验证码服务”和“系统监控”之间新增“飞书配置”区块
  - 新增 5 个飞书输入框和 1 个 Webhook 测试按钮
  - `App Secret` 使用 `type="password"`
- 测试与验证
  - 更新 `tests/单元测试/测试_系统设置机器码.py`
  - 新增对飞书配置区块、测试按钮和字段映射的静态断言
  - 已执行针对性回归：`5 passed`
  - 已执行前端类型检查：`npx vue-tsc -b`
  - 已执行全量测试：`304 passed, 16 warnings`
- 注意事项：
  - 本轮仅修改 `Settings.vue` 和对应静态测试
  - 统一保存按钮逻辑保持不变，飞书 5 个字段全部是可选项
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示

## 本轮新增任务（Task 46A：售后队列表 + 详情页完整抓取 + 列表翻页扫描 + JS 动态读取）

- `backend/models/售后队列模型.py`
  - 新建 `aftersale_queue` 表建表 SQL
  - 新增 `初始化售后队列表(...)`，同时创建批次内按 `batch_id + 订单号` 去重的唯一索引
- `backend/services/售后队列服务.py`
  - 新增售后队列服务，提供批次生成、单条/批量写入、详情更新、阶段更新、到期查询、拒绝次数查询和批次统计
  - 详情更新时会把完整详情 JSON 与可 SQL 查询的关键字段一并落库
- `backend/models/数据库.py`
  - 将 `售后队列建表SQL` 纳入统一初始化链路
  - 在数据库初始化时补充调用 `初始化售后队列表(...)`
- `selectors/售后页选择器.py`
  - 新增更精确的 `待商家处理Tab`
  - 新增详情页区域选择器，供详情标签页加载判定复用
- `pages/售后页.py`
  - `切换待处理()` 改为优先点击“待商家处理”
  - 新增 `扫描所有待处理()`，支持整表翻页扫描并汇总摘要
  - 新增详情标签页打开/关闭管理
  - 新增 `抓取详情页完整信息()`，通过单次 `evaluate(...)` 动态扫描 DOM 与全文文本提取详情
  - 新增动态按钮点击、按钮列表读取、待处理状态检查和详情页截图方法
- 测试与验证
  - 新建 `tests/test_售后队列服务.py`，覆盖批次、写入、去重、详情更新、阶段更新、到期查询、拒绝次数和批次统计
  - 更新 `tests/test_售后页.py`，覆盖翻页扫描、详情标签页切换、JS 抓取、动态按钮、待处理状态检查和详情截图
  - 已执行针对性回归：`python -m pytest -q tests/test_售后队列服务.py tests/test_售后页.py`
  - 针对性验证结果：`24 passed`
  - 已执行全量测试：`python -m pytest -c tests/pytest.ini -q`
  - 全量验证结果：`347 passed, 16 warnings`
- 注意事项：
  - 本轮按任务约束未改动 `tasks/售后任务.py` 与 `backend/api/`，仅完成数据抓取层
  - 全量测试中的 16 条 warning 仍为既有存量：10 条来自第三方 `openpyxl`，6 条来自 Celery `datetime.utcnow()` 弃用提示
