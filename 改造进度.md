# 改造进度

## 总体状态

- 已完成 6/6 个改造步骤
- 每一步完成后均执行了 `pytest tests/`
- 当前现有测试结果稳定为：`29 passed`

## 本轮补充对齐（根据 `建议.md`）

- 补齐 Worker 级事件循环复用，避免 `tasks/桥接任务.py` 对每个 Celery 任务重复 `asyncio.run(...)` 后关闭循环，影响同进程浏览器实例复用
- `tasks/celery应用.py`
  - 新增 `获取Worker事件循环()`
  - 在 `worker_init` 初始化时创建并绑定 Worker 级事件循环
  - 在 `worker_shutdown` 清理时关闭事件循环并重置状态
- `tasks/桥接任务.py`
  - `_运行异步任务(...)` 改为复用 Worker 级事件循环执行协程
  - 继续保留桥接函数内的 `初始化Worker环境()` 懒加载兜底
- `backend/services/任务服务.py`
  - `统一执行任务(...)` 改为直接调用 `backend/services/浏览器服务.py` 的 `确保已初始化()`
  - 保留 HTTP 与 Celery 共用同一执行入口的结构，不改动现有 HTTP 触发链路
- 新增回归测试：
  - `tests/单元测试/测试_Celery桥接.py`
  - `tests/单元测试/测试_启动入口.py`
- 本轮验证命令：`python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：`20 passed`

## 本轮新增任务（Redis 连接检测接口）

- `backend/api/系统接口.py`
  - 新增 `POST /api/system/test-redis`
  - 支持请求体传入 `redis_url`，为空时回退到 `配置实例.REDIS_URL`
  - 使用 `redis.asyncio` 执行 `ping()`，统计 `latency_ms`
  - 无论成功失败都在 `finally` 中关闭 Redis 客户端
- `backend/models/数据结构.py`
  - 新增 `Redis连接测试请求`
- `frontend/src/views/Settings.vue`
  - “测试连接”按钮接入真实接口
  - 增加按钮 loading 状态
  - 使用 toast 展示成功与失败提示
- `requirements.txt`
  - 明确 Redis 依赖版本为 `redis>=5.0.0`
- 新增回归测试：
  - `tests/单元测试/测试_系统接口.py`
- 本轮验证命令：`python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：`22 passed`

## 本轮新增任务（代码健壮性审查）

- `backend/services/任务服务.py`
  - 为 `统一执行任务(...)` 中的浏览器初始化、自动打开浏览器、任务执行增加超时与异常包装
  - 补充页面对象为空时的显式失败兜底
- `tasks/桥接任务.py`
  - `_运行异步任务(...)` 在 Worker 事件循环不可用时回退到 `asyncio.run(...)`
  - Worker 事件循环运行协程失败时转换为明确的运行时错误
- `tasks/celery应用.py`
  - `worker_init` / `worker_shutdown` 信号钩子改为最佳努力模式，异常仅记录不外抛
  - Worker 事件循环关闭失败时只记录不影响退出流程
- `backend/services/浏览器服务.py`
  - 新增与当前事件循环绑定的 `asyncio.Lock`
  - 为 `确保已初始化()` / `初始化浏览器()` 增加并发保护与初始化超时
- `backend/services/心跳服务.py`
  - 循环发送心跳时对上报异常、等待停止事件异常统一吞吐并记录
  - 停止心跳任务时的异常不再影响主服务关闭
- `backend/api/系统接口.py`
  - Redis 连接关闭动作增加 finally 内的超时与异常保护，避免覆盖主响应
- 新增回归测试：
  - `tests/单元测试/测试_浏览器服务.py`
  - `tests/单元测试/测试_心跳服务.py`
  - `tests/单元测试/测试_任务服务.py`
  - 扩展 `tests/单元测试/测试_Celery桥接.py`
  - 扩展 `tests/单元测试/测试_系统接口.py`
- 本轮验证命令：`python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：`29 passed`

## 第一步：修正 Celery 基础接线

- 修改 `tasks/celery应用.py`
- 修正 Celery 配置读取，统一改为使用 `配置实例.REDIS_URL`
- 为恢复现有测试通过，同时补了若干兼容性修复：
  - `tasks/登录任务.py`
  - `pages/登录页.py`
  - `pages/基础页.py`
  - `browser/反检测.py`

## 第二步：整理任务服务统一入口

- 修改 `backend/services/任务服务.py`
- 新增 `创建任务记录(...)`
- 新增 `统一执行任务(...)`
- 保留 HTTP 触发链路的原有行为：先返回 `pending`，再后台执行
- 统一请求字段为 `task_name`

关联修改：

- `backend/models/数据结构.py`
- `backend/api/任务接口.py`
- `frontend/src/views/TaskMonitor.vue`
- `frontend/src/api/mock.ts`

## 第三步：新增 Celery 桥接任务

- 新增 `tasks/桥接任务.py`
- 提供唯一桥接函数 `桥接执行任务(...)`
- 桥接函数支持：
  - 直接复用已有 `task_id`
  - 缺少 `task_id` 时自动创建任务记录
  - 使用 `task_name` 作为统一任务名称字段

## 第四步：补齐 Worker 初始化

- 修改 `tasks/celery应用.py`
- 使用 `worker_init` 适配 `solo` 模式 Worker
- Worker 启动时初始化：
  - 任务注册表
  - `AGENT_CALLBACK_URL` 回调地址
- 修改 `tasks/桥接任务.py`
- 增加桥接函数内部的懒加载兜底初始化

## 第五步：新增心跳服务

- 新增 `backend/services/心跳服务.py`
- 修改 `backend/配置.py`
- 修改 `.env`
- 新增配置项：
  - `AGENT_MACHINE_ID`
  - `AGENT_HEARTBEAT_URL`
- 心跳服务每 30 秒发送一次在线心跳，失败静默忽略

## 第六步：接入生命周期并更新文档

- 修改 `backend/启动入口.py`
- 在 FastAPI `lifespan` 启动阶段：
  - 初始化任务注册表
  - 设置回调地址
  - 启动心跳服务
- 在关闭阶段：
  - 停止心跳服务
  - 关闭数据库

文档更新：

- 修改 `CLAUDE.md`
- 新增 Celery Worker 桥接说明
- 新增心跳服务说明
- 新增 `.env` 配置项说明
- 新增 Worker 启动命令说明

## 当前 Worker 启动命令

```bash
celery -A tasks.celery应用 worker -Q machine_a -P solo
```

## 本轮新增任务（重写 AGENTS 文档）

- `AGENTS.md`
  - 按 `建议.md` 要求重写为完整仓库说明文档
  - 覆盖 13 个章节：项目概述、技术栈、结构、命名规范、API 规范、核心模块、数据模型、外部集成、环境变量、开发部署、测试、协作规范、注意事项与红线
  - 整合 `CLAUDE.md`、根 `AGENTS.md`、`.claude/*`、`.roles/*` 中的协作规则与红线
  - 基于实际代码列出目录结构、入口文件、API 路由、SQLite 表结构、环境变量、外部依赖与调用链路
  - 对缺失项明确标注“当前项目暂无此内容”，包括 Docker、`docker-compose.yml`、`pyproject.toml`、`.env.example`、认证鉴权、数据库迁移、PR 流程、根 README
  - 在文末补充生成日期 `2026-03-10` 与“基于代码自动生成，请人工审核后使用”提示

## 本轮新增任务（拆分 AGENTS 文档）

- `docs/architecture.md`
  - 收纳项目概述、整体架构关系、完整目录树、入口文件、分层设计、核心模块与关键调用链路
- `docs/api-spec.md`
  - 收纳统一响应、分页协议、错误处理、认证现状、路由清单与请求响应模型
- `docs/database.md`
  - 收纳 SQLite 表结构、字段说明、逻辑关联关系与 Pydantic 数据模型补充
- `docs/coding-style.md`
  - 收纳代码风格、命名边界、分层约束、测试命名与常见红线
- `docs/callback.md`
  - 收纳 Celery Worker、Redis、统一执行入口、自动回调、心跳与职责边界
- `docs/testing.md`
  - 收纳测试框架、目录、测试文件、覆盖范围、命名规则与当前测试现状
- `docs/frontend.md`
  - 收纳前端路由、主布局、页面职责、公共组件、数据层与页面接口映射
- `docs/deployment.md`
  - 收纳技术栈详细版本、环境变量、启动命令、运行依赖、外部集成配置与部署现状
- `AGENTS.md`
  - 精简为仓库级速览，保留技术栈概要、命名规范、架构核心原则、协作规范、禁止事项与 `docs/` 索引

## 本轮新增任务（清理历史任务文档）

- 删除以下历史/一次性文档：
  - `ISSUES.md`
  - `建议.md`
  - `Agent桥接与心跳功能改造建议.md`
- `docs/architecture.md`
  - 同步移除目录树中对上述已删除文档的引用，避免文档索引失真

## 本轮新增任务（外部机器接入测试脚本）

- `scripts/machine_worker.py`
  - 新增外部机器模拟脚本，启动时调用注册接口
  - 后台按 `idle/running` 状态定时调用心跳接口
  - 直接订阅 Celery 队列 `worker.{machine_id}`，收到任务后打印消息、模拟执行并回调结果
  - 注册、心跳、回调路径均支持命令行或环境变量覆盖
- `scripts/dispatch_test.py`
  - 新增派发测试脚本，支持按 `machine_id` 调用 `POST /api/task-dispatches/echo-test`
  - 支持附加 JSON 请求字段、打印服务端返回内容和提取任务 ID
  - 可选轮询任务状态，默认状态查询模板为 `/api/tasks/{task_id}`
- 兼容性说明：
  - 仓库当前没有 `/api/machines/*` 与 `/api/task-dispatches/*` 的后端源码
  - 脚本默认按 `.pipeline/task.md` 约定路由工作，并将相关路径全部做成可配置项，便于直接对接外部服务
- 本轮验证命令：
  - `python scripts/machine_worker.py --help`
  - `python scripts/dispatch_test.py --help`
  - `python -m py_compile scripts/dispatch_test.py scripts/machine_worker.py`

## 本轮新增任务（机器接入脚本对齐 Agent 既有 API）

- `scripts/machine_worker.py`
  - 默认连接地址改为 Agent 端口 `http://localhost:8001`
  - 注册接口改为 `POST /api/machines`，上报 `{machine_id, machine_name}`
  - 心跳接口改为 `POST /api/machine/heartbeat`，上报 `{machine_id, shadowbot_running}`
  - 状态回调改为 `PUT /api/machines/{machine_id}/status`，通过 `X-RPA-KEY` 鉴权并上报 `{status}`
  - 删除可配置的注册/心跳/回调路径，统一从 `--server-url` 推导 Agent 路由
  - 新增 `--machine-name` 参数，默认回退为 `machine-id`
  - 启动即注册并回调 `idle`，任务执行期间更新 `running`，失败更新 `error`，退出更新 `offline`
- `tests/单元测试/测试_机器接入脚本.py`
  - 新增脚本单元测试，覆盖默认配置、注册载荷、心跳载荷、状态回调请求头、心跳异常吞吐和空 `RPA_KEY` 异常路径
- `browser/反检测.py`
  - 调整 `随机延迟()` 的短延迟策略，避免 Windows 调度抖动导致既有测试偶发超出断言范围
- 兼容性说明：
  - `scripts/dispatch_test.py` 本轮未改动
  - Worker 仍只负责桥接、心跳和状态发送；未在本项目新增 Agent 端接收接口
- 本轮验证命令：
  - `python scripts/machine_worker.py --help`
  - `python -m py_compile scripts/machine_worker.py tests/单元测试/测试_机器接入脚本.py`
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_机器接入脚本.py -v`
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_反检测.py -v`
  - `python -m pytest -c tests/pytest.ini tests -v`

## 本轮新增任务（dispatch_test 请求补充 X-RPA-KEY）

- `scripts/dispatch_test.py`
  - 启动时通过 `python-dotenv` 加载 `.env`
  - 新增 `构建请求头()`，统一构造 `{"X-RPA-KEY": ...}` 请求头
  - 派发请求和轮询请求统一携带 `X-RPA-KEY`
  - 默认从 `.env` 的 `X_RPA_KEY` 读取，兼容回退 `RPA_KEY`
  - 密钥为空时显式抛出 `ValueError("X_RPA_KEY 不能为空")`
- `tests/单元测试/测试_任务派发脚本.py`
  - 新增脚本单元测试，覆盖环境变量读取、请求头构建、派发 header 注入和轮询 header 注入
- 兼容性说明：
  - `.env` 文件本身未修改
  - `scripts/machine_worker.py` 本轮未改动
- 本轮验证命令：
  - `python scripts/dispatch_test.py --help`
  - `python -m py_compile scripts/dispatch_test.py tests/单元测试/测试_任务派发脚本.py`
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_任务派发脚本.py -v`
  - `python -m pytest -c tests/pytest.ini tests -v`

## 本轮新增任务（统一 Agent 请求头使用 .env 的 X_RPA_KEY）

- `scripts/machine_worker.py`
  - 启动时通过 `python-dotenv` 加载 `.env`
  - 新增统一 `构建请求头()`，注册、心跳、状态回调全部携带 `X-RPA-KEY`
  - `--rpa-key` 默认值改为读取 `.env` 的 `X_RPA_KEY`
  - 缺少密钥时显式抛出 `ValueError("X_RPA_KEY 不能为空")`
- `scripts/dispatch_test.py`
  - 默认只从 `.env` 的 `X_RPA_KEY` 读取请求头密钥
  - 去掉对旧环境变量 `RPA_KEY` 的默认回退
- `tests/单元测试/测试_机器接入脚本.py`
  - 补充默认读取 `X_RPA_KEY` 的参数解析测试
  - 收紧注册、心跳请求断言，要求都带 `X-RPA-KEY`
  - 将空密钥异常断言更新为 `X_RPA_KEY 不能为空`
- `tests/单元测试/测试_任务派发脚本.py`
  - 补充“不回退 RPA_KEY”的断言
- `browser/反检测.py`
  - 调整极短延迟分支，确保全量回归再次稳定通过
- 兼容性说明：
  - `.env` 文件内容未改动，只统一改为从其中的 `X_RPA_KEY` 读取默认值
  - 两支脚本仍保留 `--rpa-key` 命令行覆盖能力
- 本轮验证命令：
  - `python scripts/machine_worker.py --help`
  - `python scripts/dispatch_test.py --help`
  - `python -m py_compile scripts/machine_worker.py scripts/dispatch_test.py tests/单元测试/测试_机器接入脚本.py tests/单元测试/测试_任务派发脚本.py`
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_机器接入脚本.py tests/单元测试/测试_任务派发脚本.py -v`
  - `python -m pytest -c tests/pytest.ini tests -v`

## 本轮新增任务（machine_worker 路由切换到 /api/workers）

- `scripts/machine_worker.py`
  - 注册路由改为 `POST /api/workers/register`
  - 心跳路由改为 `POST /api/workers/heartbeat`
  - 状态回调路由改为 `PUT /api/workers/{machine_id}/status`
  - 保持注册、心跳、状态回调全部携带 `X-RPA-KEY`
  - 更新脚本顶部说明文字与内置路由常量
- `tests/单元测试/测试_机器接入脚本.py`
  - 同步更新配置地址、注册请求、心跳请求、状态回调请求的 URL 断言
- 兼容性说明：
  - `scripts/dispatch_test.py` 本轮未改动
  - 请求头规则保持不变，仍从 `.env` 的 `X_RPA_KEY` 读取默认值
- 本轮验证命令：
  - `python scripts/machine_worker.py --help`
  - `python -m py_compile scripts/machine_worker.py tests/单元测试/测试_机器接入脚本.py`
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_机器接入脚本.py -v`
  - `python -m pytest -c tests/pytest.ini tests -v`

## 本轮新增任务（新增数据库模型：店铺 / 流程 / 定时任务）

- `backend/models/表结构.py`
  - 新增轻量级表结构声明，统一维护字段定义、建表 SQL 生成和中文属性映射
- `backend/models/店铺模型.py`
  - 新增 `shops` 数据模型，使用中文属性映射英文列名
  - 保留 `user_agent`、`profile_dir`、`smtp_*` 等现有扩展字段，兼容现有店铺服务
- `backend/models/流程模型.py`
  - 新增 `flows` 数据模型
  - 将 `steps` 统一序列化为 `[{task, on_fail}]` JSON 数组
  - 对 `on_fail` 做白名单校验，支持 `skip_shop / continue / log_and_skip / retry:N / abort`
- `backend/models/定时任务模型.py`
  - 新增 `execution_schedules` 数据模型
  - 将 `shop_ids` 序列化为 JSON 数组
  - 校验并发数、调度参数和必填字段
- `backend/models/数据库.py`
  - 改为从模型定义统一建表
  - 初始化时新增 `flows`、`execution_schedules`，并为 `flow_id` 开启外键约束
- `backend/models/__init__.py`
  - 导出新增模型与表定义
- `tests/单元测试/测试_数据库模型.py`
  - 覆盖中文属性到英文列名映射
  - 覆盖 `steps` / `shop_ids` 的正常与异常路径
  - 覆盖数据库初始化后的真实建表结果
- `docs/database.md`
  - 同步补充 `flows`、`execution_schedules` 和新的建表入口说明
- 本轮验证命令：
  - `python -c "from backend.models.店铺模型 import 店铺模型; from backend.models.流程模型 import 流程模型, 流程步骤; from backend.models.定时任务模型 import 定时任务模型; print('ok')"`
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_数据库模型.py -q`
  - `python -m pytest -c tests/pytest.ini -q`

## 本轮新增任务（新增任务注册表机制）

- `tasks/注册表.py`
  - 新增正式任务注册表实现
  - 提供 `register_task(名称, 描述)`、`获取所有任务()`、`获取任务类(名称)`、`获取任务(名称)` 和 `初始化任务注册表()`
  - 初始化时自动扫描 `tasks/` 目录并导入任务模块，后续新增 Task 文件无需修改注册机制
- `tasks/基础任务.py`
  - 新增抽象基类 `基础任务`
  - 统一定义抽象 `执行(self, 页面, 店铺配置) -> str`
  - 新增 `安全执行(...)`，统一包装 success / failed 返回结构
- `tasks/任务注册表.py`
  - 改为兼容导出层，复用 `tasks/注册表.py`，避免影响既有调用方
- `tasks/登录任务.py`
  - 改用 `@register_task("登录", "打开浏览器并登录店铺后台")`
  - 继承 `基础任务`
  - 保持现有登录核心逻辑不变
- `backend/api/可用任务.py`
  - 新增 `GET /api/tasks/available`
  - 从注册表读取任务列表，返回 `{code, msg, data: {tasks: [...]}}`
- `backend/api/路由注册.py`
  - 将可用任务路由注册到应用
  - 注册顺序放在动态 `/{task_id}` 路由前，避免 `/available` 被误匹配
- `tests/单元测试/测试_任务注册表.py`
  - 覆盖基础任务安全执行的成功与异常路径
  - 覆盖任务注册、查找、自动初始化
  - 覆盖 `GET /api/tasks/available` 的真实路由返回
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_任务注册表.py -q`
  - `python -c "from tasks.注册表 import 初始化任务注册表, 获取所有任务; 初始化任务注册表(); print(获取所有任务())"`
  - `python -m pytest -c tests/pytest.ini -q`

## 本轮新增任务（新增流程模板 + 店铺管理 CRUD 接口）

- `backend/services/店铺服务.py`
  - 保留原有店铺 CRUD、Cookie 管理和浏览器相关逻辑
  - 新增店铺名称非空校验
  - 将返回中的 `password`、`smtp_pass` 字段统一遮蔽为 `***`
- `backend/api/店铺接口.py`
  - 为 `GET /api/shops`、`POST /api/shops` 增加无尾斜杠别名
  - 保持现有接口兼容，同时满足验收命令直接访问 `/api/shops`
- `backend/models/数据结构.py`
  - 为店铺响应新增脱敏 `password` 字段
  - 新增 `流程创建请求`、`流程更新请求`、`流程响应`
- `backend/services/流程服务.py`
  - 新增流程模板 CRUD
  - 支持 `steps` 传数组或 JSON 字符串
  - 校验 `steps` 必须是 JSON 数组
  - 校验 `on_fail` 只允许 `skip_shop / continue / log_and_skip / retry:N / abort`
  - 校验每个 step 的 `task` 必须存在于任务注册表
- `backend/api/流程接口.py`
  - 新增 `GET /api/flows`
  - 新增 `POST /api/flows`
  - 新增 `PUT /api/flows/{id}`
  - 新增 `DELETE /api/flows/{id}`
- `backend/api/路由注册.py`
  - 注册流程路由
- `tests/单元测试/测试_店铺和流程接口.py`
  - 覆盖店铺创建、列表、更新、删除
  - 覆盖店铺空名称异常路径
  - 覆盖流程创建、列表、更新、删除
  - 覆盖未知任务和非法 JSON steps 异常路径
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_店铺和流程接口.py -q`
  - `python -m pytest -c tests/pytest.ini -q`
## 本轮新增任务（批量执行 API + Celery 投递）
- `backend/services/执行服务.py`
  - 新增批次创建、停止与 Redis 状态维护逻辑
  - 统一使用 `execute:batch:{batch_id}`、`execute:current`、`execute:status` 管理批次快照与事件
  - 批次投递改为“先 `freeze` 并写状态，再统一 `apply_async()`”，避免 Worker 抢先启动读不到批次元数据
- `backend/api/执行接口.py`
  - 新增 `POST /api/execute/batch`
  - 新增 `POST /api/execute/stop`
  - 新增 `GET /api/execute/status`，通过 SSE 推送批次状态
- `tasks/执行任务.py`
  - 新增批次子任务入口
  - 复用 `任务服务.统一执行任务(...)`
  - 按 `on_fail` 支持 `continue / log_and_skip / abort / retry:N`
- `backend/models/数据结构.py`
  - 新增 `批量执行请求`
  - 新增 `停止执行请求`
- `tasks/celery应用.py`
  - 增加 `tasks.执行任务` 导入，保持其它 Celery 配置不变
- `tasks/注册表.py`
  - 将 `执行任务` 加入排除列表，避免被业务任务自动发现机制扫描
- `backend/api/路由注册.py`
  - 注册执行接口路由
- `tests/单元测试/测试_执行服务.py`
  - 覆盖批次创建、参数校验、未注册任务、停止批次
- `tests/单元测试/测试_执行接口.py`
  - 覆盖批量执行接口统一响应与状态流 SSE 输出
- `tests/单元测试/测试_执行任务.py`
  - 覆盖 `continue / abort / retry:N` 分支
- `browser/反检测.py`
  - 调整极短延时分支，恢复全量回归稳定性
- 兼容性说明：
  - `concurrency` 当前仅记录在批次元数据中，实际并发仍由 Celery Worker 启动参数控制
  - 停止批次通过撤销已冻结的子任务 `task_id` 实现，已投递中的链路会按当前状态落到 `stopped`
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_执行服务.py -q`
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_执行接口.py -q`
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_执行任务.py -q`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - `68 passed`

## 本轮新增任务（定时任务 CRUD + Celery Beat）

- `backend/services/定时执行服务.py`
  - 新增定时计划 CRUD、参数校验、RedBeat 条目同步
  - 支持暂停/恢复时移除或重建 RedBeat 计划
  - 到点触发时复用 `执行服务实例.创建批次(...)`，不复制批量执行编排
  - 使用 `schedule:batch:{schedule_id}` 记录计划最近批次，支持 `skip / wait / parallel` 的基础重叠控制
- `backend/api/定时执行接口.py`
  - 新增 `GET /api/schedules`
  - 新增 `POST /api/schedules`
  - 新增 `PUT /api/schedules/{id}`
  - 新增 `DELETE /api/schedules/{id}`
  - 新增 `POST /api/schedules/{id}/pause`
  - 新增 `POST /api/schedules/{id}/resume`
- `tasks/定时任务.py`
  - 新增 `执行定时计划` Celery 任务入口
  - 复用 Worker 级事件循环执行异步计划触发逻辑
- `backend/models/数据结构.py`
  - 新增 `定时任务创建请求`
  - 新增 `定时任务更新请求`
  - 新增 `定时任务响应`
- `backend/models/__init__.py`
  - 导出新增的定时计划请求/响应模型
- `backend/api/路由注册.py`
  - 注册 schedules 路由
- `tasks/celery应用.py`
  - 追加 `beat_scheduler="redbeat.RedBeatScheduler"`
  - 追加 `redbeat_redis_url=配置实例.REDIS_URL`
  - 将 `tasks.定时任务` 加入 Celery 自动导入列表
- `tasks/注册表.py`
  - 将 `定时任务` 加入自动发现排除列表，避免基础设施任务被业务任务扫描
- `requirements.txt`
  - 添加 `celery-redbeat>=2.0.0`
  - 说明：任务文案写的是 `redbeat`，但 PyPI 实际可安装包名为 `celery-redbeat`，import 名仍为 `redbeat`
- `tests/单元测试/测试_定时执行服务.py`
  - 覆盖计划创建、RedBeat 同步、到点触发批量执行、Cron 运行时间回写和调度参数冲突异常
- `tests/单元测试/测试_定时执行接口.py`
  - 覆盖 schedules 的创建、列表、暂停、恢复、删除和流程不存在异常路径
- 本轮验证命令：
  - `python -m py_compile backend/services/定时执行服务.py backend/api/定时执行接口.py tasks/定时任务.py tests/单元测试/测试_定时执行服务.py tests/单元测试/测试_定时执行接口.py`
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_定时执行服务.py tests/单元测试/测试_定时执行接口.py -q`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - `74 passed`

## 本轮新增任务（新增前端 4 个页面）

- `frontend/src/api/types.ts`
  - 新增前端管理页通用类型，统一店铺、流程、批量执行、定时任务与可用任务的数据结构
- `frontend/src/api/shops.ts`
  - 新增店铺 CRUD 请求封装
- `frontend/src/api/flows.ts`
  - 新增流程模板 CRUD 请求封装
- `frontend/src/api/execute.ts`
  - 新增批量执行启动、停止与状态订阅请求封装
- `frontend/src/api/schedules.ts`
  - 新增定时任务 CRUD、暂停、恢复请求封装
- `frontend/src/api/tasks.ts`
  - 新增可用任务列表请求封装
- `frontend/src/views/ShopManage.vue`
  - 重写为通用店铺管理页
  - 移除平台耦合动作，只保留列表、新增/编辑、删除与状态展示
- `frontend/src/views/FlowManage.vue`
  - 新增流程模板页
  - 支持步骤增删、拖拽排序、复制、任务下拉与失败策略配置
- `frontend/src/views/BatchExecute.vue`
  - 新增批量执行页
  - 支持流程/单任务模式切换、店铺多选、并发数选择、SSE 进度展示
  - 修复默认选中流程/任务时的空值类型保护
- `frontend/src/views/ScheduleManage.vue`
  - 新增定时任务页
  - 支持固定间隔与 Cron 调度、店铺多选、暂停/恢复/删除
  - 编辑时显式清空未选中的 `interval_seconds` / `cron_expr`，避免后端保留旧值
- `frontend/src/router/index.ts`
  - 追加 `/flows`、`/execute`、`/schedules` 路由，不改现有路由
- `frontend/src/App.vue`
  - 追加流程模板、批量执行、定时任务导航入口
  - 侧边栏标题调整为 `自动化工作台`
- `tests/单元测试/测试_前端管理页.py`
  - 新增前端管理页烟雾测试，校验路由与页面关键结构
- 兼容性说明：
  - `ScheduleManage.vue` 的“执行中”状态当前基于 `enabled + last_run_at + interval_seconds` 做前端近似推断，因为后端暂未返回独立执行态字段
  - `ShopManage.vue` 沿用 `/shops` 现有入口，按任务要求直接升级为通用版，不新增重复路由
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_前端管理页.py -q`
  - `python -m pytest -c tests/pytest.ini -q`
  - `cd frontend && npx vue-tsc -b`
  - `cd frontend && npm run build`
- 本轮验证结果：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_前端管理页.py -q` 通过（`3 passed`）
  - `python -m pytest -c tests/pytest.ini -q` 通过（`77 passed`）
  - `cd frontend && npx vue-tsc -b` 通过
  - `cd frontend && npm run build` 在当前沙箱失败：`spawn EPERM`，失败点为 Vite/esbuild 启动子进程，不是新的前端类型错误

## 本轮新增任务（恢复店铺模型字段与店铺页布局）

- 历史对照：
  - 通过 `git show 210f396:backend/models/数据库.py`、`git show 210f396:backend/services/店铺服务.py`、`git show 210f396:frontend/src/views/ShopManage.vue` 对照改造前状态
  - 确认原有 `shops` 字段包括 `user_agent`、`profile_dir`、`cookie_path`、`last_login`、`smtp_host`、`smtp_port`、`smtp_user`、`smtp_pass`、`smtp_protocol`、`remark`
  - 当前后端模型与服务未丢失这些列，本轮重点恢复前端类型、交互封装与旧布局
- `frontend/src/api/types.ts`
  - 为 `Shop` 补回 `smtp_pass`
  - 为 `ShopPayload` 补回 `user_agent`、邮箱配置与 `remark`，兼容旧字段
- `frontend/src/api/shops.ts`
  - 新增 `openShopBrowser`
  - 新增 `checkShopStatus`
  - 新增 `testShopEmailConnection`
- `frontend/src/views/ShopManage.vue`
  - 恢复旧版店铺页结构
  - 重新使用小卡片网格 `shops-grid`
  - 移除顶部统计卡片区域
  - 恢复邮箱配置表单、测试连接按钮、打开/检查/编辑/删除交互
- `frontend/src/components/ShopCard.vue`
  - 放宽 `Shop` props 类型，兼容 `last_login`、`smtp_user` 等可空字段
- `tests/单元测试/测试_店铺恢复.py`
  - 覆盖 `/api/shops` 返回旧字段与脱敏字段
  - 覆盖店铺页恢复为旧卡片布局、邮箱字段和店铺动作封装
- `browser/反检测.py`
  - 微调极短随机延迟分支，避免 Windows 调度抖动导致全量测试偶发失败
- 兼容性说明：
  - 后端 `shops` 模型和 CRUD 结构无需新增列，本轮主要是恢复前端消费这些旧字段的能力
  - 店铺页保留新增的编辑弹窗、删除确认，但布局和样式恢复为旧版卡片式展示
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_店铺恢复.py -q`
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_前端管理页.py -q`
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_反检测.py -q`
  - `python -m pytest -c tests/pytest.ini -q`
  - `cd frontend && npx vue-tsc -b`
- 本轮验证结果：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_店铺恢复.py -q` 通过（`2 passed`）
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_前端管理页.py -q` 通过（`3 passed`）
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_反检测.py -q` 通过（`6 passed`）
  - `python -m pytest -c tests/pytest.ini -q` 通过（`79 passed`）
  - `cd frontend && npx vue-tsc -b` 通过

## 本轮新增任务（修复流程编排页面显示细节）

- `frontend/src/views/FlowManage.vue`
  - 失败策略下拉改为中文显示，保留英文原值
  - `retry:N` 选项的内部值改为 `retry:N`，保持与任务文档一致
  - 流程卡片中的失败策略预览同步改为中文文案
  - 任务下拉不再拼接 `name + description`
  - 任务描述拆到选择框下方的副文本提示，并保留 `title` 作为悬浮提示
- `frontend/src/views/ShopManage.vue`
  - 复核后确认编辑弹窗密码输入框已满足要求：`type="password"`、编辑时 placeholder 为 `留空则不修改`、编辑态不会回显后端脱敏值
  - 本轮无需继续修改该文件逻辑
- `tests/单元测试/测试_前端显示细节.py`
  - 新增显示细节回归测试
  - 覆盖失败策略中文显示、任务下拉不拼接描述、店铺密码输入框细节
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_前端显示细节.py tests/单元测试/测试_前端管理页.py -q`
  - `python -m pytest -c tests/pytest.ini -q`
  - `cd frontend && npx vue-tsc -b`
- 本轮验证结果：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_前端显示细节.py tests/单元测试/测试_前端管理页.py -q` 通过（`5 passed`）
  - `python -m pytest -c tests/pytest.ini -q` 通过（`81 passed`）
  - `cd frontend && npx vue-tsc -b` 通过
## 本轮新增任务（修复批量执行时的店铺名显示）

- `backend/services/执行服务.py`
  - 创建批次时先读取店铺详情，缓存真实店铺名
  - 批次 `shops` 快照新增 `shop_name`
  - 投递 Celery 子任务时追加 `shop_name`
- `tasks/执行任务.py`
  - 增加 `shop_name` 参数
  - Worker 开始执行日志改为优先打印真实店铺名
  - 将 `shop_name` 透传给任务记录参数和统一执行入口
  - 在兼容旧返回结构的前提下，仅在显式传入时回填 `shop_name`
- `backend/services/任务服务.py`
  - 统一执行日志优先显示 `shop_name`
  - 成功/失败返回结果补充 `shop_name`
- `frontend/src/api/types.ts`
  - `BatchShopState` 新增 `shop_name?: string | null`
- `frontend/src/views/BatchExecute.vue`
  - 新增 `getBatchShopName(shop)`
  - 等待中、执行中、已完成三列统一优先显示快照里的真实店铺名
- `tests/单元测试/测试_批量执行店铺名.py`
  - 覆盖真实店铺名透传、缺省名回退、Worker 日志、任务服务日志和前端静态展示
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_批量执行店铺名.py tests/单元测试/测试_执行服务.py tests/单元测试/测试_执行任务.py tests/单元测试/测试_任务服务.py -q`
  - `cd frontend && npx vue-tsc -b`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 针对性测试 `15 passed`
  - 全量测试 `87 passed`

## 本轮新增任务（生产环境审查修复）

- `backend/models/数据库.py`
  - 统一 SQLite 连接配置，启用 `WAL`、`busy_timeout`、`synchronous=NORMAL` 和 `row_factory`
  - 初始化阶段增加旧库迁移，自动补齐 `operation_logs.shop_name`
- `backend/services/任务服务.py`
  - 任务日志查询、写入、更新、清理统一改走 `获取连接()`
- `backend/services/日志服务.py`
  - 操作日志写入/查询/清理统一改走 `获取连接()`
  - 结合日志表迁移，修复 `shop_name` 字段写入与查询兼容性
- `backend/services/系统服务.py`
  - 健康检查中的数据库探活改走统一连接入口
- `backend/services/店铺服务.py`
  - 未配置 `ENCRYPTION_KEY` 时改为回退到 `data/.encryption.key` 持久化密钥文件，避免服务重启后历史密码失效
  - 删除店铺前先扫描关联定时计划：仅剩该店铺时删除计划，多店铺计划时移除该 `shop_id`
- `backend/services/执行服务.py`
  - 订阅批次状态时增加空闲心跳标记，避免长连接在无事件期被代理或浏览器回收
- `backend/api/执行接口.py`
  - 将执行状态保活标记转换为 SSE 注释帧 `: ping`
- `backend/api/日志接口.py`
  - 日志流在空闲期按固定间隔输出 SSE 注释帧 `: ping`
- `tasks/celery应用.py`
  - 增加 `task_reject_on_worker_lost=True`
  - 增加 `broker_connection_retry_on_startup=True`
- `tests/单元测试/测试_生产环境审查.py`
  - 新增 SQLite 配置/迁移、持久化密钥、店铺删除级联、SSE 保活和 Celery 生产配置回归测试
- 风险说明：
  - 启动阶段仍未对 Redis 可用性和数据库可写性做 fail-fast 检查，这一项保留为后续风险项，不在本轮改变启动语义
  - `shop_ids`、`steps` 仍缺少更严格的上限约束，本轮保留现有接口行为，只在审查结论中提示
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_生产环境审查.py -q`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 针对性测试 `6 passed`
  - 全量测试 `93 passed`

## 本轮新增任务（修复多线程并发执行时事件循环冲突）

- `tasks/celery应用.py`
  - Worker 事件循环改为按线程分配和复用，不再让 `threads` 池中的多个任务共用同一个 loop
  - 增加线程级事件循环注册表和初始化锁，保证 Worker 环境初始化只执行一次
- `tasks/执行任务.py`
  - 按任务要求引入 `nest_asyncio.apply()`
  - `_运行异步任务(...)` 遇到当前线程已有运行中事件循环时，回退到临时线程执行协程，避免 `This event loop is already running`
  - 保留当前 Worker 线程的专属事件循环复用逻辑，避免每次任务都新建 loop
- `tasks/桥接任务.py`
  - 同步增强 `_运行异步任务(...)`，和批量执行任务保持一致的线程安全行为
- `backend/services/浏览器服务.py`
  - 对主线程保留原有全局单例语义
  - 对 Worker 线程池改为线程隔离的浏览器管理器与初始化锁，确保不同线程任务不共享同一个 Playwright 实例
- `backend/services/任务服务.py`
  - 不再直接读取 `浏览器服务.管理器实例` 全局变量，改为获取当前线程绑定的管理器实例
- `requirements.txt`
  - 新增 `nest_asyncio`
- `tests/单元测试/测试_线程池事件循环.py`
  - 覆盖不同线程返回独立 Worker 事件循环
  - 覆盖当前线程已有运行中 loop 时的回退路径
  - 覆盖不同线程的浏览器服务使用独立管理器实例
- 环境说明：
  - 当前沙箱执行 `python -m pip install nest_asyncio` 时因用户目录写权限不足失败
  - 仓库已补 `requirements.txt`，并在 `tasks/执行任务.py` 增加缺包兜底，正式环境仍应通过 `pip install -r requirements.txt` 安装依赖
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_线程池事件循环.py -q`
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_Celery桥接.py tests/单元测试/测试_浏览器服务.py tests/单元测试/测试_执行任务.py tests/单元测试/测试_任务服务.py -q`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 针对性测试 `13 passed`
  - 全量测试 `97 passed`

## 本轮新增任务（新增批量执行完成后回调 Agent）

- `backend/services/执行服务.py`
  - 新增批次完成回调地址解析逻辑，优先使用请求体 `callback_url`，否则回退到 `AGENT_CALLBACK_URL`，默认根地址为 `http://localhost:8001`
  - 新增批次完成回调载荷构建逻辑，将内部批次快照转换为 `{batch_id, status, total, completed, failed, results}`
  - 在同步批次状态写入后检测终态，并通过 Redis `NX` 标记保证完成回调只发送一次
  - 回调失败时只打印日志，不影响批次状态落库和 Redis 推送
- `backend/models/数据结构.py`
  - 为 `批量执行请求` 新增可选字段 `callback_url`
- `backend/api/执行接口.py`
  - 透传 `callback_url` 给执行服务创建批次入口
- `tests/单元测试/测试_批量执行回调.py`
  - 覆盖回调载荷 `partial_failed` 场景
  - 覆盖自定义 `callback_url` 写入批次元数据
  - 覆盖回调异常不影响主流程
  - 覆盖 REST 接口透传 `callback_url`
- 兼容性说明：
  - 未改动现有桥接任务回调逻辑
  - 未改动 Agent 端代码，只在本项目增加向 Agent 回调的客户端逻辑
  - 内部批次 `status` 仍保持现有 `running/completed/failed/stopped` 语义，回调载荷额外转换为 `completed/partial_failed`
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_批量执行回调.py tests/单元测试/测试_执行服务.py tests/单元测试/测试_执行接口.py -q`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 针对性测试 `11 passed`
  - 全量测试 `101 passed`

## 本轮新增任务（修复 Worker 机器注册兼容 threads 池）

- `backend/配置.py`
  - 新增 `MACHINE_NAME`、`X_RPA_KEY` 配置项，供 Worker 注册逻辑统一读取
- `tasks/celery应用.py`
  - 保留 `worker_init` 作为启动钩子，不再依赖 `worker_ready`
  - 新增 Agent 根地址推导、注册请求头构建、业务响应校验和 Worker 机器注册逻辑
  - `初始化Worker环境()` 在任务注册表与回调地址配置后主动调用 `POST /api/machines`
  - 注册失败时仅打印日志，不阻塞 Worker 启动，也不会影响 `Worker环境已初始化` 置为 `True`
- `tests/单元测试/测试_Celery桥接.py`
  - 新增初始化即注册机器断言
  - 新增注册失败时静默吞吐断言
- `browser/反检测.py`
  - 将 30ms 内短延迟收敛到区间中值，消除 Windows 环境下 `测试_随机延迟在范围内` 的偶发抖动
- 环境说明：
  - 下一步任务 07 需要修改 `E:\Agent`，但当前沙箱可写根仅包含 `E:\douyin` 与 memories，暂时无法直接落盘到 `E:\Agent`
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_Celery桥接.py -q`
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_反检测.py -q`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 针对性测试 `12 passed`
  - 全量测试 `103 passed`

## 本轮新增任务（生产环境全面审查补漏）

- `browser/任务回调.py`
  - 自动回调统一补齐 `X-RPA-KEY` 请求头
  - 增加 Agent 统一业务响应校验，避免 200 + `code != 0` 被误判为成功
  - 未配置 `X_RPA_KEY` 时跳过回调，不再发送匿名请求
- `backend/services/心跳服务.py`
  - 心跳请求体改回 Agent 既有约定的 `{machine_id, shadowbot_running}`
  - 心跳请求统一携带 `X-RPA-KEY`
  - 为心跳请求增加 HTTP 状态和业务响应双重校验
- `backend/services/执行服务.py`
  - 批次完成回调默认不再直接复用完整 `AGENT_CALLBACK_URL`，而是提取 Agent 根地址后固定拼接 `/api/batch-callback`
  - 批次完成回调统一携带 `X-RPA-KEY`，并校验 Agent 统一业务响应
- `tests/单元测试/测试_Celery桥接.py`
  - 将 Worker 注册成功断言修正为 `POST /api/workers/register`
- `tests/单元测试/测试_批量执行回调.py`
  - 覆盖默认批次回调地址拼装、鉴权头注入和业务响应校验
- `tests/单元测试/测试_心跳服务.py`
  - 覆盖心跳请求体使用 `shadowbot_running` 和 `X-RPA-KEY`
- `tests/单元测试/测试_任务回调.py`
  - 新增任务自动回调的协议回归测试
  - 覆盖业务失败时静默吞吐，不影响任务主流程
- 协议说明：
  - Worker 注册接口保持 `POST /api/workers/register`
  - Worker 心跳接口保持 `POST /api/workers/heartbeat`
  - 机器状态回调保持 `PUT /api/workers/{machine_id}/status`
  - Worker → Agent 请求统一要求 `X-RPA-KEY`
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_Celery桥接.py tests/单元测试/测试_批量执行回调.py tests/单元测试/测试_心跳服务.py tests/单元测试/测试_任务回调.py -q`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 针对性测试 `16 passed`
  - 全量测试 `107 passed`

## 本轮新增任务（拼多多登录流程改造）

- `pages/登录页.py`
  - 登录地址切换为 `https://mms.pinduoduo.com/login/`
  - 首页地址切换为 `https://mms.pinduoduo.com/home`
  - 删除抖店邮箱登录、协议勾选和邮箱验证码检测方法
  - 新增 `切换账号登录()`、`填写手机号()`、`检测短信验证码()`
  - 登录成功与 Cookie 有效性统一改为按拼多多 `/home` URL 判定
- `tasks/登录任务.py`
  - 登录主流程改为 Cookie 优先 → 手机号密码登录 → 滑块验证码 → 短信验证码 → 保存 Cookie
  - 移除 `smtp_host` 邮箱验证码分支
  - 移除任务层旧 `.user-info` 选择器依赖，成功判断完全回到登录页 POM
  - `context destroyed` 场景改为按 `mms.pinduoduo.com/home` 兜底判定成功
- `tests/单元测试/测试_登录页.py`
  - 新增拼多多登录页 POM 单元测试
  - 覆盖账号登录切换、手机号/密码输入、登录按钮定位、短信验证码检测和首页 URL 判定
- `tests/单元测试/测试_登录任务.py`
  - 重写登录任务单元测试
  - 覆盖 Cookie 有效直登、手机号密码登录成功、滑块验证码、短信验证码和 `context destroyed` 分支
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_登录页.py tests/单元测试/测试_登录任务.py -q`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 针对性测试 `13 passed`
  - 全量测试 `117 passed`
- 注意事项：
  - 登录任务人工输入分支返回值已从 `需要邮箱验证码` 调整为 `需要短信验证码`
  - 全量测试仍有 6 条 Celery `datetime.utcnow()` 弃用警告，为现有存量问题，非本轮引入

## 本轮新增任务（短信验证码输入后继续判断登录结果）

- `tasks/登录任务.py`
  - 短信验证码检测后不再直接返回 `需要短信验证码`
  - 改为最长轮询 `120` 秒，每 `3` 秒检查一次当前 URL 是否已跳转到 `https://mms.pinduoduo.com/home`
  - 跳转成功后立即记录成功日志、保存 Cookie、补截图并返回 `成功`
  - 超时未跳转时记录失败日志、补截图并返回 `失败`
- `tests/单元测试/测试_登录任务.py`
  - 将原短信验证码等待人工输入用例改为“验证通过后成功登录”
  - 新增短信验证码等待超时返回失败用例
  - 通过 mock `asyncio.sleep` 避免测试真实等待 120 秒
- 本轮验证命令：
  - `python -m pytest -c tests/pytest.ini tests/单元测试/测试_登录任务.py tests/单元测试/测试_登录页.py -q`
  - `python -m pytest -c tests/pytest.ini -q`
- 本轮验证结果：
  - 针对性测试 `14 passed`
  - 全量测试 `118 passed`
- 注意事项：
  - 当前短信验证码分支的最终返回值改为 `成功` 或 `失败`，不再直接返回 `需要短信验证码`
  - 全量测试仍有 6 条 Celery `datetime.utcnow()` 弃用警告，为现有存量问题，非本轮引入
