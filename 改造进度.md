# 改造进度

## 本轮进度：设置页 Celery 动态刷新、Redis URL 归一化与系统测试接口

- `tasks/celery_app.py`
  - 新增 `刷新Celery配置()`，当 settings 中的 Redis / Celery backend 配置变化时，动态更新 Celery app 的 broker、backend、redbeat 地址
  - 刷新时同步清理 `_pool`、`amqp._producer_pool` 与 backend 缓存，避免继续复用旧连接
- `backend/services/system_service.py`
  - 旧 `/api/system/config` 兼容层在保存 `redis_url` 前自动修正常见格式错误
  - 保存完成后最佳努力刷新 Celery 配置，解决“改完设置仍需重启后端才生效”的问题
- `backend/api/settings_api.py`
  - 当前 `/settings` 页面实际走的 `/api/settings` / `/api/settings/batch` 也补上了同样的 Redis URL 规范化与 Celery 刷新
  - 这样通过新设置页修改 `celery_broker_url` / `celery_result_backend` 时，主进程侧会立即感知新配置
- `backend/models/data_structure.py`
  - 新增 `验证码测试请求`、`飞书Webhook测试请求`
  - 兼容旧字段别名，避免旧页面调用参数名不一致时直接失败
- `backend/api/system_api.py`
  - 新增 `POST /api/system/test-captcha`，支持请求体优先、配置回退，并对 YesCaptcha 调用余额接口
  - 新增 `POST /api/system/test-feishu-webhook`，支持请求体优先、配置回退，可选飞书签名
- `tasks/execute_task.py`
  - 每次 Worker 执行任务前先刷新一次 Celery 配置，减少 Redis 地址变更后任务仍卡在等待中的概率
- 测试补齐
  - 新增 Celery 配置刷新、系统服务跟进补丁、系统测试接口、执行任务前刷新、settings API 写入链路补丁 5 组单测
  - 同时验证了现有 `system_api`、`settings_api`、`execute_task`、`celery_bridge`、飞书相关回归未被破坏

## 测试与验证

- 已执行新增定向测试：
  - `python -m pytest tests/unit/test_celery_config_refresh.py -q`
  - `python -m pytest tests/unit/test_system_followup.py -q`
  - `python -m pytest tests/unit/test_system_api_followup.py -q`
  - `python -m pytest tests/unit/test_execute_task_refresh.py -q`
  - `python -m pytest tests/unit/test_settings_api_followup.py -q`
- 已执行受影响回归：
  - `python -m pytest tests/unit/test_system_api.py tests/unit/test_settings_api.py tests/unit/test_execute_task.py tests/unit/test_celery_bridge.py tests/unit/test_system_set_machine_code.py -q`
  - `python -m pytest tests/test_feishu_service.py -q`
- 已执行全量回归：
  - `python -m pytest -c tests/pytest.ini -q`
  - 结果：`525 passed, 18 warnings`

## 注意事项

- 本轮把旧 `/api/system/config` 和当前 `/api/settings` 两条设置写入链路都补齐了 Celery 刷新，避免只修兼容层、不修真实页面入口。
- Redis URL 会自动修正 `redis://host/:6380/0 -> redis://host:6380/0` 这一类常见脏格式。
- `POST /api/system/test-captcha` 当前按任务要求支持 `yescaptcha` 余额测试；其他验证码服务商暂未扩展。
- 18 条 warnings 仍来自既有第三方依赖 `celery`、`openpyxl` 和既有 `PytestUnraisableExceptionWarning`，不是本轮新增问题。
